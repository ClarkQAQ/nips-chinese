# NIP-A3

## payto: 支付目标

`draft` `optional`

本 NIP 标准化了如何在任何网络或平台上声明支付地址。

#### 标签格式

支付目标使用 `payto` 标签指定，结构如下：

```text
["payto", "<type>", "<address>"]
```

其中：
- 第一个元素始终是字面字符串 `"payto"`
- 第二个元素是支付 `type`（例如 `"bitcoin"`、`"lightning"`），始终小写。
- 第三个元素是 `address`（例如地址、用户名）

客户端可以对已识别的类型执行额外的网络或平台特定验证。

### 示例

```json
{
  "pubkey": "afc93622eb4d79c0fb75e56e0c14553f7214b0a466abeba14cb38968c6755e6a",
  "kind": 10133,
  "content": "",
  "tags": [
    ["payto", "bitcoin", "bc1qxq66e0t8d7ugdecwnmv58e90tpry23nc84pg9k"],
    ["payto", "nano", "nano_1dctqbmqxfppo9pswbm6kg9d4s4mbraqn8i4m7ob9gnzz91aurmuho48jx3c"],
    ["payto", "unknowntype", "l7tbta5b9xze6ckkfc99uohzxd009b0r"]
  ],
  ...
}
```

对于 kind `10133` 事件中的每个 `payto` 标签，客户端可以将支付地址渲染为按钮或链接。当存在可用的特定 URI 方案时，客户端可以使用该方案，例如 `bitcoin:<address>` 或 `ethereum:<address>`。否则回退到 [RFC-8905](https://www.rfc-editor.org/rfc/rfc8905.html) 定义的 `payto` URI 方案：`payto://<type>/<address>`。

可能渲染出的 URI：
- `bitcoin:bc1qxq66e0t8d7ugdecwnmv58e90tpry23nc84pg9k`
- `payto://nano/nano_1dctqbmqxfppo9pswbm6kg9d4s4mbraqn8i4m7ob9gnzz91aurmuho48jx3c`
- `payto://unknowntype/l7tbta5b9xze6ckkfc99uohzxd009b0r`

### 常用标签

- `bip352`（静默支付）
- `bip353`（DNS 地址）
- `bitcoin`
- `cashme`（Cash App 的 `$` 前缀 cashtag）
- `ethereum`
- `lightning`（闪电网络地址）
- `litecoin`
- `monero`
- `nano`
- `paypal`
- `revolut`
- `solana`
- `venmo`
- `zcash`

新的广泛部署的格式之后可以添加到该列表中。
