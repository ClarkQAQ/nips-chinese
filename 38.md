
NIP-38
======

用户状态
-------------

`draft` `optional`

## 摘要

本 NIP 使用户能够分享直播状态，例如他们正在收听什么音乐，以及他们当前正在做什么：工作、娱乐、外出等。

## 直播状态

定义了一个特殊的 `kind:30315` "用户状态"事件，它是一个*可选择设置过期时间的* *可寻址事件*，其中 `d` 标签代表状态类型：

例如：

```json
{
  "kind": 30315,
  "content": "注册 nostrasia!",
  "tags": [
    ["d", "general"],
    ["r", "https://nostr.world"]
  ],
}
```

```json
{
  "kind": 30315,
  "content": "Intergalatic - Beastie Boys",
  "tags": [
    ["d", "music"],
    ["r", "spotify:search:Intergalatic%20-%20Beastie%20Boys"],
    ["expiration", "1692845589"]
  ],
}
```

定义了两种常见的状态类型：`general`（通用）和 `music`（音乐）。`general` 表示通用状态："工作中"、"徒步中"等。

`music` 状态事件用于直播您当前正在收听的音乐。`music` 状态的过期时间应为音乐曲目停止播放的时间。

可以使用任何其他状态类型，但它们不由本 NIP 定义。

状态可以包含一个 `r`、`p`、`e` 或 `a` 标签，用于链接到 URL、个人资料、笔记或可寻址事件。

`content` 可以包含表情符号或 [NIP-30](30.md) 自定义表情符号。如果 `content` 为空字符串，则客户端应清除该状态。

## 客户端行为

客户端可以在帖子或个人资料上的用户名旁边显示此信息，以提供实时的用户状态信息。

## 使用场景

* 日历 nostr 应用，当您在会议中时更新您的通用状态
* Nostr Nests，当您加入时更新您的通用状态并附带指向该 nest 的链接
* Nostr 音乐流媒体服务，当您收听时更新您的音乐状态
* 播客应用，当您收听播客时更新您的音乐状态，并附带一个供他人收听的链接
* 客户端可以使用系统媒体播放器来更新正在播放的音乐状态
