> __警告__  `不推荐`: 已弃用，推荐使用 [NIP-17](17.md)

NIP-04
======

加密的直接消息
------------------------

`final` `unrecommended` `optional` `relay`

kind 为 `4` 的特殊事件，表示"加密的直接消息"。它应具有以下属性：

**`content`** 必须等于 base64 编码的 AES-256-CBC 加密字符串，内容为用户想要书写的任何内容，使用通过接收者的公钥与发送者的私钥组合生成的共享密码进行加密；并在其后附加 base64 编码的初始化向量，作为名为 "iv" 的查询字符串参数。格式如下：`"content": "<encrypted_text>?iv=<initialization_vector>"`。

**`tags`** 必须包含一个标识消息接收者的条目（以便中继可以自然地将此事件转发给他们），格式为 `["p", "<pubkey, as a hex string>"]`。

**`tags`** 可以包含一个标识对话中上一条消息或我们明确回复的消息的条目（以便进行上下文相关的、更有组织的对话），格式为 `["e", "<event_id>"]`。

**注意**：在 [libsecp256k1](https://github.com/bitcoin-core/secp256k1) ECDH 实现的默认行为中，密钥是共享点（X 和 Y 坐标）的 SHA256 哈希值。在 Nostr 中，仅使用共享点的 X 坐标作为密钥，并且不进行哈希处理。如果使用 libsecp256k1，则必须在 `secp256k1_ecdh` 中将一个复制 X 坐标的自定义函数作为 `hashfp` 参数传入。参见[此处](https://github.com/bitcoin-core/secp256k1/blob/master/src/modules/ecdh/main_impl.h#L29)。

在 JavaScript 中生成此类事件的代码示例：

```js
import crypto from 'crypto'
import * as secp from '@noble/secp256k1'

let sharedPoint = secp.getSharedSecret(ourPrivateKey, '02' + theirPublicKey)
let sharedX = sharedPoint.slice(1, 33)

let iv = crypto.randomFillSync(new Uint8Array(16))
var cipher = crypto.createCipheriv(
  'aes-256-cbc',
  Buffer.from(sharedX),
  iv
)
let encryptedMessage = cipher.update(text, 'utf8', 'base64')
encryptedMessage += cipher.final('base64')
let ivBase64 = Buffer.from(iv.buffer).toString('base64')

let event = {
  pubkey: ourPubKey,
  created_at: Math.floor(Date.now() / 1000),
  kind: 4,
  tags: [['p', theirPublicKey]],
  content: encryptedMessage + '?iv=' + ivBase64
}
```

## 安全警告

本标准远未达到点对点加密通信的当前最高水平，并且会在事件中泄露元数据，因此不得用于任何真正需要保密的内容，并且只能与使用 `AUTH` 限制谁能获取你的 `kind:4` 事件的中继一起使用。

## 客户端实现警告

客户端*不应*在 `.content` 中搜索并替换公钥或笔记引用。如果像处理普通文本笔记一样处理（将 `@npub...` 替换为带有 `["p", "..."]` 标签的 `#[0]`），标签会被泄露，并且被提及的用户会在其收件箱中收到该消息。
