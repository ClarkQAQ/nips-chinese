NIP-37
======

草稿包装（Draft Wraps）
-----------

`draft` `optional`

本 NIP 定义了 kind `31234`，作为任何其他 kind 的未签名草稿事件的加密存储。

草稿经过 JSON 字符串化，[NIP-44 加密](44.md) 到签名者的公钥，并放置在 `.content` 中。

`k` 标签标识草稿的 kind。

```js
{
  "kind": 31234,
  "tags": [
    ["d", "<identifier>"],
    ["k", "<草稿事件的 kind>"], // 必需
    ["expiration", "当前时间 + 90 天"] // 推荐
  ],
  "content": nip44Encrypt(JSON.stringify(draft_event)),
  // 其他字段
}
```

空的 `.content` 字段表示草稿已被删除。

建议使用 [NIP-40](40.md) 的 `expiration` 标签。

客户端应将 kind `31234` 事件发布到下面 kind `10013` 中列出的中继。

## 检查点

`kind:1234` 定义了属于父级 `kind:31234` 事件的检查点。这些可用于提供父事件的修订历史。

```js
{
  "kind": 1234,
  "tags": [
    ["a", "31234:<pubkey>:<identifier>"]
  ],
  "content": nip44Encrypt(JSON.stringify(draft_event)),
}
```

## 私有内容的中继列表

Kind `10013` 表示用户首选用于存储私有事件（如草稿包装）的中继。

事件必须在私有标签中包含一个 `relay` URL 列表。私有标签经过 JSON 字符串化、[NIP-44 加密](44.md) 到签名者的密钥，并放置在事件的 `.content` 中。

```js
{
  "kind": 10013,
  "tags": [],
  "content": nip44Encrypt(
    JSON.stringify(
      [
        ["relay", "wss://myrelay.mydomain.com"]
      ]
    )
  )
  //...其他字段
}
```

建议私有存储中继应进行 [NIP-42](42.md) 认证，并且只允许下载由认证用户签名的事件。

客户端必须将 kind `10013` 事件发布到作者的 [NIP-65](65.md) `write` 中继。
