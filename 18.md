NIP-18
======

转发
----

`草案` `可选`

转发是一个 `kind 6` 事件，用于向关注者表明某条 `kind 1` 文本笔记值得阅读。

转发事件的 `content` 是_被转发笔记的字符串化 JSON_。它也可以为空，但不推荐。对 [NIP-70](70.md) 保护的事件的转发应始终使用空的 `content`。

转发事件必须包含一个 `e` 标签，带有被转发笔记的 `id`。该标签必须包含一个中继 URL 作为其第三项，以指示可以在哪里获取它。

转发应包含一个 `p` 标签，带有被转发事件的 `pubkey`。

## 引用转发

对任何事件中 [NIP-21](21.md) 实体（如 `nevent`、`note` 和 `naddr`）的提及必须转换为 `q` 标签。`q` 标签确保引用转发不会被拉取并作为讨论串中的回复包含进去。它还允许你轻松拉取和统计某条帖子的所有引用。语法如下：

`["q", "<event-id> or <event-address>", "<relay-url>", "<pubkey-if-a-regular-event>"]`

## 通用转发

由于 `kind 6` 转发保留给 `kind 1` content，我们使用 `kind 16` 作为"通用转发"，它可以包含除 `kind 1` 之外的任何 kind 事件。

`kind 16` 转发应包含一个 `"k"` 标签，其值为被转发事件的字符串化 kind 编号。

当转发可替换事件时，转发应包含一个 `"a"` 标签，带有被转发事件的事件坐标（`kind:pubkey:d-tag`）。

如果 `"a"` 标签不存在，则表示正在转发可替换事件的特定版本，此时 `content` 字段必须包含被转发事件的完整 JSON 字符串。
