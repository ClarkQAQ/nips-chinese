NIP-43
======

中继访问元数据和请求
------------------

`draft` `optional` `relay`

本 NIP 定义了中继发布成员列表以及客户端代表用户请求中继准入的方式。

## 角色定义

中继可以发布一个 `kind 33534` 事件，该事件定义了一个可分配给成员的角色。此事件必须由中继 [NIP 11](./11.md) 文档的 `self` 字段中指定的公钥签名。

以下是必需的标签：

- 一个 [NIP 70](./70.md) `-` 标签
- 事件的 `d` 标签定义角色 ID。

以下是可选的标签：

- `label` - 角色的标签。
- `description` - 角色的描述。
- `color` - 角色的 `hue` 值，范围从 `0` 到 `360`。
- `order` - 角色的 `order` 整数（仅用于客户端显示）。

```yaml
{
  "kind": 33534,
  "pubkey": "<nip11.self>",
  "tags": [
    ["-"],
    ["d", "28b7e50f"],
    ["label", "king"],
    ["description", "ruler of the relay"],
    ["color", "37"],
    ["order", "1"],
  ],
  // ...其他字段
}
```

中继可以根据中继访问策略对具有给定角色的成员区别对待，但目前未定义任何内省机制。

## 成员列表

中继可以发布一个 `kind 13534` 事件，指示哪些公钥有权访问给定中继。此事件必须由中继 [NIP 11](./11.md) 文档的 `self` 字段中指定的公钥签名。

以下是必需的标签：

- 一个 [NIP 70](./70.md) `-` 标签
- 应为每个成员包含一个包含十六进制公钥的 `member` 标签。分配的角色可以作为后续参数包含。

此列表不应被视为详尽或权威的。要确定会员资格，应同时参考中继的 `kind 13534` 事件和成员的 `kind 10010` 事件。

示例：

```yaml
{
  "kind": 13534,
  "pubkey": "<nip11.self>",
  "tags": [
    ["-"],
    ["member", "c308e1f882c1f1dff2a43d4294239ddeec04e575f2d1aad1fa21ea7684e61fb5"],
    ["member", "ee1d336e13779e4d4c527b988429d96de16088f958cbf6c074676ac9cfd9c958", "28b7e50f"]
  ],
  // ...其他字段
}
```

## 添加用户

中继可以在成员被添加到中继时发布一个 `kind 8000` 事件。此事件必须由中继 [NIP 11](./11.md) 文档的 `self` 字段中指定的公钥签名。

以下是必需的标签：

- 一个 [NIP 70](./70.md) `-` 标签
- 一个 `p` 标签，指示成员的十六进制公钥

示例：

```yaml
{
  "kind": 8000,
  "pubkey": "<nip11.self>",
  "tags": [
    ["-"],
    ["p", "c308e1f882c1f1dff2a43d4294239ddeec04e575f2d1aad1fa21ea7684e61fb5"]
  ],
  // ...其他字段
}
```

## 移除用户

中继可以在成员被从中继移除时发布一个 `kind 8001` 事件。此事件必须由中继 [NIP 11](./11.md) 文档的 `self` 字段中指定的公钥签名。

以下是必需的标签：

- 一个 [NIP 70](./70.md) `-` 标签
- 一个 `p` 标签，指示成员的十六进制公钥

示例：

```yaml
{
  "kind": 8001,
  "pubkey": "<nip11.self>",
  "tags": [
    ["-"],
    ["p", "c308e1f882c1f1dff2a43d4294239ddeec04e575f2d1aad1fa21ea7684e61fb5"]
  ],
  // ...其他字段
}
```

## 加入请求

用户可以发送一个 `kind 28934` 事件给中继以请求准入。它必须包含一个带有邀请码的 `claim` 标签。事件的 `created_at` 必须为当前时间，前后误差数分钟内。

```yaml
{
  "kind": 28934,
  "pubkey": "<用户公钥>",
  "tags": [
    ["-"],
    ["claim", "<邀请码>"]
  ],
  // ...其他字段
}
```

在收到申领后，中继必须使用 `OK` 消息通知客户端申领的状态。失败的申领应使用 NIP 42 指定的相同标准 `"restricted: "` 前缀。

中继应更新其 `kind 13534` 成员列表，并且可以发布一个 `kind 8000` "添加成员" 事件。

一些示例：

```
["OK", <事件-id>, false, "restricted: that invite code is expired."]
["OK", <事件-id>, false, "restricted: that is an invalid invite code."]
["OK", <事件-id>, true, "duplicate: you are already a member of this relay."]
["OK", <事件-id>, true, "info: welcome to wss://relay.bunk.skunk!"]
```

## 邀请请求

用户可以通过请求 `kind 28935` 事件来向中继请求一个申领字符串。此事件必须由中继 [NIP 11](./11.md) 文档的 `self` 字段中指定的公钥签名。

```yaml
{
  "kind": 28935,
  "pubkey": "<nip11.self>",
  "tags": [
    ["-"],
    ["claim", "<邀请码>"],
  ],
  // ...其他字段
}
```

请注意，这些事件属于 `ephemeral` 范围，这意味着中继必须明确选择加入此行为，在收到请求时即时生成申领。这允许中继通过为每个请求发放不同的申领、仅向特定用户发放申领或使申领过期来提高安全性。

## 离开请求

用户可以发送一个 `kind 28936` 事件给中继以请求撤销其访问权限。事件的 `created_at` 必须为当前时间，前后误差数分钟内。此事件必须包含一个 [NIP 70](./70.md) `-` 标签。

```yaml
{
  "kind": 28936,
  "tags": [["-"]],
  // ...其他字段
}
```

中继应更新其 `kind 13534` 成员列表，并且可以发布一个 `kind 8001` "移除成员" 事件。

## 实现

客户端应仅向在其 [NIP 11](./11.md) 中继信息文档的 `supported_nips` 部分中包含本 NIP 的中继请求 `kind 28935` 事件和发送 `kind 28934` 事件。
