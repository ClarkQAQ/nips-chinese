NIP-29
======

基于中继的群组（Relay-based Groups）
------------------

`draft` `optional` `relay`

本 NIP 定义了一种标准，用于创建仅可由封闭用户集合写入的群组。这些群组可以对外部用户公开可读，也可以不公开。

群组由一个任意长度的随机字符串标识，该字符串作为 _id_（群组ID）。

没有创建群组的方法，实际情况是中继（通常在用户要求时）会围绕某些特定 id 创建规则，使得这些 id 可以作为实际的群组，因此发送到该群组的消息将受这些规则的约束。

通常一个群组最初属于某个特定的中继，但社区可以选择将群组迁移到其他中继，甚至分叉该群组，使其在不同中继上以不同的形式存在——仍然使用相同的 _id_。

## 群组身份、迁移与分叉

一个群组由两件事维系在一起：群组 _id_ 和当前代表一个或多个群组所有者或管理员对该 _id_ 执行访问规则的中继。由于执行依赖于特定中继，群组的持久性取决于该中继愿意且能够持续托管它的意愿和能力。

### 将群组迁移到另一个中继

如果托管群组的中继无响应、永久离线或变得不可信，可以通过将现有事件复制到不同的中继来迁移群组，前提是该新中继同意接收旧群组并继续执行相同的规则以及承认相同的管理员。

### 分叉一个群组

另一种可能需要将群组迁移到不同中继的情况是原始群组成员之间出现分歧。

群组内的异议派系可以将群组历史记录带到另一个中继，该中继同意_更改_该群组的规则并将管理权限分配给不同的用户集合。这就是群组_分叉_：新中继保持相同的 _id_，但执行与原始中继不同的一套审核策略和管理员名单。

分叉不是缺陷。本 NIP 的特性在于，相同的 _id_ 可以同时标识具有不同治理方式的不同社区，由不同的中继托管，每个中继都有自己的 `kind:39000` 元数据事件、自己的 `kind:39001` 管理员列表以及自己的消息历史。

### 检测迁移与分叉

为了检查群组是否已迁移或是否存在分叉，客户端应定期——并且如果其主要中继离线或不可达，则必须——查看群组管理员和可信好友的 `kind:10009` 事件。用户所在群组的管理员的公钥应缓存在本地，以便即使在原始中继离线时也能执行此检查。

如果某个群组的 `kind:10009` 条目现在指向与客户端之前使用的不同的中继提示，客户端应通知用户该群组可能已迁移或被分叉，并提供从新中继获取该群组事件的选项，如果用户愿意，还可以将他们的参与也迁移到该中继，同时更新当前用户的 `kind:10009`。

### 复制群组以实现弹性

不希望在主中继离线时丢失历史的群组应设置所有群组事件的实时备份/副本到第二个中继。副本中继不需要执行任何自己的审核规则——它仅用于保存事件历史，以便在主中继消失时，可以选择新的权威中继并从副本复制历史事件。

## 中继生成的事件

中继应生成描述群组元数据和群组管理员的事件。这些是由中继密钥对直接签名的_可寻址_事件，以群组 _id_ 作为 `d` 标签。

## 群组引用

群组可以通过其 `kind:39000` 元数据事件的 `naddr1...` 代码来标识，公钥设置为中继 `self` 公钥，`d` 标识符设置为群组 ID，种类设置为 `39000`，并带有托管该群组的中继的中继提示。

邀请码可以附加到群组标识符后：

```
naddr1...?invite=<code>
```

客户端应在 `kind:9021` 加入请求的 `code` 标签中使用此代码。由于 bech32 字符集不包含 `?`，`?` 之前的所有内容仍然是一个有效的标识符，因此不支持后缀的客户端可以忽略它。

## `h` 标签

用户发送给群组的事件（聊天消息、文本笔记、审核事件等）必须包含一个 `h` 标签，其值设置为群组 _id_。

## 时间线引用

为了避免被断章取义地使用，发送到这些群组的事件可以在 `previous` 标签中包含对从中继先前看到的事件的引用。选择哪些先前事件由客户端决定。引用应使用用户在中继上看到的最近 50 个事件（不包括用户自身事件）中任意事件的前 8 个字符（4 字节）。可以有任意数量的引用（包括零个），但建议客户端至少包含 3 个引用，并且中继应强制执行此要求。

这是一种 hack 手段，用于防止消息被断章取义地广播到具有群组分叉的外部中继。中继应拒绝任何包含对自身数据库中不存在的事件的时间线引用的事件。客户端也应检查这些引用，以保持中继的诚实性。

### 示例

```json
  ["previous", "eb96c864", "2db75638", "b5d1065f"]
```

## 延迟发布

中继应防止延迟发布（以几天甚至几小时前的时间戳发布消息），除非它们愿意接受从中继分叉或迁移来的群组。

## 群组管理

群组可以有任意数量的具有提升权限的用户。这些用户通过角色标签来标识，角色标签由中继任意定义（另请参见 `kind:39003` 的描述）。每个角色能做什么也未在本 NIP 中定义，这是中继策略，可能有所不同。角色可以由其他用户分配（只要他们具有添加角色的能力），通过发布一个 `kind:9000` 事件，在该事件的 `p` 标签中包含该用户的公钥和之后的角色（即使用户已经是群组成员，也可以发布 `kind:9000` 事件，并且用户角色必须相应更新）。

群组支持的、被赋予某些特权的角色应在 `kind:39003` 事件中可访问，但中继也可以接受由客户端任意定义的其他角色名称，只是不对它们做任何处理。

具有任何特权角色的用户可以在广义上被视为_管理员_，并在群组的 `kind:39001` 事件中返回。

## 实时音视频空间

群组可以通过在群组公告事件中包含 `livekit` 标签来表明它们支持通过 [LiveKit](https://docs.livekit.io/transport/) 服务器的实时音视频聊天。如果群组不支持文本消息，只支持实时音视频聊天，则应使用空的 `supported_kinds` 标签。这种设置既支持 Discord 风格的独立音视频房间和文本房间，也支持 Telegram 风格的同时支持文本和音视频的单一房间群组。

当在群组中看到 `livekit` 标签时，想要参与音视频空间的客户端应使用 [NIP-98](98.md) 风格的 `Authorization` 头，调用中继路径 `/.well-known/nip29/livekit/<group-id>` 处的[令牌端点](https://docs.livekit.io/frontends/build/authentication/endpoint/)，以获取 LiveKit JWT 和 LiveKit 服务器的 URL，然后在那里继续标准的 LiveKit 流程。

`Authorization` 事件应为 kind `27235`，并包含标签 `["u", "https://relay.tld/.well-known/nip29/livekit/<group-id>"]`。

中继应根据群组设置在 LiveKit JWT 创建时强制执行访问控制。

中继必须设置所签发 JWT 的 `sub` 属性（在标准 LiveKit 库中称为 "identity"），使得前 64 个字符对应 Nostr 用户的小写十六进制公钥。同一用户可以被签发多个 JWT 并多次加入同一个 LiveKit 房间，因此中继应在这 64 个字符后附加一个随机标识符。

为了告知客户端中继对音视频的支持（以便用户在创建或编辑群组时可以显示该选项），中继应在 `https://relay.tld/.well-known/nip29/livekit` 返回状态码 `204`。

## 事件定义

以下是在 NIP-29 群组中预期能找到的事件。

### 普通用户创建的事件

群组可以接受任何事件类型，包括聊天、讨论串、长文、日历、直播、市场公告等。这些事件应按照各自 NIP 中的定义，并额外添加 `h` 标签。

### 用户相关的群组管理事件

以下是用户可发送的用于管理其在群组中状况的事件，它们也需要 `h` 标签。

- *加入请求*（`kind:9021`）

任何用户都可以向中继发送 kind `9021` 事件，以请求加入群组。如果用户尚未被添加到群组，中继必须拒绝该请求。附带的错误消息应说明拒绝是否为最终决定、请求是否正在等待审核，或者是否有其他特殊处理（例如是否需要付款）。如果用户已经是成员，则必须拒绝该事件，错误消息前缀为 `duplicate: `。

```json
{
  "kind": 9021,
  "content": "可选原因",
  "tags": [
    ["h", "<group-id>"],
    ["code", "<可选邀请码>"]
  ]
}
```

可选的 `code` 标签可由中继用于预授权接受，与 `kind:9009` `create-invite` 审核事件一起使用。

- *离开请求*（`kind:9022`）

任何用户都可以向中继发送此类事件，以自动从群组中移除。中继将自动发布一个 `kind:9001` 事件作为响应，移除该用户。

```json
{
  "kind": 9022,
  "content": "可选原因",
  "tags": [
    ["h", "<group-id>"]
  ]
}
```

### 群组状态——或审核

以下是预期由中继主密钥或群组管理员发送的事件——如果它们不是来自授权的管理员，中继应拒绝这些事件。它们也需要 `h` 标签。

- *审核事件*（`kinds:9000-9020`）（可选）

客户端可以向中继发送这些事件以执行审核操作。中继必须根据发送事件的公钥的角色以及中继的内部策略（另请参见 `kind:39003` 的描述）检查其是否能够执行给定的操作。

```yaml
{
  "kind": 90xx,
  "content": "可选原因",
  "tags": [
    ["h", "<group-id>"],
    ["previous", /*...*/]
  ]
}
```

每种审核操作使用不同的 kind 并需要不同的参数，这些参数以标签形式给出。定义如下表所示：

| kind | 名称            | 标签                                   |
| ---  | ---             | ---                                    |
| 9000 | `put-user`      | `p` 带公钥十六进制和可选角色 |
| 9001 | `remove-user`   | `p` 带公钥十六进制                    |
| 9002 | `edit-metadata` | 所有*群组元数据*的字段     |
| 9005 | `delete-event`  | `e` 带事件 ID 十六进制                  |
| 9007 | `create-group`  |                                        |
| 9008 | `delete-group`  |                                        |
| 9009 | `create-invite` | 任意 `code`                       |
| 9010 | `update-pin-list` | 零个或多个 `e` 带事件 ID 十六进制或 `a` 带事件地址 |

预计群组状态（谁是允许的成员或不是、谁是管理员以及具有哪些权限、群组名称和图片等）可以从这些事件的规范序列中完全重建。

### 群组元数据事件

这些事件在 `d` 标签（而非 `h` 标签）中包含群组 ID。它们只能由中继主密钥创建（根据 [NIP-11](11.md) 的 `"self"` 公钥），并且每个群组在任何时候每个事件最多只有一个实例（或没有）。它们仅是信息性的，但应反映最新的群组状态（随时间由审核事件更改）。

- *群组元数据*（`kind:39000`）（可选）

此事件定义群组的元数据——基本上是客户端应如何显示它。它必须由群组所在的中继生成并签名。如果由其他任何人签名，中继不应接受此事件。

如果群组被分叉并在多个中继上托管，则每个不同中继上都会存在此事件的多个版本。

```yaml
{
  "kind": 39000,
  "content": "",
  "tags": [
    ["d", "<group-id>"],
    ["name", "Pizza Lovers"],
    ["picture", "https://pizza.com/pizza.png"],
    ["banner", "https://pizza.com/banner.png"],
    ["about", "一个热爱披萨的人的群组"],
    ["private"],
    ["closed"],
    ["supported_kinds", "9", "11"]
  ]
  // 其他字段...
}
```

- `name`、`picture`、`banner` 和 `about` 是用于显示目的的群组基本元数据。
- `private` 表示只有成员可以_读取_群组消息。省略此标签表示任何人都可以读取群组消息。
- `restricted` 表示只有成员可以向群组_写入_消息。省略此标签表示任何人都可以向群组发送消息。
- `hidden` 表示中继应对非成员隐藏群组元数据。省略此标签表示任何人都可以请求群组元数据事件。
- `closed` 表示加入请求被忽略。省略此标签表示用户可以预期加入请求会被处理。
- `livekit` 表示群组支持由 LiveKit 驱动的媒体房间（实时音视频）。
- `supported_kinds` 是群组支持的字符串化 kind 编号列表。未指定时，假设支持所有 kinds。它可以不包含任何项，在这种情况下不支持任何 kinds（例如，仅音视频群组就是这种情况）。

- *群组管理员*（`kind:39001`）（可选）

每个管理员列出一个或多个角色。这些角色应与中继支持的角色对应，如 `kind:39003` 事件所宣传的。

```yaml
{
  "kind": 39001,
  "content": "披萨爱好者群组的管理员列表",
  "tags": [
    ["d", "<group-id>"],
    ["p", "<pubkey1-as-hex>", "ceo"],
    ["p", "<pubkey2-as-hex>", "secretary", "gardener"],
    // 其他公钥...
  ],
  // 其他字段...
}
```

- *群组成员*（`kind:39002`）（可选）

这是群组成员的公钥列表。中继可能选择不发布此信息、限制哪些公钥可以获取它，或只显示其中的部分成员。

客户端不应假定此事件始终存在或包含完整的成员列表。

```yaml
{
  "kind": 39002,
  "content": "披萨爱好者群组的成员列表",
  "tags": [
    ["d", "<group-id>"],
    ["p", "<admin1>"],
    ["p", "<member-pubkey1>"],
    ["p", "<member-pubkey2>"],
    // 其他公钥...
  ],
  // 其他字段...
}
```

- *群组角色*（`kind:39003`）（可选）

这是中继可能发布的事件，告知用户和客户端该中继根据其内部逻辑支持哪些角色。

例如，中继可能选择支持 `"admin"` 和 `"moderator"` 角色，其中 `"admin"` 被允许编辑群组元数据、删除消息和从群组中移除用户，而 `"moderator"` 只能删除消息（或者中继可能选择将这些角色称为 `"ceo"` 和 `"secretary"`，确切的角色名称并不重要）。

中继决定支持哪些角色以及如何基于这些角色在内部处理审核事件的过程，是每个中继特有的，不在本规范中指定。

```yaml
{
  "kind": 39003,
  "content": "此群组支持的角色列表",
  "tags": [
    ["d", "<group-id>"],
    ["role", "<role-name>", "<可选描述>"],
    ["role", "<role-name>", "<可选描述>"],
    // 其他角色...
  ],
  // 其他字段...
}
```

- *LiveKit 参与者*（`kind:39004`）（可选）

这是中继可能发布的事件，告知用户和客户端谁在音视频房间中在线。

当有用户离开或加入群组时，此事件应更新，客户端应主动订阅它。

事件必须包含零个或多个 `participant` 标签，参与者的公钥为小写十六进制。

```yaml
{
  "kind": 39004,
  "content": "",
  "tags": [
    ["d", "<group-id>"],
    ["participant", "<pubkey>"],
    // 其他参与者...
  ],
  // 其他字段...
}
```

- *群组置顶事件*（`kind:39005`）（可选）

这是群组中置顶事件的列表，按显示顺序排列。`kind:9010` 审核事件将完整列表作为 `e` 标签（普通事件）和 `a` 标签（可寻址事件）携带，因此置顶、取消置顶、重新排序和清除置顶都是通过提交新列表来完成的。每当一个被接受时，中继会重新生成此事件以镜像它。中继可能限制置顶数量。

客户端可以将这些事件显示在群组视图的顶部。

```jsonc
{
  "kind": 39005,
  "content": "",
  "tags": [
    ["d", "<group-id>"],
    ["e", "<event-id1-as-hex>"],
    ["a", "<kind>:<pubkey>:<d-identifier>"],
    ["e", "<event-id2-as-hex>"],
    // 其他事件引用...
  ],
  // 其他字段...
}
```

## 子群组

群组可以分层组织。子群组是一个普通的 NIP-29 群组（相同的 `kind:39000`、相同的审核事件、相同的 `h` 标签机制），其元数据包含一个 `parent` 标签，指向其父级的 `d` 标识符：

```jsonc
// 根群组
{ "kind": 39000, "tags": [["d", "tech"], ["name", "技术"], ["child", "nostr"]] }

// 子群组
{ "kind": 39000, "tags": [["d", "nostr"], ["name", "Nostr"], ["parent", "tech"], ["child", "nip29"]] }

// 子子群组
{ "kind": 39000, "tags": [["d", "nip29"], ["name", "NIP-29"], ["parent", "nostr"]] }
```

没有 `parent` 标签的群组是根群组。为了构建树，客户端获取 `{"kinds":[39000]}` 并在本地组装：没有 `parent` 标签的事件是根；其余事件按其 `parent` 标签引用的 `d` 进行分组。树的作用域限定于单个中继。当跨中继聚合时，客户端可能会看到同一 `d` 存在冲突的 `parent` 值；在这种情况下，当前服务于该群组事件的中继的 `kind:39000` 是权威的。

### 中继支持检测

支持子群组的中继应在其 NIP-11 中继信息文档中的 `nip29` 对象下宣传：

```json
{
  "supported_nips": [29],
  "nip29": { "subgroups": true }
}
```

### 规则

- 设置为子群组和提升为根群组由来自授权管理员的 `kind:9002`（`edit-metadata`）事件触发，该事件携带所需的 `parent` 值（或不带 `parent` 标签以分离）；然后中继相应更新群组的 `kind:39000`。中继还会更新父级的 `kind:39000`，将新添加的子群组作为 `child` 标签包含进去（反之亦然）。如果群组正在从一个父级切换到另一个父级，中继也会相应更新先前的父级。
- 群组 `h` 标签下的历史事件在这些转换中仍然有效。
- 一个 `kind:9002` 最多可以携带一个 `parent` 标签。因此，生成的 `kind:39000` 也最多携带一个 `parent` 标签。
- 缺少 `parent` 标签使群组成为根群组。
- 中继必须拒绝 `parent` 值会造成循环（包括自引用）的 `kind:9002`。
- 如果群组声明的父级在中继上不存在，则必须拒绝该 `kind:9002`。
- 当父级被删除（`kind:9008`）时，其剩余的子群组自动成为根群组。
- 中继必须拒绝任何携带 `["parent", "X"]` 的 `kind:9002`，除非事件作者同时也是 `X` 的管理员（根据 `X` 的 `kind:39001`）。
- 父群组的 `kind:39000` 必须包含 `["child", "<id>"]` 标签，作为其子群组的有序列表；每个标签在数组中的位置即为顺序。默认情况下，中继可以在子群组创建时将其追加到列表中，但父级管理员可以通过携带所需顺序的 `["child", ...]` 标签的 `kind:9002` 重新排列列表，每次编辑替换之前的列表。不将所有子群组作为 `child` 标签包含在内的 `kind:9002` 元数据编辑必须被拒绝。

### 生命周期示例

```jsonc
// "social" 收养 "nostr"（原属于 "tech"）
{ "kind": 9002, "tags": [["h", "nostr"], ["name", "Nostr"], ["parent", "social"]] }
// 中继发出：
{ "kind": 39000, "tags": [["d", "nostr"], ["name", "Nostr"], ["parent", "social"]] }
{ "kind": 39000, "tags": [["d", "social"], ["child", "nostr"]] }

// 管理员将 "nostr" 提升为根群组：edit-metadata 不带 parent 标签
{ "kind": 9002, "tags": [["h", "nostr"], ["name", "Nostr"]] }
// 中继发出：
{ "kind": 39000, "tags": [["d", "nostr"], ["name", "Nostr"]] }
{ "kind": 39000, "tags": [["d", "social"]] }
```

### 成员资格

每个子群组都有自己独立的成员资格：用户通过 `kind:9021` → `kind:9000` 加入，就像任何其他群组一样。父子链接之间不继承任何内容。成员资格不会级联：是父级的成员并不授予任何子群组的成员资格，每个群组的成员列表仅由其自身的 `kind:9000`/`kind:9001` 事件定义。管理角色同样不会继承；每个群组的 `kind:39001` 在其自身范围内是权威的，是父级的管理员不授予对任何子群组的权限，除非子群组自身的 `kind:39001` 列出了相同的公钥。

本规范未定义父子之间成员资格的自动传播。希望有这种行为的中继可以自行发出相应的 `kind:9000`/`kind:9001` 审核事件，但客户端不得假定这一点：子群组中缺少 `kind:9000` 意味着该用户不是该子群组的成员，无论父群组成员资格如何。

## 实现细节

### 检查自己在群组中的成员资格

群组中存在的最新 `kind:9000` 或 `kind:9001` 事件应告知用户他们当前是否为群组成员，或者是否已被移除。如果这些事件都不存在，则假定用户不是该群组的成员。

### 将自己加入群组

任何人都可以向群组发送 `kind:9021` 加入请求。中继可以立即生成一个 `kind:9000` 事件，或者将该决定推迟给管理员。如果群组是 `closed`（关闭的），除非加入请求包含邀请码，否则不会被处理。

### 存储群组列表

[NIP-51](51.md) 中包含了 `kind:10009` 的定义，允许客户端存储用户希望记住的群组列表。
