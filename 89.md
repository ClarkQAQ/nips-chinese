NIP-89
======

推荐应用处理器
--------------------------------

`草案` `可选`

本 NIP 描述了 `kind:31989` 和 `kind:31990`：一种发现能够处理未知事件类型的应用的方式。

## 动机

Nostr 的可发现性和透明事件交互是其最有趣/新颖的机制之一。
本 NIP 提供了一种简单的方式，让客户端能够发现处理特定类型事件的应用，以确保跨客户端和跨类型的顺畅交互。

### 参与方

该工作流程涉及三个角色：

* 处理特定事件类型的应用（注意，应用不一定是独立的实体，它也可以与用户 A 使用同一个公钥）
    * 发布 `kind:31990`，详细说明应用应如何重定向到它
* 用户 A，推荐一个处理特定事件类型的应用
    * 发布 `kind:31989`
* 用户 B，寻找处理特定事件类型的应用的推荐
    * 查询 `kind:31989`，并根据结果查询 `kind:31990`

## 事件

### 推荐事件
```yaml
{
  "kind": 31989,
  "pubkey": <recommender-user-pubkey>,
  "tags": [
    ["d", <supported-event-kind>],
    ["a", "31990:app1-pubkey:<d-identifier>", "wss://relay1", "ios"],
    ["a", "31990:app2-pubkey:<d-identifier>", "wss://relay2", "web"]
  ],
  // 其他字段...
}
```

`kind:31989` 中的 `d` 标签是该事件所推荐的支持的事件类型。

同一个 `kind:31989` 上可以出现多个 `a` 标签。

标签的第二个值应为一个中继提示。
标签的第三个值应为该推荐适用的平台。

### 处理器信息
```yaml
{
  "kind": 31990,
  "pubkey": "<application-pubkey>",
  "content": "<optional-kind:0-style-metadata>",
  "tags": [
    ["d", <random-id>],
    ["k", <supported-event-kind>],
    ["latest", "35128:<application-pubkey>:<site-d-identifier>", "wss://relay1"],
    ["next", "35128:<application-pubkey>:<next-site-d-identifier>", "wss://relay1"],
    ["web", "https://..../a/<bech32>", "nevent"],
    ["web", "https://..../p/<bech32>", "nprofile"],
    ["web", "https://..../e/<bech32>"],
    ["ios", ".../<bech32>"]
  ],
  // 其他字段...
}
```

* `content` 是一个可选的类似 `metadata` 的 JSON 字符串化对象，如 NIP-01 中所述。当创建 `kind:31990` 的公钥不是一个应用时，此内容很有用。如果 `content` 为空，则应使用该公钥的 `kind:0` 来显示应用信息（例如名称、图片、网站、LUD16 等）。
* `k` 标签的值是该 `kind:31990` 支持的事件类型。
使用 `k` 标签（而不是使用 `d` 标签的类型）有以下好处：
    * 如果应用支持多种事件类型且其处理器 URL 相同，则同一个事件中可以包含多个 `k` 标签。
    * 同一个公钥可以有多个事件，使用不同的应用处理相同的事件类型。
* 应用描述符事件应在适用时标记或以其他方式引用相关的网站清单事件，以便客户端可以直接从上游应用描述符发现应用的已部署网站清单。
* 对于 Web 应用，`kind:31990` 事件应在适用时包含 `latest` 和 `next` 标签，引用相关的 nsite 清单。这些标签使用与 `a` 标签相同的格式：第二个元素是 `<kind>:<pubkey>:<d-tag>`，第三个元素是可选中继提示。`latest` 应引用应用的当前网站清单，而 `next` 应引用计划用于发布或升级的下一个网站清单。
* URL 中的 `bech32` 必须被客户端替换为应用应加载的 NIP-19 编码实体。

应用可能会注册多个标签，遵循 NIP-19 命名规范，将命名作为数组的第二个值。

数组中第二个值为空的标签应被视为通用处理器，用于处理其他标签未处理的任何 NIP-19 实体。

## 客户端标签
在发布事件时，客户端可以包含一个 `client` 标签，用于标识发布笔记的客户端。此标签是一个由 `name`、`address`（标识处理器事件）和中继 `hint`（用于查找处理器事件）组成的元组。这对用户有隐私影响，因此客户端应允许用户选择不使用此标签。

```yaml
{
  "kind": 1,
  "tags": [
    ["client", "My Client", "31990:app1-pubkey:<d-identifier>", "wss://relay1"]
  ]
  // 其他字段...
}
```

## 用户流程
使用非 `kind:1` 为中心的 nostr 应用的用户 A 可以选择宣布/推荐某个特定类型处理器应用。

当用户 B 看到未知的事件类型时，例如在以社交媒体为中心的 nostr 客户端中，该客户端将允许用户 B 与该未知类型的事件进行交互（例如点击它）。

客户端可以查询用户及其关注者的处理器。

## 示例

### 用户 A 推荐一个 `kind:31337` 处理器
用户 A 可能是 Zapstr（一个以 `kind:31337` 为中心的客户端，用于曲目）的用户。用户 A 使用 Zapstr 发布一个事件，推荐 Zapstr 作为 `kind:31337` 处理器。

```yaml
{
  "kind": 31989,
  "tags": [
    ["d", "31337"],
    ["a", "31990:1743058db7078661b94aaf4286429d97ee5257d14a86d6bfa54cb0482b876fb0:abcd", <relay-url>, "web"]
  ],
  // 其他字段...
}
```

### 用户 B 与 `kind:31337` 处理器交互
用户 B 可能会在其时间线上看到一个引用 `kind:31337` 事件的事件（例如一个标记了 `kind:31337` 的 `kind:1` 事件）。

用户 B 的客户端不知道如何处理 `kind:31337`，可能会使用其 `alt` 标签（如 NIP-31 中所述）显示该事件。当用户点击该事件时，应用会查询此 `kind` 的处理器：

```
["REQ", <id>, { "kinds": [31989], "#d": ["31337"], "authors": [<user>, <users-contact-list>] }]
```

关注用户 A 的用户 B 看到了 `kind:31989` 事件，并获取了应用的 `a` 标签事件和处理器信息。

用户 B 的客户端看到应用的 `kind:31990`，其中包含将用户重定向到相关 URL 并将所需实体替换到 URL 中的信息。

### 绕过 `kind:31989` 的替代查询
或者，用户可以选择直接查询某个事件类型的 `kind:31990`。客户端应谨慎执行此操作，并使用垃圾信息防范机制或查询高质量的限制中继，以避免将用户引导至恶意处理器。

```
["REQ", <id>, { "kinds": [31990], "#k": [<desired-event-kind>], "authors": [...] }]
```
