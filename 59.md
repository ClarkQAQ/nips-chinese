NIP-59
======

礼物包装
---------

`可选` `中继`

本 NIP 定义了一种封装任意 nostr 事件的协议。这使得可以在很大程度上隐藏给定事件的元数据、执行协作签名等。

本 NIP *不*定义任何消息传递协议。本 NIP 的应用应单独定义。

本 NIP 依赖于 [NIP-44](./44.md) 的版本化加密算法。

## 概述

该协议使用三个主要概念来保护目标事件的传输：`谣言（未签名事件）`、`密封`和`礼物包装`。

- `谣言（未签名事件）`是一个普通的 nostr 事件，但**未签名**。这意味着如果它被泄露，无法被验证。
- `谣言（未签名事件）`被序列化为 JSON，加密后放置在`密封`的`content`字段中。然后`密封`由笔记的作者签名。`密封`上唯一公开的信息是谁签名了它，但看不到说了什么。
- `密封`被序列化为 JSON，加密后放置在`礼物包装`的`content`字段中。

这实现了跨各层的关注点隔离：

- 谣言携带内容但未签名，这意味着如果泄露，它将被中继和客户端拒绝，并且无法被认证。这提供了一定程度的可否认性。
- 密封标识了作者，但不透露内容或接收者。
- 礼物包装可以添加元数据（接收者、标签、不同的作者），而不透露真实的作者。

## 协议描述

### 1. 谣言事件种类

`谣言`与未签名事件相同。任何事件种类都可以通过移除签名来成为`谣言`。

### 2. 密封事件种类

`密封`是一个`kind:13`事件，使用发送者的常规密钥包装`谣言`。`密封`**始终**使用接收者的公钥加密，但没有指向接收者的`p`标签。没有接收者或发送者的私钥就无法知道该谣言是针对谁的。此事件中唯一的公开信息是谁在签名它。

```json
{
  "id": "<id>",
  "pubkey": "<真实作者的公钥>",
  "content": "<加密后的谣言>",
  "kind": 13,
  "created_at": 1686840217,
  "tags": [],
  "sig": "<真实作者公钥的签名>"
}
```

在`kind:13`中，标签必须始终为空。内部事件必须始终未签名。

### 3. 礼物包装事件种类

`礼物包装`事件是一个`kind:1059`事件，用于包装任何其他事件。`tags`应包含将事件路由到其预期接收者所需的任何信息，包括接收者的`p`标签或 [NIP-13](13.md) 工作量证明。

```json
{
  "id": "<id>",
  "pubkey": "<随机的、一次性使用的公钥>",
  "content": "<加密后的 kind 13 事件>",
  "kind": 1059,
  "created_at": 1686840217,
  "tags": [["p", "<接收者公钥>"]],
  "sig": "<随机的、一次性使用公钥的签名>"
}
```

### 4. 临时礼物包装事件种类

临时礼物包装是一个`kind:21059`事件。它与`kind:1059`具有相同的结构，但遵循临时事件语义（中继不得存储它）。

```json
{
  "id": "<id>",
  "pubkey": "<随机的、一次性使用的公钥>",
  "content": "<加密后的 kind 13 事件>",
  "kind": 21059,
  "created_at": 1686840217,
  "tags": [["p", "<接收者公钥>"]],
  "sig": "<随机的、一次性使用公钥的签名>"
}
```

`kind:1059`用于常规的直接消息和需要消息持久化及离线投递的异步通信，例如 NIP-17。`kind:21059`用于不需要异步操作且消息仅对当前连接的接收者有意义的实时应用，例如实时聊天或实时游戏。

## 加密载荷

加密遵循 [NIP-44](44.md) 对 JSON 编码的事件进行操作。将加密载荷放置在包装事件（`密封`或`礼物包装`）的 `.content` 中。

## 垃圾信息防护

礼物包装事件由随机的一次性密钥签名，因此中继不能依赖基于公钥的反垃圾信息或反女巫攻击措施（例如声誉、信任评分、按密钥限速）。为缓解此问题，中继可以强制实施 [NIP-42](42.md) 的 `auth-required` 策略，在接受礼物包装前对客户端进行身份认证。因此客户端应支持使用用户的真实身份向此类中继进行身份认证。由于 NIP-42 身份认证是临时的（不记录在事件签名中），它不会完全破坏礼物包装的隐私性或可否认性。

## 其他考虑

如果`谣言`针对多方，或者作者希望保留一个加密副本，可以针对每个接收者单独包装和寻址同一个`谣言`。

规范的`created_at`时间属于`谣言`。所有其他时间戳应进行调整以防止时间分析攻击。请注意，某些中继不提供日期在未来的事件，因此所有时间戳应设定在过去。

中继可能选择不存储礼物包装事件，因为它们在公开意义上没有用处。客户端可以选择按照 [NIP-13](13.md) 在包装事件上附加一定量的工作量证明，以证明该事件不是垃圾信息或拒绝服务攻击。

为保护接收者元数据，中继应基于用户 AUTH 来保护对 `kind 1059` 事件的访问。在可能的情况下，客户端应仅向提供此保护的中继发送包装事件。

为保护接收者元数据，中继应仅向标记的接收者提供 `kind 1059` 事件。在可能的情况下，客户端应仅将包装事件发送给接收者那些实现了 AUTH 且拒绝向非接收者提供包装事件的 `read` 中继。

在同时向`密封`和`礼物包装`层添加过期标签时，实现应为每一层使用独立的随机时间戳。使用不同的`created_at`值增加了时间差异，有助于防止元数据关联攻击。

由于签名密钥是随机的，中继应删除那些 p 标签与 [NIP-09](09.md) 删除事件或 [NIP-62](62.md) 消失请求的签名者匹配的 `kind:1059` 事件。

## 示例

让我们发送一个包装后的 `kind 1` 消息，在双方之间询问"今晚你去参加派对吗？"

- 作者私钥：`0beebd062ec8735f4243466049d7747ef5d6594ee838de147f8aab842b15e273`
- 接收者私钥：`e108399bd8424357a710b606ae0c13166d853d327e47a6e5e038197346bdbf45`
- 临时包装密钥：`4f02eac59266002db5801adc5270700ca69d5b8f761d8732fab2fbf233c90cbd`

请注意，此消息协议不应在实际中使用，这只是一个示例。请参阅其他 NIP 中以获取依赖礼物包装的具体消息协议。

### 1. 创建事件

创建一个 `kind 1` 事件，包含消息、接收者以及任何其他你想要的标签，由作者签名。不要对事件签名。

```json
{
  "created_at": 1691518405,
  "content": "今晚你去参加派对吗？",
  "tags": [],
  "kind": 1,
  "pubkey": "611df01bfcf85c26ae65453b772d8f1dfd25c264621c0277e1fc1518686faef9",
  "id": "9dd003c6d3b73b74a85a9ab099469ce251653a7af76f523671ab828acd2a0ef9"
}
```

### 2. 密封谣言

使用作者私钥和接收者公钥派生的会话密钥加密 JSON 编码的`谣言`。将结果放置在`kind 13` `密封`事件的`content`字段中。使用作者的密钥签名。

```json
{
  "content": "",
  "kind": 13,
  "created_at": 1703015180,
  "pubkey": "611df01bfcf85c26ae65453b772d8f1dfd25c264621c0277e1fc1518686faef9",
  "tags": [],
  "id": "28a87d7c074d94a58e9e89bb3e9e4e813e2189f285d797b1c56069d36f59eaa7",
  "sig": "02fc3facf6621196c32912b1ef53bac8f8bfe9db51c0e7102c073103586b0d29c3f39bdaa1e62856c20e90b6c7cc5dc34ca8bb6a528872cf6e65e6284519ad73"
}
```

### 3. 包装密封

使用你的临时、一次性随机密钥加密 JSON 编码的 `kind 13` 事件。将结果放置在`kind 1059`的`content`字段中。添加一个包含接收者公钥的`p`标签。使用上一步中生成的随机密钥对`礼物包装`进行签名。

```json
{
  "content": "",
  "kind": 1059,
  "created_at": 1703021488,
  "pubkey": "18b1a75918f1f2c90c23da616bce317d36e348bcf5f7ba55e75949319210c87c",
  "id": "5c005f3ccf01950aa8d131203248544fb1e41a0d698e846bd419cec3890903ac",
  "sig": "35fabdae4634eb630880a1896a886e40fd6ea8a60958e30b89b33a93e6235df750097b04f9e13053764251b8bc5dd7e8e0794a3426a90b6bcc7e5ff660f54259",
  "tags": [["p", "166bf3765ebd1fc55decfe395beff2ea3b2a4e0a8946e7eb578512b555737c99"]]
}
```

### 4. 选择性广播

仅将 `kind:1059` 或 `kind:21059` 事件广播到接收者的中继。删除所有其他事件。

## 代码示例

### JavaScript

```javascript
import {bytesToHex} from "@noble/hashes/utils"
import type {EventTemplate, UnsignedEvent, Event} from "nostr-tools"
import {getPublicKey, getEventHash, nip19, nip44, finalizeEvent, generateSecretKey} from "nostr-tools"

type Rumor = UnsignedEvent & {id: string}

const TWO_DAYS = 2 * 24 * 60 * 60

const now = () => Math.round(Date.now() / 1000)
const randomNow = () => Math.round(now() - (Math.random() * TWO_DAYS))

const nip44ConversationKey = (privateKey: Uint8Array, publicKey: string) =>
  nip44.v2.utils.getConversationKey(bytesToHex(privateKey), publicKey)

const nip44Encrypt = (data: EventTemplate, privateKey: Uint8Array, publicKey: string) =>
  nip44.v2.encrypt(JSON.stringify(data), nip44ConversationKey(privateKey, publicKey))

const nip44Decrypt = (data: Event, privateKey: Uint8Array) =>
  JSON.parse(nip44.v2.decrypt(data.content, nip44ConversationKey(privateKey, data.pubkey)))

const createRumor = (event: Partial<UnsignedEvent>, privateKey: Uint8Array) => {
  const rumor = {
    created_at: now(),
    content: "",
    tags: [],
    ...event,
    pubkey: getPublicKey(privateKey),
  } as any

  rumor.id = getEventHash(rumor)

  return rumor as Rumor
}

const createSeal = (rumor: Rumor, privateKey: Uint8Array, recipientPublicKey: string) => {
  return finalizeEvent(
    {
      kind: 13,
      content: nip44Encrypt(rumor, privateKey, recipientPublicKey),
      created_at: randomNow(),
      tags: [],
    },
    privateKey
  ) as Event
}

const createWrap = (event: Event, recipientPublicKey: string) => {
  const randomKey = generateSecretKey()

  return finalizeEvent(
    {
      kind: 1059,
      content: nip44Encrypt(event, randomKey, recipientPublicKey),
      created_at: randomNow(),
      tags: [["p", recipientPublicKey]],
    },
    randomKey
  ) as Event
}

const createEphemeralWrap = (event: Event, recipientPublicKey: string) => {
  const randomKey = generateSecretKey()

  return finalizeEvent(
    {
      kind: 21059,
      content: nip44Encrypt(event, randomKey, recipientPublicKey),
      created_at: randomNow(),
      tags: [["p", recipientPublicKey]],
    },
    randomKey
  ) as Event
}

// 使用上述示例的测试用例
const senderPrivateKey = nip19.decode(`nsec1p0ht6p3wepe47sjrgesyn4m50m6avk2waqudu9rl324cg2c4ufesyp6rdg`).data
const recipientPrivateKey = nip19.decode(`nsec1uyyrnx7cgfp40fcskcr2urqnzekc20fj0er6de0q8qvhx34ahazsvs9p36`).data
const recipientPublicKey = getPublicKey(recipientPrivateKey)

const rumor = createRumor(
  {
    kind: 1,
    content: "今晚你去参加派对吗？",
  },
  senderPrivateKey
)

const seal = createSeal(rumor, senderPrivateKey, recipientPublicKey)
const wrap = createWrap(seal, recipientPublicKey)

// 接收者用自己的私钥解开包装。

const unwrappedSeal = nip44Decrypt(wrap, recipientPrivateKey)
const unsealedRumor = nip44Decrypt(unwrappedSeal, recipientPrivateKey)
```
