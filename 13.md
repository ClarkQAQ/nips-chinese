NIP-13
======

工作量证明
-------------

`draft` `optional` `relay`

本 NIP 定义了一种为 nostr 笔记生成和解释工作量证明的方法。工作量证明（PoW）是一种为笔记添加计算工作量证明的方式。这是一种持有者证明（bearer proof），所有中继和客户端都可以通过少量代码进行通用验证。该证明可用作垃圾信息防御的一种手段。

`难度` 定义为 `NIP-01` id 中前导零位的数量。例如，id 为 `000000000e9d97a1ab09fc381030b346cdd7a142ad57e6df0b46dc9bef6c7e2d` 的难度为 `36`，即 `36` 个前导 0 位。

`002f...` 二进制表示为 `0000 0000 0010 1111...`，共有 10 个前导零。不要忘记计算十六进制数字 <= `7` 的前导零位数。

挖掘
------

要为 `NIP-01` 笔记生成工作量证明，使用 `nonce` 标签：

```json
{"content": "It's just me mining my own business", "tags": [["nonce", "1", "21"]]}
```

在挖掘时，nonce 标签的第二个条目会被更新，然后重新计算 id（参见 [NIP-01](./01.md)）。如果 id 具有所需数量的前导零位，则该笔记已被挖掘。建议在此过程中同时更新 `created_at`。

nonce 标签的第三个条目 `SHOULD` 包含目标难度。这使得客户端能够防范针对较低难度的批量垃圾发送者偶然匹配到较高难度的情况。例如，如果你要求回复你的线程需要 40 位，但看到一个提交的目标为 30，即使该笔记具有 40 位的难度，你也可以安全地拒绝它。如果没有提交的目标难度，你将无法拒绝它。提交目标难度是所有诚实的矿工都应当接受的，如果缺少难度提交声明，客户端 `MAY` 拒绝匹配目标难度的笔记。

已挖掘笔记示例
------------------

```json
{
  "id": "000006d8c378af1779d2feebc7603a125d99eca0ccf1085959b307f64e5dd358",
  "pubkey": "a48380f4cfcc1ad5378294fcac36439770f9c878dd880ffa94bb74ea54a6f243",
  "created_at": 1651794653,
  "kind": 1,
  "tags": [
    ["nonce", "776797", "20"]
  ],
  "content": "It's just me mining my own business",
  "sig": "284622fc0a3f4f1303455d5175f7ba962a3300d136085b9566801bc2e0699de0c7e31e44c81fb40ad9049173742e904713c3594a1da0fc5d2382a25c11aba977"
}
```

验证
----------

以下是用于计算 nostr 事件 id 的难度（即前导零位数）的参考 C 代码：

```c
int zero_bits(unsigned char b)
{
        int n = 0;

        if (b == 0)
                return 8;

        while (b >>= 1)
                n++;

        return 7-n;
}

/* find the number of leading zero bits in a hash */
int count_leading_zero_bits(unsigned char *hash)
{
        int bits, total, i;
        for (i = 0, total = 0; i < 32; i++) {
                bits = zero_bits(hash[i]);
                total += bits;
                if (bits != 8)
                        break;
        }
        return total;
}
```

以下是执行相同操作的 JavaScript 代码：

```javascript
// hex should be a hexadecimal string (with no 0x prefix)
function countLeadingZeroes(hex) {
  let count = 0;

  for (let i = 0; i < hex.length; i++) {
    const nibble = parseInt(hex[i], 16);
    if (nibble === 0) {
      count += 4;
    } else {
      count += Math.clz32(nibble) - 28;
      break;
    }
  }

  return count;
}
```

委托工作量证明
-----------------------

由于 `NIP-01` 笔记 id 不依赖任何签名，工作量证明可以外包给工作量证明提供方（可能需要付费）。这为客户端提供了一种方式，使其无需自行进行任何计算工作即可将消息发送到受工作量证明限制的中继，这对于手机等能源受限的设备非常有用。
