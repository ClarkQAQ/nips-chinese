NIP-39
======

将个人资料关联到其他平台
-----------------------------------

`draft` `optional`

## 摘要

用户可以通过在 kind `10011` 事件中使用 `i` 标签来声明对一个或多个在线身份（如用户名、个人资料页面、密钥对）的控制权。

```yaml
{
  "kind": 10011,
  "tags": [
    ["i", "github:semisol", "9721ce4ee4fceb91c9711ca2a6c9a5ab"],
    ["i", "twitter:semisol_public", "1619358434134196225"],
    ["i", "mastodon:bitcoinhackers.org/@semisol", "109775066355589974"]
    ["i", "telegram:1087295469", "nostrdirectory/770"]
  ],
  // 其他字段...
}
```

一个 `i` 标签必须包含两个参数，定义如下：
1. `platform:identity`：平台名称（例如 `github`）与该平台上的身份（例如 `semisol`）通过 `:` 连接在一起。
2. `proof`：指向拥有该身份证明的字符串或对象。

客户端应处理任何包含超过 2 个值的 `i` 标签，以便未来扩展。
身份提供者名称应仅包含 `a-z`、`0-9` 以及字符 `._-/`，并且不能包含 `:`。
身份名称应尽可能规范化——将大写字母替换为小写字母，如果一个实体有多个别名，应使用主要别名。

## 声明类型

### `github`

身份：GitHub 用户名。

证明：一个 GitHub Gist ID。该 Gist 应由 `<identity>` 创建，包含一个单文件，文件内容为 `Verifying that I control the following Nostr public key: <npub encoded public key>`。
可在 `https://gist.github.com/<identity>/<proof>` 进行验证。

### `twitter`

身份：Twitter 用户名。

证明：一个推文 ID。该推文应由 `<identity>` 发布，文本内容为 `Verifying my account on nostr My Public Key: "<npub encoded public key>"`。
可在 `https://twitter.com/<identity>/status/<proof>` 进行验证。

### `mastodon`

身份：Mastodon 实例和用户名，格式为 `<instance>/@<username>`。

证明：一条 Mastodon 帖子 ID。该帖子应由 `<username>@<instance>` 发布，文本内容为 `Verifying that I control the following Nostr public key: "<npub encoded public key>"`。
可在 `https://<identity>/<proof>` 进行验证。

### `telegram`

身份：一个 Telegram 用户 ID。

证明：格式为 `<ref>/<id>` 的字符串，指向发布在名为 `<ref>` 的公共频道或群组中、消息 ID 为 `<id>` 的消息。该消息应由用户 ID 为 `<identity>` 的用户发送，文本内容为 `Verifying that I control the following Nostr public key: "<npub encoded public key>"`。
可在 `https://t.me/<proof>` 进行验证。
