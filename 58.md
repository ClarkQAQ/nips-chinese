NIP-58
======

徽章
------

`draft` `optional`

使用四种特殊事件来定义、授予、展示和分类用户个人资料中的徽章：

1. "徽章定义"事件被定义为一种具有 kind `30009` 的可寻址事件，具有一个 `d` 标签，其值唯一标识该徽章（例如 `bravery`），由徽章颁发者发布。徽章定义可以更新。

2. "徽章授予"事件是一种 kind `8` 事件，具有一个引用"徽章定义"事件的 `a` 标签，以及一个或多个 `p` 标签，每个 `p` 标签对应徽章颁发者希望授予的公钥。被授予的徽章是不可变且不可转移的。

3. "个人资料徽章"事件被定义为一种具有 kind `10008` 的_可替换事件_，作为 [NIP-51](51.md) 标准列表。个人资料徽章包含一个有序的 `a` 和 `e` 标签对列表，分别引用要展示的每个徽章的"徽章定义"和"徽章授予"。它还可以包含引用"徽章集"事件的 `a` 标签。

4. "徽章集"事件被定义为一种具有 kind `30008` 的_可寻址事件_，作为 [NIP-51](51.md) 集。徽章集允许用户将已接受的徽章分类到带标签的组中。它们包含有序的 `a` 和 `e` 标签对，分别引用集合中每个徽章的"徽章定义"和"徽章授予"。

### 徽章定义事件

必须包含以下标签：

- `d` 标签，带有徽章的唯一名称。

可以包含以下标签：

- 一个 `name` 标签，带有徽章的简短名称。
- `image` 标签，其值为表示徽章的高分辨率图片的 URL。第二个值可选地指定图片的尺寸为 `宽度`x`高度`（像素）。徽章推荐尺寸为 1024x1024 像素。
- 一个 `description` 标签，其值可以包含图片的文本描述、徽章的含义或其颁发原因。
- 一个或多个 `thumb` 标签，其第一个值为指向 `image` 标签中引用的图片的缩略图版本的 URL。第二个值可选地指定缩略图的尺寸为 `宽度`x`高度`（像素）。

### 徽章授予事件

必须包含以下标签：

- 一个 `a` 标签，引用 kind `30009` 徽章定义事件。
- 一个或多个 `p` 标签，引用每个被授予的公钥。

### 个人资料徽章事件

一个公钥可以被授予的徽章数量没有上限。个人资料徽章事件允许个体用户接受或拒绝被授予的徽章，以及选择徽章在其个人资料上的显示顺序。

可以包含以下标签：

- 零个或多个有序的连续 `a` 和 `e` 标签对，分别引用 kind `30009` 徽章定义和 kind `8` 徽章授予。客户端应忽略没有对应 `e` 标签的 `a` 标签，反之亦然。`e` 标签引用的徽章授予应包含相同的 `a` 标签。

### 动机

用户可以因认可、感谢、参与或赞赏某个目标、任务或事业而被授予徽章（但不限于此）。

用户可以选择用徽章装饰其个人资料，以获取名气、知名度、认可、支持等，来自他们认为信誉良好的徽章颁发者。

### 推荐

客户端可以白名单徽章颁发者（公钥），以确保为其用户保留有价值/特殊的因素。

徽章图片推荐宽高比为 1:1，高分辨率尺寸为 1024x1024 像素。

徽章缩略图图片推荐尺寸为：512x512（xl）、256x256（l）、64x64（m）、32x32（s）和 16x16（xs）。

客户端可以选择渲染比用户在个人资料徽章事件中指定的更少的徽章，或替换徽章图片和缩略图以适应该客户端的主题。

客户端应尝试根据用户选择的徽章数量和可用空间来渲染最合适的徽章缩略图。客户端应在用户操作（点击、触摸、悬停）时尝试渲染高分辨率版本。

### 徽章定义事件示例

```yaml
{
  "pubkey": "alice",
  "kind": 30009,
  "tags": [
    ["d", "bravery"],
    ["name", "Medal of Bravery"],
    ["description", "Awarded to users demonstrating bravery"],
    ["image", "https://nostr.academy/awards/bravery.png", "1024x1024"],
    ["thumb", "https://nostr.academy/awards/bravery_256x256.png", "256x256"]
  ],
  // other fields...
}
```

### 徽章授予事件示例

```yaml
{
  "id": "<badge award event id>",
  "kind": 8,
  "pubkey": "alice",
  "tags": [
    ["a", "30009:alice:bravery"],
    ["p", "bob", "wss://relay"],
    ["p", "charlie", "wss://relay"]
  ],
  // other fields...
}
```

### 个人资料徽章事件示例

勇敢的 Honorable Bob：
```yaml
{
  "kind": 10008,
  "pubkey": "bob",
  "tags": [
    ["a", "30009:alice:bravery"],
    ["e", "<bravery badge award event id>", "wss://nostr.academy"],
    ["a", "30009:alice:honor"],
    ["e", "<honor badge award event id>", "wss://nostr.academy"]
  ],
  // other fields...
}
```

### 已弃用的个人资料徽章事件

本 NIP 的早期版本使用了一种 kind `30008` 可寻址事件，其 `d` 标签值为 `profile_badges`，用于个人资料徽章事件。客户端应将此类事件视为等同于 kind `10008`，并迁移到新格式。
