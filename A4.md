NIP-A4
======

公开消息
---------------

`draft` `optional`

本 NIP 定义了 kind `24`，作为发送给一个或多个 Nostr 用户的简单纯文本消息。

`.content` 包含消息内容。`p` 标签标识一个或多个接收者。

```yaml
{
  "pubkey": "<sender-pubkey>",
  "kind": 24,
  "tags": [
    ["p", "<receiver>", "<relay-url>"],
  ],
  "content": "<message-in-plain-text>",
}
```

消息必须发送到每个接收者的 [NIP-65](65.md) 收件箱中继以及发送者的发件箱中继。

Kind `24` 设计用于在通知屏幕上显示和回复。其目标是让客户端能够支持此功能，而无需担心聊天历史记录。不存在消息链。在此系统中，"帖子"、"帖子根"或"聊天室"的概念不存在，因为消息可以开始和继续而无需任何语法上的相互连接。不得使用 `e` 标签。

此 kind 并非设计用于展示在信息流中，但任何人都可以看到和回复可能并非发给他们的消息。

## 高级支持

推荐使用 [NIP-40](40.md) 的 `expiration` 标签。由于没有聊天室的概念，这些消息随着时间的推移可能不再有意义。

在引用 `.content` 中的事件时，可以使用 [NIP-18](18.md) 的引用转发 `q` 标签，配合 [NIP-21](21.md)。

```json
["q", "<event-id> or <event-address>", "<relay-url>", "<pubkey-if-a-regular-event>"]
```

[NIP-25](25.md) 的反应必须添加 `k` 标签为 `24`。

[NIP-57](57.md) 的 zap 必须包含 `k` 标签为 `24`。

使用 [NIP-19](19.md) 的 `nevent1` 的 [NIP-21](21.md) 链接必须包含 `24` 的 `kind`。非 `kind:24` 的链接不应期望由客户端原生渲染。

对于图片和视频链接，应添加 [NIP-92](92.md) 的 `imeta` 标签。

## 警告

此 kind 不得有任何隐私期望。它只是一个公开回复，但没有根笔记。

避免将此 kind 与 [NIP-17](17.md) 私信中的 Kind `14` 谣言混淆。此 kind 是签名的，设计用于公开消费。
