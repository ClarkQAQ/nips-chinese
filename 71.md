NIP-71
======

视频事件
--------

`draft` `optional`

本规范定义了_视频_事件，表示一个专门的外部托管内容帖子。

与附有视频的 `kind:1` 事件不同，视频事件旨在包含有关主题媒体的所有附加元数据，并在特定于视频的客户端（而非一般的微博客户端）中呈现。其理念是此类事件应被引用到类似 Netflix、YouTube 或 TikTok 的 Nostr 客户端中，其中视频本身是体验的核心。

## 视频事件

有两种类型的视频事件，由不同的 kind 表示：_普通_和_短视频_事件。这是为了允许客户端分别适配每种类型，因为较长的、大多数是横向（风景）的视频的观看体验通常不同于短格式、大多数是纵向（肖像）的视频（"故事"、"reels"、"shorts"等）。

除了礼貌和常识之外，没有任何因素阻止_短视频_变长，或_普通视频_变为纵向，这可能有或可能没有合理性，这主要是一种风格上的定性差异，而非实际原始尺寸的问题。

#### 格式

格式使用_常规事件_ kind `21` 表示_普通_视频，kind `22` 表示_短视频_。

## 可寻址视频事件

对于发布后可能需要更新的内容（例如修正元数据、描述或处理 URL 迁移），提供了可寻址版本：

- Kind `34235` 用于_可寻址普通视频_
- Kind `34236` 用于_可寻址短视频_

这些可寻址事件遵循与其常规对应事件相同的格式，但包含一个 `d` 标签作为唯一标识符，并且可以在保持相同可寻址引用的同时进行更新。这对于以下情况特别有用：

- 元数据修正（描述、标题、标签）而无需重新发布
- 保留来自旧平台的导入内容 ID
- 托管变更时的 URL 迁移
- 平台迁移跟踪

这些事件的 `.content` 是视频内容的摘要或描述。

视频信息的主要来源是 `imeta` 标签，该标签在 [NIP-92](92.md) 中定义。

每个 `imeta` 标签可用于通过 `dim` 和 `m` 属性指定视频的变体，以及多个音轨（语言）。

客户端应检查音轨是否可用，并优先使用它们而非视频内置音频，以允许在不中断音频的情况下平滑切换分辨率。`ov`（原始版本）标记原始语言音轨。

本 NIP 定义了除 [NIP-92](92.md) 和 [NIP-94](94.md) 中列出的属性之外的以下额外 `imeta` 属性：

* `duration`（推荐）视频/音频的时长，以秒为单位（浮点数）
* `bitrate`（推荐）视频/音频的平均比特率，以 bits/sec 为单位
* `waveform`（可选，仅音频）随时间变化的振幅值，以空格分隔的整数，少于 100 个值应足以呈现良好的可视化效果

示例：
```json
[
  ["imeta",
    "dim 1920x1080",
    "url https://myvideo.com/1080/12345.mp4",
    "x 3093509d1e0bc604ff60cb9286f4cd7c781553bc8991937befaacfdc28ec5cdc",
    "m video/mp4",
    "image https://myvideo.com/1080/12345.jpg",
    "image https://myotherserver.com/1080/12345.jpg",
    "fallback https://myotherserver.com/1080/12345.mp4",
    "fallback https://andanotherserver.com/1080/12345.mp4",
    "service nip96",
    "bitrate 3000000",
    "duration 29.223"
  ],
  ["imeta",
    "dim 1280x720",
    "url https://myvideo.com/720/12345.mp4",
    "x e1d4f808dae475ed32fb23ce52ef8ac82e3cc760702fca10d62d382d2da3697d",
    "m video/mp4",
    "image https://myvideo.com/720/12345.jpg",
    "image https://myotherserver.com/720/12345.jpg",
    "fallback https://myotherserver.com/720/12345.mp4",
    "fallback https://andanotherserver.com/720/12345.mp4",
    "service nip96",
    "bitrate 2000000",
    "duration 29.24"
  ],
  ["imeta",
    "dim 1280x720",
    "url https://myvideo.com/720/12345.m3u8",
    "x 704e720af2697f5d6a198ad377789d462054b6e8d790f8a3903afbc1e044014f",
    "m application/x-mpegURL",
    "image https://myvideo.com/720/12345.jpg",
    "image https://myotherserver.com/720/12345.jpg",
    "fallback https://myotherserver.com/720/12345.m3u8",
    "fallback https://andanotherserver.com/720/12345.m3u8",
    "service nip96",
    "duration 29.21"
  ],
  ["imeta",
    "url https://myaudio.com/audio/en/12345.mp3",
    "x b2e0a7a82ac9f3f3a71f1d9a78c381d5be9d1cf19dce258765c17c8a76287c93",
    "m audio/mp3",
    "waveform 0 7 35 8 100 100 49 8 4 16 8 10 7 2 20 10 100 100 100 100 100 100 15 100 100 100 25 60 5 4 3 1 0 100 100 15 100 29 88 0 33 11 39 100 100 19 4 100 42 35 5 0 1 5 0 0 11 38 100 94 17 11 44 58 5 100 100 100 55 14 72 100 100 57 6 1 14 2 16 100 100 40 16 100 100 6 32 14 13 41 36 16 14 6 3 0 1 2 1 6 0",
    "l en ISO-639-1 ov",
    "fallback https://myotherserver.com/audio/en/12345.mp3",
    "fallback https://andanotherserver.com/audio/en/12345.mp3",
    "service nip96",
    "bitrate  320000",
    "duration 29.24"
  ],
]
```

其中 `url` 是主服务器 URL，`fallback` 是托管相同文件的其他服务器，`url` 和 `fallback` 应被同等对待，建议客户端使用任何提供的视频 URL。

`image` 标签包含一个预览图片（分辨率相同）。可以使用多个 `image` 标签以指定备用副本，就像 `fallback` 用于 `url` 一样。

此外，可以包含 `service nip96` 以允许客户端搜索作者 NIP-96 服务器列表，使用哈希值查找文件。

### 可寻址事件必需的标签：
* `d` - 此视频的唯一标识符（用户选择的字符串，kinds 34235, 34236 必需）

### 其他标签：
* `title`（必需）视频标题
* `published_at`，视频首次发布时的 Unix 秒级时间戳（字符串形式）
* `text-track`（可选，可重复）指向视频 WebVTT 文件的链接，补充信息类型（字幕/字幕/章节/元数据），可选的语言代码
* `content-warning`（可选）关于 NSFW 视频内容的警告
* `alt`（可选）无障碍描述
* `segment`（可选，可重复）开始时间戳（格式 `HH:MM:SS.sss`）、结束时间戳（格式 `HH:MM:SS.sss`）、章节/分段标题、分段缩略图 URL
* `t`（可选，可重复）用于分类视频的话题标签
* `p`（可选，可重复）视频中参与者的 32 字节十六进制公钥，可选的建议 relay URL
* `r`（可选，可重复）引用/指向网页的链接

### 导入内容的可选标签：
* `origin` - 追踪原始平台和 ID：`["origin", "<platform>", "<external-id>", "<original-url>", "<optional-metadata>"]`

```yaml
{
  "id": "<32-bytes lowercase hex-encoded SHA-256 of the the serialized event data>",
  "pubkey": "<32-bytes lowercase hex-encoded public key of the event creator>",
  "created_at": <Unix timestamp in seconds>,
  "kind": 21 | 22,
  "content": "<视频摘要/描述>",
  "tags": [
    ["title", "<视频标题>"],
    ["published_at", "<unix timestamp>"],
    ["alt", "<描述>"],

    // 视频数据
    ["imeta",
      "dim 1920x1080",
      "url https://myvideo.com/1080/12345.mp4",
      "x 3093509d1e0bc604ff60cb9286f4cd7c781553bc8991937befaacfdc28ec5cdc",
      "m video/mp4",
      "image https://myvideo.com/1080/12345.jpg",
      "image https://myotherserver.com/1080/12345.jpg",
      "fallback https://myotherserver.com/1080/12345.mp4",
      "fallback https://andanotherserver.com/1080/12345.mp4",
      "service nip96",
    ],

    ["text-track", "<encoded `kind 6000` event>", "<recommended relay urls>"],
    ["content-warning", "<reason>"],
    ["segment", <start>, <end>, "<title>", "<thumbnail URL>"],

    // 参与者
    ["p", "<32-bytes hex of a pubkey>", "<optional recommended relay URL>"],
    ["p", "<32-bytes hex of a pubkey>", "<optional recommended relay URL>"],

    // 话题标签
    ["t", "<tag>"],
    ["t", "<tag>"],

    // 引用链接
    ["r", "<url>"],
    ["r", "<url>"]
  ]
}
```

## 可寻址事件示例

```yaml
{
  "id": <32-bytes lowercase hex-encoded SHA-256 of the the serialized event data>,
  "pubkey": <32-bytes lowercase hex-encoded public key of the event creator>,
  "created_at": <Unix timestamp in seconds>,
  "kind": 34235 | 34236,
  "content": "<视频摘要/描述>",
  "tags": [
    ["d", "<unique-identifier>"],
    ["title", "<视频标题>"],
    ["published_at", "<unix timestamp>"],
    ["alt", "<无障碍描述>"],

    // 视频数据
    ["imeta",
      "url https://example.com/media.mp4",
      "m video/mp4",
      "dim 480x480",
      "blurhash eVF$^OI:${M{%LRjWBoLoLaeR*",
      "image https://example.com/thumb.jpg",
      "x 3093509d1e0bc604ff60cb9286f4cd7c781553bc8991937befaacfdc28ec5cdc"
    ],
    // 音频数据
    ["imeta",
      "url https://example.com/audio.mp3",
      "x b2e0a7a82ac9f3f3a71f1d9a78c381d5be9d1cf19dce258765c17c8a76287c93",
      "m audio/mp3",
      "l en ISO-639-1 ov"
    ],

    ["duration", <duration in seconds>],
    ["content-warning", "<reason>"],

    // 导入内容的来源追踪
    ["origin", "<platform>", "<external-id>", "<original-url>", "<optional-metadata>"],

    // 参与者
    ["p", "<32-bytes hex of a pubkey>", "<optional recommended relay URL>"],

    // 话题标签
    ["t", "<tag>"],
    ["t", "<tag>"],

    // 引用链接
    ["r", "<url>"]
  ]
}
```

## 引用可寻址事件

要引用可寻址视频：

```
["a", "34235:<pubkey>:<d-tag-value>", "<relay-url>"]  // 用于普通视频
["a", "34236:<pubkey>:<d-tag-value>", "<relay-url>"]  // 用于短视频
```
