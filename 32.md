NIP-32
======

标记
----

`draft` `optional`

本 NIP 定义了两个新的可索引标签用于标记事件，以及一个新的事件类型（`kind:1985`）用于将标签附加到现有事件。这支持多种用例，包括分布式审核、收藏管理、许可证分配和内容分类。

新标签：

- `L` 表示标签命名空间
- `l` 表示标签

标签命名空间标签
----

`L` 标签可以是任何字符串，但发布者应通过使用明确定义的命名空间（如 ISO 标准）或反向域名表示法来确保其无歧义。

建议使用 `L` 标签以便按命名空间而非特定标签进行搜索。当标签内容由最终用户提供时，可以使用特殊的 `ugc`（"用户生成内容"）命名空间。

以 `#` 开头的 `L` 标签表示标签目标应与标签的值关联。这是一种将标准 nostr 标签附加到事件、公钥、relay、URL 等对象的方式。

标签标签
----

`l` 标签的值可以是任何字符串。如果使用了 `L` 标签，则 `l` 标签必须包含与同一事件中某个 `L` 标签值匹配的标记（mark）。如果没有包含 `L` 标签，仍应包含一个标记。如果未包含任何标记，则默认为 `ugc`。

标签目标
----

标签事件必须包含一个或多个表示被标记对象的标签：`e`、`p`、`a`、`r` 或 `t` 标签。这允许分别标记事件、人物、relay 或主题。与 NIP-01 一样，使用 `e` 和 `p` 标签时应包含 relay 提示。

内容
-------

标签应为简短、有意义的字符串。较长的讨论（例如解释某物为何被如此标记）应放在事件的 `content` 字段中。

自标记
-------

`l` 和 `L` 标签可以添加到其他事件类型中，以支持自标记。对于类型不是 1985 的事件，标签指的是事件本身。

示例事件
--------------

建议将多个公钥关联到 `permies` 主题。

```yaml
{
  "kind": 1985,
  "tags": [
    ["L", "#t"],
    ["l", "permies", "#t"],
    ["p", <pubkey1>, <relay_url>],
    ["p", <pubkey2>, <relay_url>]
  ],
  // other fields...
}
```

根据 ontology.example.com 的定义标记针对人类的暴力行为。

```yaml
{
  "kind": 1985,
  "tags": [
    ["L", "com.example.ontology"],
    ["l", "VI-hum", "com.example.ontology"],
    ["p", <pubkey1>, <relay_url>],
    ["p", <pubkey2>, <relay_url>]
  ],
  // other fields...
}
```

对聊天事件的审核建议。

```yaml
{
  "kind": 1985,
  "tags": [
    ["L", "nip28.moderation"],
    ["l", "approve", "nip28.moderation"],
    ["e", <kind40_event_id>, <relay_url>]
  ],
  // other fields...
}
```

为事件分配许可证。

```yaml
{
  "kind": 1985,
  "tags": [
    ["L", "license"],
    ["l", "MIT", "license"],
    ["e", <event_id>, <relay_url>]
  ],
  // other fields...
}
```

发布者可以通过将 `l` 标签添加到自己的非 1985 事件来进行自标记。在此例中，kind 1 事件的作者使用 ISO 3166-2 将其笔记标记为与意大利米兰相关。

```yaml
{
  "kind": 1,
  "tags": [
    ["L", "ISO-3166-2"],
    ["l", "IT-MI", "ISO-3166-2"]
  ],
  "content": "It's beautiful here in Milan!",
  // other fields...
}
```

作者使用 ISO-639-1 将其笔记语言标记为英语。

```yaml
{
  "kind": 1,
  "tags": [
    ["L", "ISO-639-1"],
    ["l", "en", "ISO-639-1"]
  ],
  "content": "English text",
  // other fields...
}
```

其他说明
-----------

当使用本 NIP 一次批量标记多个目标时，可以使用 [NIP-09](09.md) 请求删除事件，并发布替代事件。由于在制定标准的 `d` 标签方面存在复杂性，我们选择不对本功能使用可寻址/可替换事件。为避免查询时的歧义，发布者应将标记事件限制在单个命名空间内。

在创建词汇表之前，请先探索您的用例是否已有现成的设计方案，并尽可能模仿该设计。建议使用反向域名表示法以避免命名空间冲突，但为了互操作性，所有命名空间应被视为对公众开放使用而非专有。换句话说，如果有适合您用例的命名空间，即使它指向他人的域名，也可以使用。

词汇表可以选择完全限定命名空间内的所有标签（例如 `["l", "com.example.vocabulary:my-label"]`）。这在定义更正式的词汇表时可能是首选，这样在查询时不会与没有 `L` 标签的另一个命名空间混淆。对于这些词汇表，所有标签都应包含命名空间（而不是混合使用限定和非限定标签）。

判断某个用例是否适合本 NIP 的一个好方法是：标签是否曾经是唯一的。例如，许多事件可能被标记为特定的地点、主题或公钥，但像"John Doe"或"3.18743"这样的特定值不是标签，而是数值，应以其他方式处理。


附录：已知本体
--------------------------

以下是当前广泛使用的本体非详尽列表。

- [社交本体分类](https://github.com/CLARIAH/awesome-humanities-ontologies)
