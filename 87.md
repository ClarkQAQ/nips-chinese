NIP-87
======

Cashu 和 Fedimint 可发现性
----------------------------------

`draft` `optional`

本 NIP 描述了 `kind:38173`、`kind:38172` 和 `kind:38000`：一种发现 ecash 铸币厂、其能力以及推荐它们的人的方式。

## 原理

Nostr 的可发现性和透明的事件交互是其最有趣/新颖的机制之一。
本 NIP 提供了一种简单的方式，让用户可以发现其他用户推荐的 ecash 铸币厂并与之交互。

### 涉及方

此工作流程涉及三个角色：

* ecash 铸币厂运营者，宣布其铸币厂及其能力。
    * 发布 `kind:38173` 或 `kind:38172`，详细说明如何连接及其能力。
* 用户 A，推荐某个 ecash 铸币厂
    * 发布 `kind:38000`
* 用户 B，寻找 ecash 铸币厂的推荐
    * 查询 `kind:38000`，并根据结果查询 `kind:38173`/`kind:38172`

## 事件

### 推荐事件
```json
{
  "kind": 38000,
  "pubkey": <推荐者-用户-公钥>,
  "tags": [
    ["k", "38173"],
    ["d", "<d-标识符>"],
    ["u", <推荐的 fedimint 邀请码>],
    ["a", "38173:fedimint-公钥:<d-标识符>", "wss://relay1"]
  ],
  "content": "我绝对信任这个铸币厂"
}
```

推荐事件是一个参数化可替换事件，这样用户可以修改其推荐而无需创建新事件。

`kind:38000` 中的 `d` 标签是被推荐的 `kind:38173`/`kind:38172` 事件标识符，如果不存在事件，`d` 标签仍可以从铸币厂的公钥/ID 计算得出。
`k` 标签是用户推荐的事件 kind 对应的 kind 编号，在本例中，`kind:38173` 用于 fedimints，`kind:38172` 用于 cashu 铸币厂。

可选的 `u` 标签可以添加以提供推荐的连接方式。
标签的值是 ecash 铸币厂的 URL 或邀请码。
同一个 `kind:38000` 上可以出现多个 `u` 标签。

`a` 标签用于指向 ecash 铸币厂的 `kind:38173`/`kind:38172` 事件。
标签的第一个值是 `kind:38173`/`kind:38172` 事件标识符，标签的第二个值是中继提示。
这用于在存在声称是同一铸币厂的重复事件时，正确指向铸币厂的 `kind:38173`/`kind:38172` 事件。

内容可用于给出评价。

## 铸币厂信息

Cashu 铸币厂应发布 `kind:38172` 事件以宣布其能力和连接方式。

对于 cashu 铸币厂，`u` 标签应为 cashu 铸币厂的 URL，并应列出该 cashu 铸币厂支持的 NUTs。

`d` 标签应为铸币厂的公钥（在查询 `/v1/info` 时找到），这样用户可以通过公钥查询并找到铸币厂公告。

应添加 `n` 标签以表明 cashu 铸币厂所在的网络（`mainnet`、`testnet`、`signet` 或 `regtest`）

```json
{
  "kind": 38172,
  "pubkey": "<应用-公钥>",
  "content": "<可选的 kind:0 样式元数据>",
  "tags": [
    ["d", <cashu 铸币厂公钥>],
    ["u", "https://cashu.example.com"],
    ["nuts", "1,2,3,4,5,6,7"],
    ["n", "mainnet"]
  ]
}
```

Fedimints 应发布 `kind:38173` 事件以宣布其能力和连接方式。

对于 fedimints，它应在 `u` 标签中列出所有已知的 fedimint 邀请码，并列出其支持的模块。

`d` 标签应为联邦 ID，这样用户可以通过联邦 ID 查询并找到 fedimint 公告。

应添加 `n` 标签以表明 fedimint 所在的网络（`mainnet`、`testnet`、`signet` 或 `regtest`）

```json
{
  "kind": 38173,
  "pubkey": "<应用-公钥>",
  "content": "<可选的 kind:0 样式元数据>",
  "tags": [
    ["d", <联邦-id>],
    ["u", "fed11abc.."],
    ["u", "fed11xyz.."],
    ["modules", "lightning,wallet,mint"],
    ["n", "signet"]
  ]
}
```

* `content` 是可选的类似 `metadata` 的字符串化 JSON 对象，如 NIP-01 所述。当创建 `kind:38173` 事件的公钥不是普通用户时，此内容很有用。如果 `content` 为空，则应使用该公钥的 `kind:0` 来显示铸币厂信息（例如名称、图片、网站、LUD16 等）。

## 示例

### 用户 A 推荐一些铸币厂
用户 A 可能是某个 cashu 铸币厂的用户。使用客户端，用户 A 发布一个事件推荐他们使用的 cashu 铸币厂。

```json
{
  "kind": 38000,
  "tags": [
    ["u", "fed11abc..", "fedimint"],
    ["u", "https://cashu.example.com", "cashu"],
    ["a", "38173:fedimint-公钥:<d-标识符>", "wss://relay1", "fedimint"],
    ["a", "38172:cashu-铸币厂-公钥:<d-标识符>", "wss://relay2", "cashu"]
  ],
  ...
}
```

### 用户 B 寻找铸币厂
用户 B 想使用 ecash 钱包，他们需要找到一个铸币厂。

用户 B 的钱包客户端查询 `kind:38000` 事件，寻找 ecash 铸币厂的推荐。

```json
["REQ", <id>, [{ "kinds": [38000], "authors": [<用户>, <用户的联系人列表>], "#k": ["38173"] }]]
```

关注用户 A 的用户 B，看到了 `kind:38000` 事件并尝试连接到相应的铸币厂。

### 绕过 `kind:38000` 的替代查询
或者，用户可以选择直接查询特定事件 kind 的 `kind:38173`。客户端应谨慎执行此操作，并使用垃圾邮件防护机制或查询高质量的限制中继，以避免将用户引导至恶意处理者。

```json
["REQ", <id>, [{ "kinds": [38173], "authors": [...] }]]
```
