NIP-B7
======

Blossom 媒体
-------------

`草案` `可选`

本 NIP 规定了 Nostr 客户端如何使用 [Blossom][] 处理媒体。

Blossom 是一套用于处理存储文件的服务器（其文件可通过 SHA-256 校验和寻址）的标准（称为 BUD）。Nostr 客户端可以使用所有 BUD 来允许用户上传文件、管理自己的文件等，但最重要的是，Nostr 客户端应使用 [BUD-03][] 获取每个用户的 `kind:10063` 服务器列表：

```json
{
  "id": "e4bee088334cb5d38cff1616e964369c37b6081be997962ab289d6c671975d71",
  "pubkey": "781208004e09102d7da3b7345e64fd193cd1bc3fce8fdae6008d77f9cabcd036",
  "content": "",
  "kind": 10063,
  "created_at": 1708774162,
  "tags": [
    ["server", "https://blossom.self.hosted"],
    ["server", "https://cdn.blossom.cloud"]
  ],
  "sig": "cc5efa74f59e80622c77cacf4dd62076bcb7581b45e9acff471e7963a1f4d8b3406adab5ee1ac9673487480e57d20e523428e60ffcc7e7a904ac882cfccfc653"
}
```

每当 Nostr 客户端在某个用户发布的事件中发现一个 URL，该 URL 以 64 字符的十六进制字符串结尾（可能带有文件扩展名），且该 URL 已不可用时，这意味着该字符串很可能代表一个 SHA-256 值，并且该用户可能已经发布了一个包含 Blossom 服务器的 `kind:10063` 列表。

鉴于此，客户端应查找 `kind:10063` 列表中的其他 Blossom 服务器，并在这些服务器中查找相同的 64 字符十六进制字符串，只需将十六进制字符串作为路径即可（末尾可选地加上文件扩展名），生成类似 `https://blossom.self.hosted/<hex-string>.png` 的 URL。

下载此类文件时，Nostr 客户端应验证文件内容的 SHA-256 哈希是否与 64 字符的十六进制字符串匹配。

更多信息请参见 [BUD-03][]。

### 更复杂的交互

客户端可以使用 Blossom 服务器提供的其他功能（例如，检查文件是否存在于 Blossom 服务器中，而无需实际下载），这些功能在 [BUD][Blossom] 文档中有更好的说明。

[Blossom]: https://github.com/hzrd149/blossom
[BUD-03]: https://github.com/hzrd149/blossom/blob/master/buds/03.md
