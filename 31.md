> __警告__  `不推荐`: 不必要地臃肿

NIP-31
======

处理未知事件 kind
--------------------------------

`draft` `unrecommended` `optional`

当创建属于自定义协议的一部分、且不打算作为文本阅读（如 `kind:1`）的新自定义事件 kind 时，客户端应使用 `alt` 标签编写一段简短的人类可读纯文本摘要，说明该事件的内容。

其目的是，那些只用于显示 `kind:1` 笔记的社交类客户端，在自定义事件出现在其时间线中时仍然可以显示一些内容。`alt` 标签的内容应提供足够的上下文，让对该事件 kind 一无所知的用户能够理解其含义。

这些只认识 `kind:1` 的客户端预计不会向中继请求其他 kind 的事件，但用户可能仍会在其笔记中引用这些怪异的事件，如果没有适当的上下文，这些笔记可能会变得毫无意义。拥有回退文本可以大大改善这种情况——即使只是为了让用户意识到他们应该尝试在其他地方查看该自定义事件。

以 `kind:1` 为中心的客户端可以通过支持 [NIP-89](89.md) 来使与这些事件 kind 的交互更加实用。
