NIP-75
======

打赏目标
---------

`草案` `可选`

本 NIP 定义了一种用于创建筹款目标的事件。用户可以通过打赏目标事件来向该目标贡献资金。

## Nostr 事件

使用 `kind:9041` 事件。

`.content` 包含目标的人类可读描述。

以下标签被定义为必需的。

- `amount` - 目标金额，单位为毫聪。
- `relays` - 用于接收和统计对该目标打赏的中继列表。

示例事件：

```yaml
{
  "kind": 9041,
  "tags": [
    ["relays", "wss://alicerelay.example.com", "wss://bobrelay.example.com", /*...*/],
    ["amount", "210000"],
  ],
  "content": "Nostrasia travel expenses",
  // 其他字段...
}
```

以下标签是可选的。

- `closed_at` - 用于确定哪些打赏计入统计的时间戳。在 `closed_at` 时间戳之后发布的打赏收据不应计入目标进度。
- `image` - 目标的图片
- `summary` - 简要描述

```yaml
{
  "kind": 9041,
  "tags": [
    ["relays", "wss://alicerelay.example.com", "wss://bobrelay.example.com", /*...*/],
    ["amount", "210000"],
    ["closed_at", "<unix timestamp in seconds>"],
    ["image", "<image URL>"],
    ["summary", "<description of the goal>"],
  ],
  "content": "Nostrasia travel expenses",
  // 其他字段...
}
```

目标可以包含一个 `r` 或 `a` 标签，链接到一个 URL 或可寻址事件。

目标可以通过指定 [`zap` 标签](57.md#appendix-g-zap-tag-on-other-events) 来包含多个受益人公钥。

可寻址事件可以通过使用 `goal` 标签（指定事件 ID 和可选的中继提示）来链接到一个目标。

```yaml
{
  "kind": 3xxxx,
  "tags": [
    ["goal", "<event id>", "<Relay URL (optional)>"],
    // 其余标签...
  ],
  // 其他字段...
}
```

## 客户端行为

客户端可以在用户个人资料上显示筹款目标。

在打赏目标事件时，客户端必须将目标事件的 `relays` 标签中的中继地址包含在打赏请求的 `relays` 标签中。

在打赏带有 `goal` 标签的可寻址事件时，客户端应将目标事件 ID 标记在打赏请求的 `e` 标签中。

## 使用场景

- 筹款客户端
- 为长文章、徽章或直播等事件添加筹款目标
