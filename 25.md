NIP-25
======

反应
---------

`draft` `optional`

反应是一个 `kind 7` 事件，用于表示用户对其他事件的反应。反应的 `content` 字段必须包含用户生成的内容，指明反应的值（惯例为 `+`、`-` 或一个 emoji）。

`content` 设置为 `+` 或空字符串的反应必须被解释为"喜欢/点赞"。
`content` 设置为 `-` 的反应必须被解释为"不喜欢/点踩"。

`content` 设置为 emoji 或 [NIP-30](30.md) 自定义表情的反应不应被解释为"喜欢/点赞"或"不喜欢/点踩"。客户端可以选择在帖子中显示此 emoji 反应。

标签
----

必须始终设置一个指向所反应事件 `id` 的 `e` 标签。`e` 标签应包含一个中继提示，指向可以找到所反应事件的中继。如果客户端决定包含其他 `e` 标签（不推荐），目标事件的 `id` 应为最后一个 `e` 标签。

应设置一个指向所反应事件公钥的 `p` 标签。如果客户端决定包含其他 `p` 标签（不推荐），目标事件的公钥应为最后一个 `p` 标签。

如果所反应事件是一个可寻址事件，则应与 `e` 标签一起包含一个 `a` 标签，它必须设置为所反应事件的坐标（`kind:pubkey:d-tag`）。

`e` 和 `a` 标签应包含中继和公钥提示。`p` 标签应包含中继提示。

反应事件可以包含一个 `k` 标签，其值为所反应事件 kind 编号的字符串形式。

**示例代码**

```swift
func make_like_event(pubkey: String, privkey: String, liked: NostrEvent, hint: String) -> NostrEvent {
    var tags: [[String]] = []
    tags.append(["e", liked.id, hint, liked.pubkey])
    tags.append(["p", liked.pubkey, hint])
    tags.append(["k", String(liked.kind)])
    let ev = NostrEvent(content: "+", pubkey: pubkey, kind: 7, tags: tags)
    ev.calculate_id()
    ev.sign(privkey: privkey)
    return ev
}
```

外部内容反应
---------------------

如果反应的目标不是原生的 nostr 事件，则该反应必须是一个 `kind 17` 事件，并且必须包含 [NIP-73](73.md) 外部 content 的 `k` 和 `i` 标签，以正确引用该 content。

_对网站做出反应：_
```yaml
{
  "kind": 17,
  "content": "⭐",
  "tags": [
    ["k", "web"],
    ["i", "https://example.com"]
  ],
}
```

_对播客剧集做出反应：_
```yaml
{
  "kind": 17,
  "content": "+",
  "tags": [
    ["k", "podcast:guid"],
    ["i", "podcast:guid:917393e3-1b1e-5cef-ace4-edaa54e1f810", "https://fountain.fm/show/QRT0l2EfrKXNGDlRrmjL"],
    ["k", "podcast:item:guid"],
    ["i", "podcast:item:guid:PC20-229", "https://fountain.fm/episode/DQqBg5sD3qFGMCZoSuLF"]
  ],
}
```

自定义表情反应
---------------------

客户端可以在反应 content 中指定一个自定义表情（[NIP-30](30.md)）`:shortcode:`。如果指定了短代码，客户端应引用 emoji 标签并将 content 渲染为 emoji。

```yaml
{
  "kind": 7,
  "content": ":soapbox:",
  "tags": [
    ["emoji", "soapbox", "https://gleasonator.com/emoji/Gleasonator/soapbox.png"]
  ],
  // other fields...
}
```

content 只能设置为一个 `:shortcode:`。并且 emoji 标签也只能有一个。
