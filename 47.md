NIP-47
======

Nostr 钱包连接 (NWC)
--------------------

`draft` `optional`

## 原理

本 NIP 描述了一种通过标准化协议让客户端访问远程闪电钱包的方式。托管服务可以实现此协议，或者用户可以运行一个桥接器，将其钱包/节点与 Nostr 钱包连接协议桥接起来。

本 NIP 定义了核心的 Nostr 钱包连接协议。更细粒度的授权模型和高级钱包功能的扩展可以在 NWC 仓库 `https://github.com/nostr-wallet-connect/nwc` 的单独规范中定义。

## 术语

* **客户端**：任何平台上想要与闪电钱包交互的 Nostr 应用。
* **用户**：使用 **客户端** 的人，希望将其钱包连接到 **客户端**。
* **钱包服务**：通常在常开计算机（例如云端或树莓派）上运行的 Nostr 应用。此应用有权访问其所服务钱包的 API。

## 运行原理

NWC 本质上是通过 nostr 中继上的 E2E 加密直接消息，在 **客户端** 和 **钱包服务** 之间进行通信。中继知道笔记的 kind 和标签，但不知道加密负载的内容。**用户** 的身份密钥不用于避免将支付活动与用户关联。理想情况下，每个单独连接使用唯一的密钥。

 1. **用户** 希望使用本 NIP 允许 **客户端** 与其钱包交互的，必须先从兼容 NIP-47 的钱包应用获取一个特殊的"连接"URI。钱包应用可以通过 QR 码屏幕、可粘贴字符串或其他方式提供此 URI。

 2. **用户** 应通过粘贴或扫描 QR 码等方式将此 URI 复制到其 **客户端** 中。**客户端** 应保存此 URI，并在以后每当 **用户**（或代表用户的 **客户端**）想要与钱包交互时使用它。**客户端** 随后应向 URI 中指定的中继请求 `info`（13194）事件。**钱包服务** 之前已将该事件发送到那些中继，中继会将其作为可替换事件保存。

 3. 当 **用户** 发起支付时，其 nostr **客户端** 创建一个 `pay_invoice` 请求，使用 URI 中的令牌进行加密，并发送（kind 23194）到连接 URI 中指定的中继。**钱包服务** 将在这些中继上监听，解密请求，然后联系 **用户的** 钱包应用发送付款。**钱包服务** 知道如何与钱包应用通信，因为连接 URI 指定了有权访问钱包应用 API 的中继。

 4. 付款完成后，**钱包服务** 将通过 URI 中的中继向 **用户** 发送加密的 `response`（kind 23195）。

 5. 可选的扩展规范可以定义额外的钱包到客户端事件。

## 事件

有三种核心事件 kind：

- `NIP-47 信息事件`：13194
- `NIP-47 请求`：23194
- `NIP-47 响应`：23195

### 信息事件

信息事件应是一个可替换事件，由 **钱包服务** 在中继上发布，以指示其支持的能力。

内容应为纯文本字符串，包含以空格分隔的支持能力，例如 `pay_invoice get_balance`。

通过可选 NWC 扩展支持并在 `extensions` 标签中公布的任何额外方法，也应包含在信息事件的内容中。

如果 **钱包服务** 支持可选 NWC 扩展，信息事件应包含一个 `extensions` 标签，其中包含以空格分隔的支持的扩展标识符，例如 `02 03 04`。

它还应包含 [加密](#加密) 部分中描述的支持的加密模式。例如：

```yaml
{
    "kind": 13194,
    "tags": [
        ["encryption", "nip44_v2 nip04"], // 加密部分中描述的支持的加密方案列表
        ["extensions", "02 03 04"]
        // ...
    ],
    "content": "pay_invoice get_balance make_invoice lookup_invoice get_info",
    // ...
}
```

### 请求和响应事件

请求和响应事件都应包含一个 `p` 标签，如果是请求则包含 **钱包服务** 的公钥，如果是响应则包含 **客户端** 的公钥。响应事件应包含一个 `e` 标签，其值为它所响应的请求事件的 ID。
可选地，请求可以有一个 `expiration` 标签，其值为 Unix 时间戳（秒）。如果请求在此时间戳之后被接收，则应忽略。

请求和响应的内容使用 [NIP44](44.md) 加密，是一个具有半固定结构的 JSON-RPCish 对象。

**向后兼容的重要说明：** 该协议的初始版本使用 [NIP04](04.md)。如果 **钱包服务** 或客户端应用在 `info` 或请求事件中没有包含 `encryption` 标签，则应假定该连接使用 NIP04 进行加密。更多信息请参见 [加密](#加密) 部分。

请求示例：

```js
{
    "kind" 23194,
    "tags": [
        ["encryption", "nip44_v2"],
        ["p", "03..." ] // 钱包服务的公钥
        // ...
    ],
    "content": nip44_encrypt({ // 加密类型对应 `encryption` 标签
        "method": "pay_invoice", // 方法，字符串
        "params": { // 参数，对象
            "invoice": "lnbc50n1..." // 命令相关数据
        }
    }),
}
```

响应示例：

```js
{
    "kind" 23195,
    "tags": [
        ["p", "03..." ] // 发起请求的客户端应用的公钥
        ["e", "1234"] // 此响应所针对的请求事件的 ID
        // ...
    ],
    "content": nip44_encrypt({ // 使用客户端请求的加密方案加密
        "result_type": "pay_invoice", // 指示 result 字段的结构
        "error": { // 对象，出错时非 null
            "code": "UNAUTHORIZED", // 字符串错误码，见下文
            "message": "人类可读的错误消息"
        },
        "result": { // 结果，对象。出错时为 null
            "preimage": "0123456789abcdef..." // 命令相关数据
        }
    })
    // ...
}
```

`result_type` 字段必须包含此事件所响应的方法的名称。
如果命令未成功执行，`error` 字段必须包含一个带有人类可读错误消息的 `message` 字段和一个带有错误码的 `code` 字段。
如果命令成功执行，`error` 字段必须为 null。

### 错误码
- `RATE_LIMITED`：客户端发送命令太快。应在几秒后重试。
- `NOT_IMPLEMENTED`：命令未知或故意未实现。
- `INSUFFICIENT_BALANCE`：钱包没有足够的资金来支付费用储备或付款金额。
- `QUOTA_EXCEEDED`：钱包已超出其支出配额。
- `RESTRICTED`：此公钥不允许执行此操作。
- `UNAUTHORIZED`：此公钥没有连接的钱包。
- `INTERNAL`：内部错误。
- `UNSUPPORTED_ENCRYPTION`：钱包服务不支持请求的加密类型。
- `OTHER`：其他错误。

## Nostr 钱包连接 URI

**客户端** 和 **钱包服务** 之间的通信需要两个密钥来加密和解密消息。连接 URI 包含 **客户端** 的密钥和仅 **钱包服务** 的公钥。

**客户端** 通过扫描 QR 码、处理深度链接或粘贴 URI 来发现 **钱包服务**。

**钱包服务** 使用协议 `nostr+walletconnect://` 生成此连接 URI，基础路径为其 32 字节十六进制编码的 `pubkey`，该密钥应每个客户端连接唯一。

连接 URI 包含以下查询字符串参数：

- `relay` 必需。**钱包服务** 连接并将在其上监听事件的中继 URL。可以有多个。
- `secret` 必需。32 字节随机生成的十六进制编码字符串。**客户端** 在与 **钱包服务** 通信时必须使用此密钥签署事件和加密负载。**钱包服务** 必须使用此密钥对应的公钥与 **客户端** 通信。
    - 授权不需要来回传递密钥。
    - 用户可以为不同的应用使用不同的密钥。密钥可以随时撤销和创建，并具有任意约束（例如预算）。
    - 密钥更难泄露，因为它不向用户显示且会被备份。
    - 它提高了隐私性，因为用户的主密钥不会与其支付行为关联。
- `lud16` 推荐。一个闪电地址，客户端可以用来在用户个人资料上自动设置 `lud16` 字段（如果用户尚未配置）。

**客户端** 随后应存储此连接，并在用户想要执行如支付发票等操作时使用它。由于本 NIP 使用临时事件，建议选择不会因不活动而关闭连接的中继，以免丢失事件，并且理想情况下应保留事件直到它们被消费或变得过时。

- 当 **客户端** 发送或接收消息时，它将使用连接 URI 中的 `secret` 和 **钱包服务** 的 `pubkey` 进行加密或解密。
- 当 **钱包服务** 发送或接收消息时，它将使用自己的 secret 和 **客户端的** `secret` 对应的公钥进行加密或解密。**钱包服务** 不应存储其为客户端生成的 secret，并且在常规操作中不得依赖知道 **客户端** secret。

### 连接字符串示例
```sh
nostr+walletconnect://b889ff5b1513b641e2a139f661a661364979c5beee91842f8f0ef42ab558e9d4?relay=wss%3A%2F%2Frelay.damus.io&secret=71a8c14c1407c113601079c4302dab36460f0ccd0ad506f1f2dc73b5100e4f3c
```

## 命令

### `pay_invoice`

描述：请求支付一张发票。

请求：
```yaml
{
    "method": "pay_invoice",
    "params": {
        "invoice": "lnbc50n1...", // bolt11 发票
        "amount": 123, // 发票金额，单位毫聪，可选
        "metadata": {} // 通用元数据，可用于添加打赏/boostagram 详情，如付款人名称/评论等，可选
    }
}
```

响应：
```yaml
{
    "result_type": "pay_invoice",
    "result": {
        "preimage": "0123456789abcdef...", // 付款的原像
        "fees_paid": 123, // 金额，单位毫聪，可选
    }
}
```

错误：
- `PAYMENT_FAILED`：付款失败。可能是由于超时、所有路由耗尽、容量不足或类似原因。

### `make_invoice`

请求：
```yaml
{
    "method": "make_invoice",
    "params": {
        "amount": 123, // 金额，单位毫聪
        "description": "string", // 发票描述，可选
        "description_hash": "string", // 发票描述哈希，可选
        "expiry": 213, // 从发票创建时间起的过期秒数，可选
        "metadata": {} // 通用元数据，可用于添加打赏/boostagram 详情，如付款人名称/评论等，可选
    }
}
```

响应：
```yaml
{
    "result_type": "make_invoice",
    "result": {
        "type": "incoming", // "incoming" 表示收款发票，"outgoing" 表示付款
        "state": "pending", // 可选
        "invoice": "string", // 编码后的发票，可选
        "description": "string", // 发票描述，可选
        "description_hash": "string", // 发票描述哈希，可选
        "preimage": "string", // 付款原像，未支付时可选
        "payment_hash": "string", // 付款的支付哈希
        "amount": 123, // 金额，单位毫聪
        "fees_paid": 123, // 金额，单位毫聪
        "created_at": unixtimestamp, // 发票/付款创建时间
        "expires_at": unixtimestamp, // 发票过期时间，不适用时可选
        "metadata": {} // 通用元数据，可用于添加打赏/boostagram 详情，如付款人名称/评论等
    }
}
```

### `lookup_invoice`

请求：
```yaml
{
    "method": "lookup_invoice",
    "params": {
        "payment_hash": "31afdf1..", // 发票的支付哈希，payment_hash 或 invoice 之一为必需
        "invoice": "lnbc50n1..." // 要查询的发票
    }
}
```

响应：
```yaml
{
    "result_type": "lookup_invoice",
    "result": {
        "type": "incoming", // "incoming" 表示收款发票，"outgoing" 表示付款
        "state": "pending", // 可以是 "pending"、"settled"、"accepted"（用于 hold 发票）、"expired"（用于发票）或 "failed"（用于付款），可选
        "invoice": "string", // 编码后的发票，可选
        "description": "string", // 发票描述，可选
        "description_hash": "string", // 发票描述哈希，可选
        "preimage": "string", // 付款原像，未支付时可选
        "payment_hash": "string", // 付款的支付哈希
        "amount": 123, // 金额，单位毫聪
        "fees_paid": 123, // 金额，单位毫聪
        "created_at": unixtimestamp, // 发票/付款创建时间
        "expires_at": unixtimestamp, // 发票过期时间，不适用时可选
        "settled_at": unixtimestamp, // 发票/付款结算时间，未支付时可选
        "metadata": {} // 通用元数据，可用于添加打赏/boostagram 详情，如付款人名称/评论等
    }
}
```

错误：
- `NOT_FOUND`：无法通过给定参数找到该发票。

### `get_balance`

请求：
```yaml
{
    "method": "get_balance",
    "params": {}
}
```

响应：
```yaml
{
    "result_type": "get_balance",
    "result": {
        "balance": 10000, // 用户余额，单位毫聪
    }
}
```

### `get_info`

请求：
```yaml
{
    "method": "get_info",
    "params": {}
}
```

响应：
```yaml
{
    "result_type": "get_info",
    "result": {
            "alias": "string",
            "color": "hex string",
            "pubkey": "hex string",
            "network": "string", // mainnet, testnet, signet, 或 regtest
            "block_height": 1,
            "block_hash": "hex string",
            "methods": ["pay_invoice", "get_balance", "make_invoice", "lookup_invoice", "get_info"], // 此连接支持的方法列表
            "extensions": ["02", "03", "04", "06", "07", "08"], // 此连接支持的可选 NWC 扩展规范列表，可选
    }
}
```

## 扩展

本 NIP 定义了核心协议和一个小型通用命令集。其他方法、元数据约定、配对流程和更细粒度的授权行为可以由可选的 NWC 扩展规范定义。

其他可选的 NWC 规范在专门的 NWC 仓库中维护：

- `https://github.com/nostr-wallet-connect/nwc`

该仓库保留 `01.md` 用于与此简化版 NIP-47 对应的 NWC 核心规范，并在从 `02.md` 开始的单独规范中定义可选功能。

## 支付发票流程示例

0. 用户使用 **客户端** 应用扫描 **钱包服务** 生成的 QR 码、点击 `nostr+walletconnect://` 深度链接或手动配置连接详情。
1. **客户端** 向 **钱包服务** 发送一个 kind `23194` 的事件。内容是 `pay_invoice` 请求。私钥是上面连接字符串中的 secret。
2. **钱包服务** 验证作者密钥是否被授权执行付款，解密负载并发送付款。
3. **钱包服务** 通过发送一个 kind `23195` 的事件来响应，其内容是包含错误消息或原像的响应。

## 加密

NWC 的初始版本使用 [NIP-04](04.md) 进行加密，该版本已被弃用，并由 [NIP-44](44.md) 取代。加密应始终优先使用 NIP-44，但在某些旧版情况下，**钱包服务** 或 **客户端** 可能尚未迁移到 NIP-44。**钱包服务** 和 **客户端** 应根据 `info` 事件中的 `encryption` 标签协商要使用的加密方法。

加密标签可以包含 `nip44_v2` 或 `nip04`。缺少此标签意味着钱包仅支持 `nip04`。

| 加密代码      | 用途                  | 备注                                                    |
|-----------------|----------------------|---------------------------------------------------------|
| `nip44_v2`      | NIP-44               | 必需                                                |
| `nip04`         | NIP-04               | 已弃用，仅用于向后兼容     |
| `<未提供>` | NIP-04               | 已弃用，仅用于向后兼容     |

协商过程如下：

1. **钱包服务** 在 `info` 事件中包含一个 `encryption` 标签。此标签包含 **钱包服务** 支持的空格分隔的加密方案列表（例如 `nip44_v2 nip04`）
2. **客户端应用** 在每个请求事件中包含一个 `encryption` 标签。此标签包含应为此请求使用的加密方案。**客户端应用** 在 **钱包服务** 支持的情况下应始终优先使用 nip44。

当 **客户端应用** 建立连接时，它应读取 info 事件并查找 `encryption` 标签。

**缺少此标签意味着钱包仅支持 nip04。**

如果存在 `encryption` 标签，**客户端应用** 将选择自身和 **钱包服务** 都支持的最优加密方案，如果可能，应始终为 nip44。

### 请求事件

当 **客户端应用** 发送请求事件时，它应包含一个 `encryption` 标签，指明其正在使用的加密方案。该方案必须被 **钱包服务** 支持，如 info 事件所示。
例如，如果客户端应用支持 nip44，请求事件可能如下所示：

```yaml
{
    "kind": 23194,
    "tags": [
        ["encryption", "nip44_v2"],
        // ...
    ],
    // ...
}
```

如果 **钱包服务** 不支持指定的加密方案，它将返回 `UNSUPPORTED_ENCRYPTION` 错误。缺少 `encryption` 标签表示使用 nip04 进行加密。

## 使用专用中继
本 NIP 未对使用的中继类型提出任何要求。但是，如果用户使用托管服务，使用由该托管服务托管的中继可能是有意义的。该中继随后可以强制执行身份验证以防止元数据泄露。在这种情况下，不依赖第三方中继也可以提高可靠性。

## 附录

### 示例 NIP-47 信息事件

```yaml
{
  "id": "df467db0a9f9ec77ffe6f561811714ccaa2e26051c20f58f33c3d66d6c2b4d1c",
  "pubkey": "c04ccd5c82fc1ea3499b9c6a5c0a7ab627fbe00a0116110d4c750faeaecba1e2",
  "created_at": 1713883677,
  "kind": 13194,
  "tags": [
    [ "encryption", "nip44_v2 nip04" ],
    [ "extensions", "02 03 04 06 07 08" ]
  ],
  "content": "pay_invoice get_balance get_info make_invoice lookup_invoice",
  "sig": "31f57b369459b5306a5353aa9e03be7fbde169bc881c3233625605dd12f53548179def16b9fe1137e6465d7e4d5bb27ce81fd6e75908c46b06269f4233c845d8"
}
```
