> __警告__ `不推荐`: 试试 [NIP-29](29.md)

NIP-28
======

公开聊天
--------

`草案` `不推荐` `可选`

本 NIP 定义了用于公开聊天频道、频道消息和基本客户端审核的新事件 kind。

它保留了五种事件 kind（40-44）供立即使用：

- `40 - 创建频道`
- `41 - 频道元数据`
- `42 - 频道消息`
- `43 - 隐藏消息`
- `44 - 静音用户`

以客户端为中心的审核使客户端开发者可以自行决定其应用中希望包含哪些类型的内容，同时不对中继施加额外要求。

## Kind 40: 创建频道

创建一个公开聊天频道。

在频道创建的 `content` 字段中，客户端应包含基本的频道元数据（如 kind 41 中规定的 `name`、`about`、`picture` 和 `relays`）。

```yaml
{
  "content": "{\"name\": \"Demo Channel\", \"about\": \"A test channel.\", \"picture\": \"https://placekitten.com/200/200\", \"relays\": [\"wss://nos.lol\", \"wss://nostr.mom\"]}",
 // other fields...
}
```


## Kind 41: 设置频道元数据

更新频道的公开元数据。

Kind 41 用于更新元数据而不修改频道的事件 id。每个 `e` 标签值可能仅保留最新的 kind 41。

客户端应忽略来自非 kind 40 公钥的 kind 41。

客户端应支持基本的元数据字段：

- `name` - string - 频道名称
- `about` - string - 频道描述
- `picture` - string - 频道图片 URL
- `relays` - array - 用于下载和广播事件的中继列表

客户端可以添加额外的元数据字段。

客户端应使用 [NIP-10](10.md) 标记的 "e" 标签来推荐中继。

也可以使用 "t" 标签设置类别名称。此类别名称可被搜索和过滤。

```yaml
{
  "content": "{\"name\": \"Updated Demo Channel\", \"about\": \"Updating a test channel.\", \"picture\": \"https://placekitten.com/201/201\", \"relays\": [\"wss://nos.lol\", \"wss://nostr.mom\"]}",
  "tags": [
    ["e", <channel_create_event_id>, <relay-url>, "root"],
    ["t", <category_name-1>],
    ["t", <category_name-2>],
    ["t", <category_name-3>],
  ],
  // other fields...
}
```


## Kind 42: 创建频道消息

向频道发送文本消息。

客户端应使用 [NIP-10](10.md) 标记的 "e" 标签来推荐中继并指定是回复还是根消息。

客户端应将 [NIP-10](10.md) 的 "p" 标签附加到回复中。

根消息：

```yaml
{
  "content": <string>,
  "tags": [["e", <kind_40_event_id>, <relay-url>, "root"]],
  // other fields...
}
```

回复另一条消息：

```yaml
{
  "content": <string>,
  "tags": [
      ["e", <kind_40_event_id>, <relay-url>, "root"],
      ["e", <kind_42_event_id>, <relay-url>, "reply"],
      ["p", <pubkey>, <relay-url>],
      // rest of tags...
  ],
  // other fields...
}
```


## Kind 43: 隐藏消息

用户不再希望看到某条消息。

`content` 可以选择包含元数据，例如 `reason`。

如果存在来自某用户的匹配事件 42 `id` 的事件 43，客户端应隐藏向该用户显示的事件 42。

客户端可以为发送事件 43 的用户以外的其他用户隐藏事件 42。

（例如，如果三个用户"隐藏"了一个事件，给出的原因包含"色情"一词，那么作为 iOS 应用的 Nostr 客户端可能会选择为所有 iOS 客户端隐藏该消息。）

```yaml
{
  "content": "{\"reason\": \"Dick pic\"}",
  "tags": [["e", <kind_42_event_id>]],
  // other fields...
}
```

## Kind 44: 静音用户

用户不再希望看到来自其他用户的消息。

`content` 可以选择包含元数据，例如 `reason`。

如果存在来自某用户的匹配事件 42 `pubkey` 的事件 44，客户端应隐藏向该用户显示的事件 42。

客户端可以为发送事件 44 的用户以外的其他用户隐藏事件 42。

```yaml
{
  "content": "{\"reason\": \"Posting dick pics\"}",
  "tags": [["p", <pubkey>]],
  // other fields...
}
```

## 中继推荐

客户端应使用元数据事件的中继 URL。

客户端可以使用任何中继 URL。例如，如果托管频道原始 kind 40 事件的中继离线，客户端可以从备份中继或比原始中继更受信任的中继获取频道数据。

动机
-----
如果我们要解决社交媒体的抗审查通信问题，我们不妨也解决 Telegram 风格消息传递的问题。

我们可以将全球对话从围墙花园带到真正对所有人开放的公共广场。


附加信息
-------

- [Chat demo PR with fiatjaf+jb55 comments](https://github.com/ArcadeCity/arcade/pull/28)
- [Conversation about NIP16](https://t.me/nostr_protocol/29566)
