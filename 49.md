NIP-49
======

私钥加密（`ncryptsec`）
------------------------------------

`draft` `optional`

本 NIP 定义了一种客户端可以用密码加密（和解密）用户私钥的方法。

对称加密密钥派生
-----------------------------------

密码 = 从用户读取。密码应 Unicode 规范化为 NFKC 格式，以确保密码可以在其他计算机/客户端上以相同方式输入。

LOG\_N = 让用户或实现者选择一个代表 2 的幂的字节（例如 18 代表 262,144），用作 scrypt 的轮数。越大的数字消耗更多时间和内存，并提供更好的保护：

    | LOG_N | 所需内存     | 快速计算机上大约时间 |
    |-------|--------------|----------------------|
    | 16    | 64 MiB       | 100 ms               |
    | 18    | 256 MiB      |                      |
    | 20    | 1 GiB        | 2 秒                 |
    | 21    | 2 GiB        |                      |
    | 22    | 4 GiB        |                      |

盐值 = 16 个随机字节

对称密钥 = scrypt(password=密码, salt=盐值, log\_n=LOG\_N, r=8, p=1)

对称密钥应为 32 字节长。

此对称加密密钥是临时的，使用后应归零并丢弃，不得存储或用于任何其他目的。


加密私钥
------------------------

私钥加密过程如下：

私钥 = 用户的私密 secp256k1 密钥，为 32 个原始字节（非十六进制或 bech32 编码！）

密钥安全字节 = 以下之一：

*  0x00 - 如果该密钥已知曾以不安全方式处理（未加密存储、未加密复制粘贴等）
*  0x01 - 如果该密钥未曾以不安全方式处理（未加密存储、未加密复制粘贴等）
 * 0x02 - 如果客户端不追踪此数据

关联数据 = 密钥安全字节

临时值 = 24 字节随机临时值

密文 = XChaCha20-Poly1305(
   明文=私钥,
   关联数据=关联数据,
   临时值=临时值,
   密钥=对称密钥
)

版本号 = 0x02

密文拼接 = concat(
   版本号,
   LOG_N,
   盐值,
   临时值,
   关联数据,
   密文
)

加密后的私钥 = bech32_encode('ncryptsec', 密文拼接)

bech32 编码前的输出应为 91 字节。

解密过程按相反顺序操作。


Test Data
---------

## Password Unicode Normalization

以下密码输入："ÅΩẛ̣"
- Unicode Codepoints: U+212B U+2126 U+1E9B U+0323
- UTF-8 bytes: [0xE2, 0x84, 0xAB, 0xE2, 0x84, 0xA6, 0xE1, 0xBA, 0x9B, 0xCC, 0xA3]

应在用于 scrypt 之前转换为 Unicode 规范化的 NFKC 格式："ÅΩẛ̣"
- Unicode Codepoints: U+00C5 U+03A9 U+1E69
- UTF-8 bytes: [0xC3, 0x85, 0xCE, 0xA9, 0xE1, 0xB9, 0xA9]

## Encryption

由于使用了随机临时值，加密过程是非确定性的。

## Decryption

以下加密后的私钥：

`ncryptsec1qgg9947rlpvqu76pj5ecreduf9jxhselq2nae2kghhvd5g7dgjtcxfqtd67p9m0w57lspw8gsq6yphnm8623nsl8xn9j4jdzz84zm3frztj3z7s35vpzmqf6ksu8r89qk5z2zxfmu5gv8th8wclt0h4p`

当使用密码='nostr' 和 log_n=16 解密时，产生以下十六进制编码的私钥：

`3501454135014541350145413501453fefb02227e449e57cf4d3a3ce05378683`

讨论
----------

### 关于密钥派生

密码不适合直接作为加密密钥。在用作加密密钥之前，需要完成两件事：

1. 需要从密码确定性地创建一个加密密钥，使其具有均匀的、功能上随机的比特分布，从而使对称加密算法的假设成立；
2. 需要在过程中注入一个缓慢的不可逆算法，以严重阻碍通过尝试大量密码进行暴力破解的解密尝试。

这些目标通过使用基于密码的密钥派生函数来实现。我们使用 scrypt，它已被证明是最大程度的内存硬的，并且多位密码学家向作者表示它优于 argon2，尽管 argon2 在 2015 年赢得了一场比赛。

### 关于对称加密算法

密码学家通常更青睐 XChaCha20-Poly1305 而不是 AES，并且它与美国政府的关联较少。它（或其不含 'X' 的早期变体）正在获得广泛使用，被用于 TLS 和 OpenSSH，并且在大多数现代加密库中可用。

建议
---------

不建议用户将这些加密后的私钥发布到 nostr，因为当攻击者能够收集大量加密私钥时，破解密钥可能会变得更容易。

建议客户端在释放内存之前，将密码和私钥的内存归零。
