NIP-40
======

过期时间戳
--------------------

`draft` `optional` `relay`

`expiration` 标签使用户能够指定一个 Unix 时间戳，在该时间戳之后消息应被视为已过期（由中继和客户端判断）并由中继删除。

#### 规范

```
标签: expiration
值:
 - [UNIX 时间戳（秒）]: 必填
```

#### 示例

```json
{
  "pubkey": "<pub-key>",
  "created_at": 1000000000,
  "kind": 1,
  "tags": [
    ["expiration", "1600000000"]
  ],
  "content": "此消息将在指定的时间戳过期，并由中继删除。\n",
  "id": "<event-id>"
}
```

注意：时间戳应与 created_at 时间戳格式相同，并应解释为中继应删除该消息的时间。

客户端行为
---------------

客户端应使用 `supported_nips` 字段来了解中继是否支持本 NIP。客户端不应向不支持本 NIP 的中继发送过期事件。

客户端应忽略已过期的事件。

中继行为
--------------

中继可以不立即删除已过期的消息，并可以无限期保留它们。
中继不应向客户端发送已过期的事件，即使它们被存储。
中继应丢弃向其发布的任何已过期的事件。
过期时间戳不影响瞬态事件的存储。

建议的使用场景
-------------------

* 临时公告 - 此标签可用于制作临时公告。例如，活动组织者可以使用此标签发布即将举办的活动的公告。
* 限时优惠 - 企业可以使用此标签进行限时优惠，这些优惠会在一定时间后过期。例如，企业可以使用此标签提供仅在限定时间内有效的特别优惠。

#### 警告
这些事件可能被第三方下载，因为它们在中继上始终是公开可访问的。
因此，不要将过期消息视为对话或其他用途的安全特性。
