NIP-52
======

日历事件
---------------

`draft` `optional`

本规范定义了表示在特定时刻或时间段内发生的日历事件。这些日历事件是_可寻址_的，且可按 [NIP-09](09.md) 删除。

与本 NIP 专指的`日历事件`不同，术语`事件`在所有 NIP 中广泛用于描述任何 Nostr 事件。此处加以区分，以辨别这两个术语。

## 日历事件

有两种类型的日历事件，由不同的 kind 表示：_基于日期的_和_基于时间的_日历事件。

以下标签对两种类型的日历事件通用：

* `d`（必填）一个简短的唯一字符串标识符。由创建日历事件的客户端生成。
* `title`（必填）日历事件的标题
* `summary`（可选）日历事件的简要描述
* `image`（可选）用于事件的图片 URL
* `location`（可选，可重复）日历事件的地点。例如：地址、GPS 坐标、会议室名称、视频通话链接
* `g`（可选）[geohash](https://en.wikipedia.org/wiki/Geohash)，用于将日历事件关联到可搜索的物理位置
* `p`（可选，可重复）32 字节十六进制参与者的公钥、可选推荐中继 URL 以及参与者在会议中的角色
* `t`（可选，可重复）用于分类日历事件的话题标签
* `r`（可选，可重复）网页、文档、视频通话、录制视频等的引用/链接
* `a`（可重复）引用 kind `31924` 日历事件的标签，请求被包含在日历中

以下标签已弃用：

* `name` 日历事件的名称。仅在 `title` 不可用时使用。

日历事件_不_要求必须是[日历](#日历)的一部分。

## 协作式日历事件请求

日历事件可以包含引用日历（kind 31924）的 `a` 标签，以请求添加到该日历中。当日历事件包含此类引用时，客户端应将其解释为通过 `a` 标签引用该日历，以请求将该事件添加到被引用的日历中。

这实现了协作式日历管理，允许多个用户为他们不拥有的日历贡献事件，但需经过日历所有者的批准。

### 基于日期的日历事件

这种类型的日历事件从某一天开始，并在未来的另一天之前结束。适用于全天或多天事件，时间和时区无关紧要。例如：纪念日、公共假日、假期。

它是一个 kind:31922 的_可寻址事件_。

这些事件的 `.content` 应为日历事件的描述。

除通用标签外，还使用以下标签：

* `start`（必填）包含起始日期，采用 ISO 8601 格式（YYYY-MM-DD）。如果存在 `end`，则必须小于 `end`。
* `end`（可选）不包含结束日期，采用 ISO 8601 格式（YYYY-MM-DD）。如果省略，日历事件与 `start` 在同一天结束。

示例：

```yaml
{
  "id": <32-bytes lowercase hex-encoded SHA-256 of the serialized event data>,
  "pubkey": <32-bytes lowercase hex-encoded public key of the event creator>,
  "created_at": <unix timestamp in seconds>,
  "kind": 31922,
  "content": "<description of calendar event>",
  "tags": [
    ["d", "<random-identifier>"],

    ["title", "<title of calendar event>"],

    // dates
    ["start", "<YYYY-MM-DD>"],
    ["end", "<YYYY-MM-DD>"],

    // location
    ["location", "<location>"],
    ["g", "<geohash>"],

    // participants
    ["p", "<32-bytes hex of a pubkey>", "<optional recommended relay URL>", "<role>"],
    ["p", "<32-bytes hex of a pubkey>", "<optional recommended relay URL>", "<role>"],
  ]
}
```

### 基于时间的日历事件

这种类型的日历事件在开始时间和结束时间之间跨越。

它是一个 kind:31923 的_可寻址事件_。

这些事件的 `.content` 应为日历事件的描述。该字段为必填，但可以是空字符串。

除通用标签外，还使用以下标签：

* `start`（必填）包含起始 Unix 时间戳（秒）。如果存在 `end`，则必须小于 `end`。
* `end`（可选）不包含结束 Unix 时间戳（秒）。如果省略，日历事件即刻结束。
* `start_tzid`（可选）开始时间戳的时区，由 IANA 时区数据库定义。例如：`America/Costa_Rica`
* `end_tzid`（可选）结束时间戳的时区，由 IANA 时区数据库定义。例如：`America/Costa_Rica`。如果省略且提供了 `start_tzid`，则结束时间戳的时区与开始时间戳相同。
* `D`（必填）事件发生日的粒度 Unix 时间戳，计算公式为 `floor(unix_seconds() / seconds_in_one_day)`。应包含多个标签以覆盖事件的时间范围。

```yaml
{
  "id": <32-bytes lowercase hex-encoded SHA-256 of the the serialized event data>,
  "pubkey": <32-bytes lowercase hex-encoded public key of the event creator>,
  "created_at": <unix timestamp in seconds>,
  "kind": 31923,
  "content": "<description of calendar event>",
  "tags": [
    ["d", "<random-identifier>"],

    ["title", "<title of calendar event>"],
    ["summary", "<brief description of the calendar event>"],
    ["image", "<string with image URI>"],

    // timestamps
    ["start", "<unix timestamp in seconds>"],
    ["end", "<unix timestamp in seconds>"],
    ["D", "82549"],

    ["start_tzid", "<IANA Time Zone Database identifier>"],
    ["end_tzid", "<IANA Time Zone Database identifier>"],

    // location
    ["location", "<location>"],
    ["g", "<geohash>"],

    // participants
    ["p", "<32-bytes hex of a pubkey>", "<optional recommended relay URL>", "<role>"],
    ["p", "<32-bytes hex of a pubkey>", "<optional recommended relay URL>", "<role>"],
  ]
}
```

## 日历

日历是日历事件的集合，以 kind `31924` 的自定义_可寻址列表_事件表示。一个用户可以有多个日历。用户可以创建日历以根据特定目的对日历事件进行分段。例如：个人、工作、旅行、聚会和会议。

日历可以接受其他用户的事件请求。当日历事件通过 `a` 标签引用日历时，这表示请求将该事件纳入。

这些事件的 `.content` 应为日历的详细描述。该字段为必填，但可以是空字符串。

* `d`（必填）全局唯一标识符。由创建日历的客户端生成。
* `title`（必填）日历标题
* `a`（可重复）引用正在响应的 kind `31922` 或 `31923` 日历事件的标签

```yaml
{
  "id": <32-bytes lowercase hex-encoded SHA-256 of the the serialized event data>,
  "pubkey": <32-bytes lowercase hex-encoded public key of the event creator>,
  "created_at": <unix timestamp in seconds>,
  "kind": 31924,
  "content": "<description of calendar>",
  "tags": [
    ["d", "<random-identifier>"],
    ["title", "<calendar title>"],
    ["a", "<31922 or 31923>:<calendar event author pubkey>:<d-identifier of calendar event>", "<optional relay url>"],
    ["a", "<31922 or 31923>:<calendar event author pubkey>:<d-identifier of calendar event>", "<optional relay url>"]
  ]
}
```

## 日历事件 RSVP（出席确认）

日历事件 RSVP 是对日历事件的回复，用以表明用户的出席意图。

如果日历事件标记了一个公钥，这可以被解释为日历事件创建者邀请该用户出席。客户端可以选择提示用户对该日历事件进行 RSVP。

任何用户都可以 RSVP，即使他们没有被日历事件标记。客户端可以选择提示日历事件创建者邀请进行 RSVP 的用户。客户端也可以选择忽略这些 RSVP。

本 NIP 有意不定义如果 RSVP 的用户未被标记，谁有权出席日历事件。这由日历事件创建者决定其语义。

本 NIP 也有意不定义如果日历事件在 RSVP 提交后发生变更会怎样。

RSVP 必须有一个指向日历事件的 `a` 标签（事件坐标），以及一个可选的指向特定日历事件修订版 ID 的 `e` 标签。如果存在 `e` 标签，客户端应将其解释为该 RSVP 是对该日历事件修订版的回应，并可选择认为其不一定适用于日历事件的其他修订版。

RSVP 可以使用 `p` 标签标记其所响应的日历事件的作者，以便客户端可以轻松查询与该作者相关的所有 RSVP。

RSVP 是一个 kind:31925 的_可寻址事件_。

这些事件的 `.content` 是可选的，应为一条自由格式的备注，为此日历事件回复提供更多上下文。

标签列表如下：

* `a`（必填）指向被响应的 kind `31922` 或 `31923` 日历事件的坐标。
* `e`（可选）被响应的 kind `31922` 或 `31923` 日历事件的事件 ID。
* `d`（必填）全局唯一标识符。由创建日历事件 RSVP 的客户端生成。
* `status`（必填）`accepted`、`declined` 或 `tentative`。确定对被引用日历事件的出席状态。
* `fb`（可选）`free` 或 `busy`。确定用户在日历事件期间是空闲还是忙碌。如果 `status` 标签设置为 `declined`，则必须省略或忽略此标签。
* `p`（可选）被响应的日历事件作者的公钥。

```yaml
{
  "id": <32-bytes lowercase hex-encoded SHA-256 of the the serialized event data>,
  "pubkey": <32-bytes lowercase hex-encoded public key of the event creator>,
  "created_at": <unix timestamp in seconds>,
  "kind": 31925,
  "content": "<note>",
  "tags": [
    ["e", "<kind 31922 or 31923 event id", "<optional recommended relay URL>"]
    ["a", "<31922 or 31923>:<calendar event author pubkey>:<d-identifier of calendar event>", "<optional recommended relay URL>"],
    ["d", "<random-identifier>"],
    ["status", "<accepted/declined/tentative>"],
    ["fb", "<free/busy>"],
    ["p", "<hex pubkey of kind 31922 or 31923 event>", "<optional recommended relay URL>"]
  ]
}
```

## 有意不支持的场景

### 重复的日历事件

重复的日历事件伴随着大量复杂性，使软件和人类都难以处理。这些复杂性包括受邀者之间的时区差异、夏令时、闰年、多种日历系统、日程或元数据的一次性变更等。

本 NIP 有意不支持重复的日历事件，将这一复杂性推给客户端自行按需手动实现。即，具有重复元数据的单个日历事件即代表重复的日历事件。
