NIP-09
======

事件删除请求
-----------

`草案` `可选` `中继`

kind 为 `5` 的特殊事件，意为"删除请求"，定义为其包含一个或多个 `e` 或 `a` 标签的列表，每个标签引用了一个作者请求删除的事件。删除请求应包含一个 `k` 标签，指明被请求删除的每个事件的 kind。

事件的 `content` 字段可以包含描述删除请求原因的文本笔记。

例如：

```yaml
{
  "kind": 5,
  "pubkey": <32-bytes hex-encoded public key of the event creator>,
  "tags": [
    ["e", "dcd59..464a2"],
    ["e", "968c5..ad7a4"],
    ["a", "<kind>:<pubkey>:<d-identifier>"],
    ["k", "1"],
    ["k", "30023"]
  ],
  "content": "these posts were published by accident",
  // other fields...
}
```

中继应删除或停止发布与被删除事件具有相同 `pubkey` 的任何被引用事件。客户端应隐藏或以其他方式指示被引用事件的删除请求状态。

中继应无限期继续发布/分享删除请求事件，因为客户端可能已经拥有了意图删除的事件。此外，客户端应将删除请求事件广播到没有该事件的其他中继。

当使用 `a` 标签时，中继应删除直到删除请求事件的 `created_at` 时间戳为止的所有版本的可替换事件。

## 客户端使用

客户端可以选择完全隐藏被有效删除请求事件引用的任何事件。这包括文本笔记、直接消息或其他尚未定义的事件 kind。或者，它们可以显示事件并附带图标或其他指示，表明作者已"放弃"该事件。`content` 字段也可以用于替换已删除事件自身的内容，但用户界面应清晰表明这是删除请求原因，而非原始内容。

客户端在隐藏或删除任何事件之前，必须验证删除请求的 `e` 标签中引用的每个事件 `pubkey` 与删除请求 `pubkey` 相同。中继通常无法执行此验证，不应被视为权威。

客户端以任何选择的方式显示删除请求事件本身，例如完全不显示，或带有显著通知。

客户端可以选择告知用户其删除请求不保证删除，因为不可能从所有中继和客户端中删除事件。

## 中继使用

中继可以验证删除请求事件仅引用与删除请求本身具有相同 `pubkey` 的事件，但这不是必需的，因为中继可能不掌握所有被引用事件的信息。

## 删除请求的删除请求

针对一个删除请求发布删除请求事件没有任何效果。客户端和中继没有义务支持"取消删除请求"功能。
