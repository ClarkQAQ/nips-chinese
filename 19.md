NIP-19
======

bech32 编码的实体
-----------------

`草案` `可选`

本 NIP 规范了 bech32 格式的字符串，可用于在客户端中展示密钥、标识符及其他信息。这些格式并不用于核心协议的任何位置，仅用于向用户展示、复制粘贴、分享、渲染二维码以及输入数据。

建议将标识符和密钥以 hex 或 binary 格式存储，因为这些格式更接近核心协议实际使用的要求。

## 裸密钥和裸标识符

为防止私钥、公钥和事件标识符（均为 32 字节字符串）之间产生混淆，可以为每种实体使用带有不同前缀的 bech32-(非-m) 编码。

以下为可能的 bech32 前缀：

  - `npub`：公钥
  - `nsec`：私钥
  - `note`：事件标识符

示例：hex 公钥 `3bf0c63fcb93463407af97a5e5ee64fa883d107ef9e558472c4eb9aaaefa459d` 转换为 `npub180cvv07tjdrrgpa0j7j7tmnyl2yr6yr7l8j4s3evf6u64th6gkwsyjh6w6`。

密钥和标识符的 bech32 编码不用于标准的 NIP-01 事件格式或过滤器内部，它们仅用于更友好的人机交互展示和输入。客户端目前仍应同时接受 hex 和 npub 格式的密钥，并在内部进行转换。

## 带有额外元数据的可分享标识符

当分享个人资料或事件时，应用可以包含中继信息和其他元数据，以便其他应用更容易定位和展示这些实体。

对于这类事件，其内容是一个二进制编码的 `TLV`（类型-长度-值）列表，其中 `T` 和 `L` 各占 1 字节（`uint8`，即 0-255 范围内的数字），`V` 是由 `L` 指定长度的字节序列。

以下为支持 `TLV` 的可能的 bech32 前缀：

  - `nprofile`：nostr 个人资料
  - `nevent`：nostr 事件
  - `naddr`：nostr _可寻址事件_ 坐标
  - `nrelay`：nostr 中继（已弃用）

以下为这些可能的标准化 `TLV` 类型：

- `0`：`特殊`
  - 取决于 bech32 前缀：
    - 对于 `nprofile`，为个人资料公钥的 32 字节
    - 对于 `nevent`，为事件标识符的 32 字节
    - 对于 `naddr`，为被引用事件的标识符（`"d"` 标签）。对于常规可替换事件，使用空字符串。
- `1`：`中继`
  - 对于 `nprofile`、`nevent` 和 `naddr`，_可选地_，一个更可能找到该实体（个人资料或事件）的中继，以 ascii 编码
  - 可以包含多次
- `2`：`作者`
  - 对于 `naddr`，事件公钥的 32 字节
  - 对于 `nevent`，_可选地_，事件公钥的 32 字节
- `3`：`种类`
  - 对于 `naddr`，种类的 32 位无符号整数，大端序
  - 对于 `nevent`，_可选地_，种类的 32 位无符号整数，大端序

## 示例

- `npub10elfcs4fr0l0r8af98jlmgdh9c8tcxjvz9qkw038js35mp4dma8qzvjptg` 应解码为公钥 hex `7e7e9c42a91bfef19fa929e5fda1b72e0ebc1a4c1141673e2794234d86addf4e`，反之亦然
- `nsec1vl029mgpspedva04g90vltkh6fvh240zqtv9k0t9af8935ke9laqsnlfe5` 应解码为私钥 hex `67dea2ed018072d675f5415ecfaed7d2597555e202d85b3d65ea4e58d2d92ffa`，反之亦然
- `nprofile1qqsrhuxx8l9ex335q7he0f09aej04zpazpl0ne2cgukyawd24mayt8gpp4mhxue69uhhytnc9e3k7mgpz4mhxue69uhkg6nzv9ejuumpv34kytnrdaksjlyr9p` 应解码为包含以下 TLV 项目的个人资料：
  - 公钥：`3bf0c63fcb93463407af97a5e5ee64fa883d107ef9e558472c4eb9aaaefa459d`
  - 中继：`wss://r.x.com`
  - 中继：`wss://djbas.sadkb.com`

## 备注

- `npub` 密钥不得在 NIP-01 事件或 NIP-05 JSON 响应中使用，仅支持 hex 格式。
- 解码 bech32 格式的字符串时，应忽略无法识别或不支持的 TLV，而不应引发错误。
- Bech32 格式的字符串应限制在 5000 个字符以内。
