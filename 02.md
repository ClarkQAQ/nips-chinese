NIP-02
======

关注列表
-----------

`final` `optional`

kind `3` 的特殊事件，代表"关注列表"，定义为包含一系列 `p` 标签，每个标签对应一个被关注或已知的个人资料。

每个标签条目应包含个人资料的公钥、可找到该公钥事件的中继 URL（如不需要可设为空字符串）、以及该个人资料的本地名称（或"昵称/爱称"）（也可设为空字符串或不提供），即：`["p", <32-bytes hex key>, <main relay URL>, <petname>]`。

`.content` 字段未被使用。

例如：

```yaml
{
  "kind": 3,
  "tags": [
    ["p", "91cf9..4e5ca", "wss://alicerelay.com/", "alice"],
    ["p", "14aeb..8dad4", "wss://bobrelay.com/nostr", "bob"],
    ["p", "612ae..e610f", "ws://carolrelay.com/ws", "carol"]
  ],
  "content": "",
  // other fields...
}
```

每次发布的新关注列表都会覆盖旧的关注列表，因此应包含所有条目。中继和客户端在收到新关注列表后，应立即删除旧的关注列表。

当新的关注对象被添加到现有列表时，客户端应将其追加到列表末尾，以便按时间顺序存储。

## 用途

### 关注列表备份

如果用户相信某个中继会长时间存储他们的事件，他们可以使用这个 kind-3 事件来备份他们的关注列表，并在其他设备上恢复。

### 个人资料发现与上下文增强

客户端可以依赖 kind-3 事件来展示用户正在浏览的个人资料的关注列表；根据用户可能正在关注或浏览的其他人的关注列表，生成关注建议列表；或在其他上下文中展示这些数据。

### 中继共享

客户端可以发布包含每个关注对象优质中继的关注列表，以便其他客户端在需要时使用这些信息更新其内部中继列表，从而提高抗审查能力。

### 昵称/爱称方案

客户端可以使用这些关注列表中的数据，构建基于他人关注列表的本地["昵称/爱称"](http://www.skyhunter.com/marcs/petnames/IntroPetNames.html)表。这减少了对全局可读名称的需求。例如：

用户有一个内部关注列表：

```json
[
  ["p", "21df6d143fb96c2ec9d63726bf9edc71", "", "erin"]
]
```

并收到两个关注列表，一个来自 `21df6d143fb96c2ec9d63726bf9edc71`：

```json
[
  ["p", "a8bb3d884d5d90b413d9891fe4c4e46d", "", "david"]
]
```

另一个来自 `a8bb3d884d5d90b413d9891fe4c4e46d`：

```json
[
  ["p", "f57f54057d2a7af0efecc8b0b66f5708", "", "frank"]
]
```

当用户看到 `21df6d143fb96c2ec9d63726bf9edc71` 时，客户端可以显示 _erin_；

当用户看到 `a8bb3d884d5d90b413d9891fe4c4e46d` 时，客户端可以显示 _david.erin_；

当用户看到 `f57f54057d2a7af0efecc8b0b66f5708` 时，客户端可以显示 _frank.david.erin_。
