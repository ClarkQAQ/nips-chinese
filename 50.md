NIP-50
======

搜索能力
--------

`草稿` `可选` `中继`

## 摘要

许多 Nostr 使用场景需要某种形式的通用搜索功能，除了按标签或 ID 的结构化查询之外。
搜索算法的具体细节将因事件 kind 而异，本 NIP 仅描述了一个用于执行此类查询的通用
可扩展框架。

## `search` 过滤器字段

为来自客户端的 `REQ` 消息引入了一个新的 `search` 字段：
```yaml
{
  // 过滤器对象上的其他字段...
  "search": <string>
}
```
`search` 字段是一个以人类可读形式描述查询的字符串，例如"最好的 nostr 应用"。
中继应尽其所能解释该查询，并返回与之匹配的事件。
中响应针对 `content` 事件字段执行匹配，并且可以在特定 kind 的上下文中有意义的情况下，
针对其他字段执行匹配。

结果应按搜索结果质量（由实现定义）降序返回，而不是按通常的 `.created_at`。`limit` 过滤器应在按匹配分数排序后应用。
查询字符串可能包含 `key:value` 对（两个由冒号分隔的单词），这些是扩展，中继应忽略
它们不支持的扩展。

客户端可以指定多个搜索过滤器，例如 `["REQ", "", { "search": "orange" }, { "kinds": [1, 2], "search": "purple" }]`。客户端可以
包括 `kinds`、`ids` 和其他过滤器字段来将搜索结果限制到特定事件 kind。

客户端应使用 supported_nips 字段来了解中继是否支持 `search` 过滤器。如果客户端准备好过滤掉不实现本 NIP 的中继
返回的多余响应，它们可以向任何中继发送 `search` 过滤器查询。

客户端应查询多个支持本 NIP 的中继，以补偿不同中继之间
可能存在的实现细节差异。

客户端可以验证中继返回的事件是否以适合客户端用例的方式
匹配指定的查询，并且可以停止查询精度低的中继。

中继应默认从搜索结果中排除垃圾信息，如果它们支持某种形式的垃圾信息过滤。

## 扩展

中继可以支持以下扩展：
- `include:spam` - 关闭默认启用的垃圾信息过滤
- `domain:<domain>` - 仅包含来自其有效 nip05 域与指定域匹配的用户的事件
- `language:<两位字母 ISO 639-1 语言代码>` - 仅包含指定语言的事件
- `sentiment:<negative/neutral/positive>` - 仅包含特定情感的事件
- `nsfw:<true/false>` - 包含或排除 NSFW（不适宜工作场所）事件（默认：true）
