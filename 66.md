NIP-66
======

中继活跃度监控
------------------

`draft` `optional` `relay`

本 NIP 定义了用于中继发现和中继监控器公告的事件。

## 中继发现事件

`30166` 中继发现事件记录从中继的 [NIP 11](https://github.com/nostr-protocol/nips/blob/master/11.md) 文档或通过探测推断出的中继特性。

当中继器发现实际执行的策略与公告的策略不同时，对应中继 NIP 11 文档字段的信息可能与实际值不一致。

`content` 可包含中继 NIP-11 信息文档的 JSON 字符串化内容。

唯一必需的标签是 `d` 标签，它必须设置为中继的[规范化](https://datatracker.ietf.org/doc/html/rfc3986#section-6) URL。对于不能通过 URL 访问的中继，可以使用十六进制编码的公钥代替。

其他标签包括：

- `rtt-open` - 中继的开放往返时间，单位为毫秒。
- `rtt-read` - 中继的读取往返时间，单位为毫秒。
- `rtt-write` - 中继的写入往返时间，单位为毫秒。
- `n` - 中继的网络类型。应为 `clearnet`、`tor`、`i2p`、`loki` 之一
- `T` - 中继类型。枚举[中继类型](https://github.com/nostr-protocol/nips/issues/1282)，格式为 `PascalCase`，例如 `PrivateInbox`
- `N` - 中继支持的 NIP
- `R` - 对应 [NIP 11](https://github.com/nostr-protocol/nips/blob/master/11.md) 的 `limitations` 数组中的需求键，包括 `auth`、`writes`、`pow` 和 `payment`。假值应使用 `!` 前缀指定，例如 `!auth`。
- `t` - 与此中继关联的主题
- `k` - 接受和不接受的事件种类（假值以 `!` 为前缀）
- `g` - [NIP-52](https://github.com/nostr-protocol/nips/blob/master/52.md) 地理哈希

具有多个值的标签应重复使用，而不是将所有值放在单个标签中，例如 `[["t", "cats"], ["t", "dogs"]]`，而不是 `[["t", "cats", "dogs"]]`。

示例：

```json
{
  "id": "<eventid>",
  "pubkey": "<monitor's pubkey>",
  "created_at": "<created_at  [some recent date ...]>",
  "signature": "<signature>",
  "content": "<optional nip 11 document>",
  "kind": 30166,
  "tags": [
    ["d","wss://some.relay/"],
    ["n", "clearnet"],
    ["N", "40"],
    ["N", "33"],
    ["R", "!payment"],
    ["R", "auth"],
    ["g", "ww8p1r4t8"],
    ["l", "en", "ISO-639-1"],
    ["t", "nsfw" ],
    ["rtt-open", "234" ]
  ]
}
```

## 中继监控器公告

种类 `10166` 中继监控器公告声明作者有意发布 `30166` 事件。此事件是可选的，旨在供打算以定期和可预测频率提供监控服务的监控器使用。

标签包括：

- `frequency` - 监控器发布事件的频率，单位为秒。
- `timeout`（可选）- 监控器执行各种检查的超时值。索引 `1` 是监控器的超时时间（毫秒）。索引 `2` 描述该超时用于何种测试。如果未提供索引 `2`，则视为所提供的超时适用于所有测试。
- `c` - 描述监控器所执行检查的小写字符串。示例包括 `open`、`read`、`write`、`auth`、`nip11`、`dns` 和 `geo`。
- `g` - [NIP-52](https://github.com/nostr-protocol/nips/blob/master/52.md) 地理哈希标签

监控器还应发布 `kind 0` 配置文件和 `kind 10002` 中继选择事件。

示例：

```json
{
  "id": "<eventid>",
  "pubkey": "<monitor's pubkey>",
  "created_at": "<created_at  [some recent date ...]>",
  "signature": "<signature>",
  "content": "",
  "tags": [
    [ "timeout", "open", "5000"  ],
    [ "timeout", "read", "3000"  ],
    [ "timeout", "write", "3000" ],
    [ "timeout", "nip11", "3000" ],
    [ "frequency", "3600" ],
    [ "c", "ws" ],
    [ "c", "nip11" ],
    [ "c", "ssl" ],
    [ "c", "dns" ],
    [ "c", "geo" ]
    [ "g", "ww8p1r4t8" ]
  ]
}
```

## 风险缓解

- 客户端绝不能依赖 `30166` 事件才能运行。缺少监控数据绝不能阻止中继连接。
- 监控器可能发布错误的 `30166` 事件，无论是由于配置错误还是恶意意图。
- 客户端不应信任单一来源。防御措施包括：信任网络过滤、查询多个监控器，以及如果过滤结果会移除过多中继则丢弃过滤结果。
