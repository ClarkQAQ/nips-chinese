NIP-F4
======

播客
--------

`draft` `optional`

本 NIP 定义了如何从中继获取播客剧集。旨在轻松适配现有的播客播放器。

## 动机

RSS 信息源很好，但它们存在一些问题，通过将信息源从 RSS URL 迁移到多个 Nostr 事件（每集一个）可以解决这些问题：

- 它们依赖于 URL，这对大多数人来说很难处理，因此播客主倾向于使用服务提供商，这些提供商可以
   - 收费——这本身并不是坏事，但事实证明，有了 Nostr 原生的播客信息源协议，托管信息源会简单得多，且托管媒体的成本极低，因此生态系统可以更加去中心化
   - 将他们封闭在自己的围墙花园中（就像 Spotify 一直在做的那样），慢慢将一个原本开放的生态系统转变为被大公司掌控的集中式系统
   - 审查播客主并阻止他们迁移（在 Nostr 上，即使中继封禁某人，这也不是问题，因为只要播客主持有密钥，迁移到其他中继就很容易，且播客客户端可以轻松发现新的中继 URL）
- 它们只能完整加载，没有任何分页或过滤功能，这意味着
   - 普通 RSS 客户端中的同步过程缓慢且笨拙（这也促使人们使用集中式解决方案）
     - 这也导致了像 [Podping](https://podping.org/) 这样有缺陷方案的产生，而在 Nostr 原生的播客信息源中是不需要这些方案的
   - 无法直接引用单个剧集（因为它只作为完整的 RSS 剧集列表中的一个成员存在），这意味着无法与朋友或在社交媒体上分享播客剧集，因此人们倾向于分享指向集中式平台或同一播客内容的 YouTube 视频的链接，这是一种反模式
- 它们无法以任何方式进行互动：听众无法"点赞"、评论或标记他们已收听该剧集，这也是推动人们走向集中式封闭平台的另一个因素：更好的分析和洞察，以及与公众互动的可能性

## 概念

其理念是每个播客拥有自己的 Nostr 密钥对。这很简单，并且允许播客将其_播客_存在与普通的 `kind:0`/`kind:1` 微博客存在结合起来。

它还允许播客拥有共享所有权（通过 MuSig2 或朋友之间的共同协议），并随时间推移改变所有权（同样基于人类对密钥材料处理方式的信任）。

## 事件定义

### 播客元数据

播客元数据存储在 `kind:10154` _可替换_事件中。此事件包含播客客户端展示播客所需的节目级别信息。

类似信息也可能存在于播客的 `kind:0` 个人资料中，但播客专用客户端应能够直接从 `kind:10154` 读取，并完全忽略 `kind:0`。

```yaml
{
  "id": "...",
  "pubkey": "<podcast-pubkey>",
  "kind": 10154,
  "created_at": 1700682555,
  "tags": [
    ["title", "<podcast-title>"],
    ["image", "<podcast-cover-image-URL>"],
    ["description", "<podcast-description>"],
    ["website", "<podcast-website-URL>"], // 可选，可重复
    ["p", "<podcast-author-pubkey>", "<role>"] // 可选，可重复
  ],
  "content": "",
  "sig": "..."
}
```

`<role>` 是可选的，可以是 `"host"`（主持人）、`"cohost"`（联合主持人）或 `"editor"`（编辑）。

### 创作的播客

`kind:10154` 中的 `p` 标签不应被盲目信任为暗示创作关系，因为任何播客都可以虚假声明任何人是其作者，因此在客户端将其作为信息展示之前，必须对照播客作者自身的等效反声明进行检查。

这就是 `kind:10164` 事件。在此，播客作者可以列出他们创作的播客的公钥。

```yaml
{
  "id": "...",
  "pubkey": "<user-pubkey>",
  "kind": 10064,
  "created_at": 1700682555,
  "tags": [
    ["p", "<podcast-pubkey-1>"],
    ["p", "<podcast-pubkey-2>"]
  ],
  "content": "",
  "sig": "..."
}
```

客户端也可以使用此事件来发现给定用户创作的所有播客。

### 播客剧集

播客剧集是 `kind:54` 事件，由播客公钥直接创作。

```yaml
{
  "id": "...",
  "pubkey": "<podcast-pubkey>",
  "kind": 54,
  "created_at": 1700682555,
  "tags": [
    ["title", "<episode title>"],
    ["image", "<optional episode image>"],
    ["description", "<a brief description>"],
    ["audio", "<audio-url>", "<optional_media_type>"], // 可以指定多次
    // ...其他重要字段将在后续进一步发现后在此指定
  ],
  "content": "<markdown content>",
  "sig": "...",
}
```

### 收藏的播客

参见 [NIP-51](51.md) 中关于 `kind:10054` 事件的描述，任何用户都可以在其中列出他们公开宣传正在收听的播客，作为软推荐。
