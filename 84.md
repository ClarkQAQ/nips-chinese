NIP-84
======

高亮
----------

`草案` `可选`

本 NIP 定义了 `kind:9802`，一种"高亮"事件，用于表示用户认为有价值的内容。

## 格式
这些事件的 `.content` 是文本中被高亮的部分。

对于非文本媒体（例如 NIP-94 音频/视频）的高亮，`.content` 可以为空。

### 引用
事件应标记高亮的来源，无论其是否为 nostr 原生内容。
对于 nostr 事件，应使用 `a` 或 `e` 标签；对于 URL，应使用 `r` 标签。

在标记 URL 时，生成这些事件的客户端应尽力清除 URL 中的跟踪器或查询字符串中明显无用的信息。

### 归属
客户端可以包含一个或多个 `p` 标签，标记被高亮材料的原始作者；这在高亮非 nostr 内容时尤其有用，因为客户端可能能够以某种方式获取该内容的 nostr 公钥
（例如提示用户或读取文档中的 `<link rel="me" href="nostr:nprofile1..." />` 标签）。可以在标签的最后一个值中包含角色信息。

```yaml
{
  "tags": [
    ["p", "<pubkey-hex>", "<relay-url>", "author"],
    ["p", "<pubkey-hex>", "<relay-url>", "author"],
    ["p", "<pubkey-hex>", "<relay-url>", "editor"]
  ],
  // 其他字段...
}
```

### 上下文
客户端可以包含一个 `context` 标签，当高亮是一个段落的一部分时，显示周围内容有助于为高亮提供上下文。

## 引用高亮
可以添加 `comment` 标签来创建引用高亮。这必须像引用转发一样渲染，将高亮作为被引用的笔记。

这样做是为了避免为单个高亮操作创建多个笔记（高亮 + kind 1），这会在微博客户端中看起来不佳，因为这些笔记可能会连续出现。

p 标签中的提及必须带有 `mention` 属性，以区别于作者和编辑。

评论中的 r 标签 URL 必须带有 `mention` 属性，以区别于高亮的来源 URL。来源 URL 必须带有 `source` 属性。
