NIP-85
======

可信断言
------------------

`草案` `可选`

某些信任网络计算需要访问大量事件和/或计算能力，这使得几乎没有可能直接在客户端上执行。本 NIP 允许用户将此类计算卸载到声明的可信服务提供者，并让这些提供者发布签名的"可信断言"事件供用户客户端使用。

## 断言事件

可信断言总是可寻址（可替换）的事件，其 `d` 标签指向断言的"主体"。本 NIP 当前识别四种不同的目标"主体"可对其执行此类计算：*公钥*、*常规事件*、*可寻址事件* 和 *nip73 标识符*。每种主体类型映射到一个事件种类：

| 主体             | 事件种类      | `d` 标签值        |
| ----------------- | -------------- | ----------------- |
| 用户              | 30382          | `<pubkey>`        |
| 事件              | 30383          | `<event_id>`      |
| 可寻址事件       | 30384          | `<event_address>` |
| NIP-73 标识符     | 30385          | `<i-tag>`         |

计算结果保存在预定义的标签中，这些标签的语法和语义由提供者和客户端共同商定。

用信任网络评分 `89` 对公钥进行排名的示例：

```yaml
{
  "kind": 30382,
  "pubkey": "<服务公钥>",
  "tags": [
    ["d", "e88a691e98d9987c964521dff60025f60700378a4879180dcbbb4a5027850411"], // 目标用户的公钥
    ["rank", "89"],
  ],
  "content": "",
  //...
}
```

服务提供者必须为不同的算法使用不同的服务密钥，包括当算法针对特定用户的视角或设置进行个性化时，为每个用户使用单独的密钥。

## Kind 30382：用户作为主体：

以下结果类型已被声明：

| 结果类型                   | 标签名称               | 标签值格式      |
| ------------------------- | ---------------------- | ----------------- |
| 关注者数量                | `followers`            | int               |
| 用户排名                  | `rank`                 | int, 归一化 0-100 |
| 首次发帖时间              | `first_created_at`     | unix 时间戳       |
| 发帖数量                  | `post_cnt`             | int               |
| 回复数量                  | `reply_cnt`            | int               |
| 反应数量                  | `reactions_cnt`        | int               |
| 接收的 Zap 金额           | `zap_amt_recd`         | int, 聪           |
| 发送的 Zap 金额           | `zap_amt_sent`         | int, 聪           |
| 接收的 Zap 数量           | `zap_cnt_recd`         | int               |
| 发送的 Zap 数量           | `zap_cnt_sent`         | int               |
| 日均接收 Zap 金额         | `zap_avg_amt_day_recd` | int, 聪           |
| 日均发送 Zap 金额         | `zap_avg_amt_day_sent` | int, 聪           |
| 接收的举报                | `reports_cnt_recd`     | int               |
| 发送的举报                | `reports_cnt_sent`     | int               |
| 共同话题                  | `t`                    | string            |
| 通常活跃开始时间          | `active_hours_start`   | int, 0-24, UTC    |
| 通常活跃结束时间          | `active_hours_end`     | int, 0-24, UTC    |

每个提供者可以提供自己的计算此类值的方式。例如，一个信任提供者的关注者数量可能会移除用户已静音的公钥，而另一个提供者则会保留它们。用户可以通过选择与自己观点一致的提供者，来决定他们希望在首选客户端中如何看到这些信息。

## Kind 30383：事件作为主体

提供者可以使用以下标签对单个事件进行评分：

| 结果类型             | 标签名称               | 标签值格式      |
| -------------------- | ---------------------- | ----------------- |
| 事件排名             | `rank`                 | int, 归一化 0-100 |
| 事件评论数量         | `comment_cnt`          | int               |
| 事件引用数量         | `quote_cnt`            | int               |
| 事件转发数量         | `repost_cnt`           | int               |
| 事件反应数量         | `reaction_cnt`         | int               |
| 事件 Zap 数量        | `zap_cnt`              | int               |
| 事件 Zap 金额        | `zap_amount`           | int, 聪           |

## Kind 30384：可寻址事件作为主体

提供者可以使用以下标签对所有版本的可寻址事件进行评分：

| 结果类型               | 标签名称               | 标签值格式      |
| ---------------------- | ---------------------- | ----------------- |
| 可寻址事件排名         | `rank`                 | int, 归一化 0-100 |
| 可寻址事件评论数量     | `comment_cnt`          | int               |
| 可寻址事件引用数量     | `quote_cnt`            | int               |
| 可寻址事件转发数量     | `repost_cnt`           | int               |
| 可寻址事件反应数量     | `reaction_cnt`         | int               |
| 可寻址事件 Zap 数量    | `zap_cnt`              | int               |
| 可寻址事件 Zap 金额    | `zap_amount`           | int, 聪           |

## Kind 30385：外部标识符作为主体

提供者可以使用 [NIP-73](73.md) 标识符对书籍、地点、电影、网站和话题标签进行评分。

| 结果类型       | 标签名称               | 标签值格式      |
| -------------- | ---------------------- | ----------------- |
| 排名           | `rank`                 | int, 归一化 0-100 |
| 评论数量       | `comment_cnt`          | int               |
| 反应数量       | `reaction_cnt`         | int               |

NIP-73 的 `k` 标签也应添加到事件中。

## 声明可信服务提供者

Kind `10040` 列出了用户对每个结果的首选来源。每个 `kind:tag` 后跟发布者的 `pubkey` 和可获取断言事件的 `relay`。服务提供者可以根据需要使用多个密钥和中继，包括当算法针对特定用户的视角或设置进行个性化时，为每个用户使用单独的密钥。用户可以公开分享这些设置，或通过使用 NIP-44 将标签列表进行 JSON 序列化并加密在 `.content` 字段中来保持私密。

```js
{
  "kind": 10040,
  "tags": [
    ["<kind:tag>", "<服务密钥>", "<中继提示>"],

    // 示例
    ["30382:rank", "4fd5e210530e4f6b2cb083795834bfe5108324f1ed9f00ab73b9e8fcfe5f12fe", "wss://nip85.nostr.band"],
    ["30382:rank", "3d842afecd5e293f28b6627933704a3fb8ce153aa91d790ab11f6a752d44a42d", "wss://nostr.wine"],
    ["30382:zap_amt_sent", "4fd5e210530e4f6b2cb083795834bfe5108324f1ed9f00ab73b9e8fcfe5f12fe", "wss://nip85.nostr.band"],
  ],
  "content": nip44Encrypt(JSON.stringify([
    ["<kind:tag>", "<服务密钥>", "<中继提示>"],

     // 示例
    ["30383:rank", "4fd5e210530e4f6b2cb083795834bfe5108324f1ed9f00ab73b9e8fcfe5f12fe", "wss://nip85.nostr.band"],
    ["30384:rank", "4fd5e210530e4f6b2cb083795834bfe5108324f1ed9f00ab73b9e8fcfe5f12fe", "wss://nip85.nostr.band"],
  ]),
  //...
}
```

如果提供者提供多种算法或某个算法的多种视角，则每个标签中列出的密钥应指向为每种算法或视角创建的密钥。

## 最后考虑

服务提供者应在新信息到达时尽快更新可信断言，但仅当每个事件的内容实际发生变化时才进行更新，以避免重新下载相同的信息。

服务提供者可以通过使用付费中继来限制对结果的访问。

在可信断言中，`p`、`e` 和 `a` 标签可以使用与 `d` 标签相同的值来添加指向该用户或事件所属中继的中继提示。

## 附录 1：服务提供者可发现性

服务提供者应对每个服务密钥签署一个 `kind 0` 事件，说明谁控制该密钥以及当前算法的版本内容。

```yaml
{
  "kind": 0,
  "pubkey": "<服务公钥>",
  "tags": [],
  "content": "{
   \"name\" = \"Vitor's Brainstormer\",
   \"about\" = \"一个来自 Vitor 视角的信任网络算法，考虑关注和静音，但不考虑举报，并为波士顿附近的任何人提供额外评分\",
   \"picture\" = \"https://brainstorm.com/logo.png\",
   \"website\" = \"https://brainstorm.com\"
  }",
  // 其他字段...
}
```

希望向其用户提供服务提供者列表的客户端应：
1. 下载用户关注列表中各用户的 `kind 10040` 事件
2. 连接到每个列出的中继，并下载相应服务密钥的 `kind 0` 事件
3. 解析 `kind 0` 并收集 `website` 属性
4. 加载该网站的 OpenGraph 标签并将其显示为可点击项
