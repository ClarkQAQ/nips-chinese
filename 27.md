NIP-27
======

文本笔记引用
--------------------

`draft` `optional`

本文档标准化了客户端在处理任何 `.content` 中包含可读文本的事件（如 kind 1 和 30023）时，对 `.content` 中其他事件和个人资料的内联引用的处理方式。

创建事件时，客户端应使用 [NIP-21](21.md) 代码在 `.content` 中间包含对其他个人资料和其他事件的提及，例如 `nostr:nprofile1qqsw3dy8cpu...6x2argwghx6egsqstvg`。

为每个引用包含 [NIP-18](18.md) 的引用标签（`["q", "<事件ID> 或 <事件地址>", "<中继URL>", "<普通事件的公钥>"]`）是可选的。当客户端希望被提及的个人资料收到提及通知，或者希望被引用的事件将其提及识别为回复时，应包含这些标签。

接收包含此类 `nostr:...` 提及的事件的阅读客户端，可以在其 `.content` 中进行任何所需的上下文增强/扩展（例如，链接到个人资料或显示被提及事件内容的预览）。如果将这些提及转换为链接，它们可以成为内部链接、[NIP-21](21.md) 链接或指向能处理这些引用的 Web 客户端的直接链接。

---

## 个人资料提及处理示例

假设 Bob 正在一个具有用户搜索和自动补全功能的客户端中写笔记，当输入 `@` 字符时触发该功能。

当 Bob 输入 `"hello @mat"` 时，客户端将提示他自动补全为 [mattn 的个人资料](https://njump.me/npub1937vv2nf06360qn9y8el6d8sevnndy7tuh5nzre4gj05xc32tnwqauhaj6)，并显示头像和名称。

Bob 按下"回车"键，现在他看到的输入笔记显示为 `"hello @mattn"`，`@mattn` 被高亮显示，表示这是一个提及。然而，在内部，事件看起来是这样的：

```json
{
  "content": "hello nostr:nprofile1qqszclxx9f5haga8sfjjrulaxncvkfekj097t6f3pu65f86rvg49ehqj6f9dh",
  "created_at": 1679790774,
  "id": "f39e9b451a73d62abc5016cffdd294b1a904e2f34536a208874fe5e22bbd47cf",
  "kind": 1,
  "pubkey": "79be667ef9dcbbac55a06295ce870b07029bfcdb2dce28d959f2815b16f81798",
  "sig": "f8c8bab1b90cc3d2ae1ad999e6af8af449ad8bb4edf64807386493163e29162b5852a796a8f474d6b1001cddbaac0de4392838574f5366f03cc94cf5dfb43f4d",
  "tags": [
    [
      "p",
      "2c7cc62a697ea3a7826521f3fd34f0cb273693cbe5e9310f35449f43622a5cdc"
    ]
  ]
}
```

（或者，提及也可以是 `nostr:npub1...` URL。）

Bob 发布此事件后，Carol 看到它时，她的客户端最初会按原样显示 `.content`，但随后会解析 `.content`，发现其中有一个 `nostr:` URL，解码它，从中提取公钥（以及可能的中继提示），从其内部数据库或中继获取该个人资料，然后将完整 URL 替换为名称 `@mattn`，并链接到该个人资料的内部页面视图。

## 详细且可能不必要的考虑

- 上面的示例非常具体，但这并不意味着所有客户端都必须实现相同的流程。可能存在完全不支持自动补全的客户端，因此它们只允许用户将原始的 [NIP-19](19.md) 代码粘贴到文本正文中，然后在发布事件前在这些代码前加上 `nostr:` 前缀。
- 引用其他事件的流程类似：用户可以粘贴 `nevent1...` 代码，客户端会将其转换为 `nostr:nevent1...` URL。当读取此类引用时，客户端可能会在预览框或类似组件中显示被引用的笔记，或者完全不显示。
- 可以采用其他显示方式：例如，如果设计为仅处理 `kind:1` 文本笔记的客户端在 `.content` 中看到 [`kind:30023`](23.md) 的 `nostr:naddr1...` URL 引用，它可以决定将其转换为指向某个硬编码的、能够显示此类事件的 Web 应用的链接。
- 客户端可以允许用户选择是否为被提及的事件或个人资料包含标签。如果有人想提及 `mattn` 但不想通知他，同时仍然在其笔记中保留一个漂亮的、可增强/可点击的链接指向他的个人资料，他们可以指示其客户端不为该特定提及创建 `["p", ...]` 标签。
- 同样地，如果有人想要引用另一条笔记，但其引用不打算与其他回复一同显示在同一条笔记下，其客户端可以选择不为 `.content` 中的任何 `nostr:nevent1...` URL 包含相应的 `["e", ...]` 标签。客户端可以决定向用户公开这些高级功能，或者在特定方面采取更明确的做法。
