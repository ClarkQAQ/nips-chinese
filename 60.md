NIP-60
======

Cashu 钱包
-------------

`draft` `optional`

本 NIP 定义了基于 cashu 的钱包的操作。

Cashu 钱包是一种其信息存储在中继中的钱包，使其可以在不同应用之间访问。

本 NIP 的目的是：
* 易用性：新用户无需在其他服务创建账户即可立即接收资金。
* 互操作性：用户的钱包随用户在不同应用中迁移。

本 NIP 不涉及用户*接收*他人的资金，它仅用于维护用户钱包的状态。

## 高层流程
1. 用户拥有一个表示钱包的 `kind:17375` 事件。
2. 用户拥有表示钱包未花费证明的 `kind:7375` 事件。——证明使用用户的私钥加密。
3. 用户拥有表示钱包花费历史的 `kind:7376` 事件。——此历史仅用于信息目的，完全可选。

### 钱包事件
```javascript
{
    "kind": 17375,
    "content": nip44_encrypt([
        [ "privkey", "hexkey" ],
        [ "mint", "https://mint1" ],
        [ "mint", "https://mint2" ]
    ]),
    "tags": []
}
```

钱包事件是一个可替换事件 `kind:17375`。

标签：
* `mint` - 此钱包使用的铸币厂——必须有一个或多个 mint 标签。
* `privkey` - 用于解锁 P2PK ecash 的私钥。必须加密存储在 `.content` 字段中。**这是专门用于钱包的不同私钥，与用户的 Nostr 私钥无关**——仅用于接收 [NIP-61](61.md) nutzaps。

### 代币事件
代币事件用于记录未花费的证明。

同一铸币厂可以有多个 `kind:7375` 事件，每个 `kind:7375` 事件内也可以有多个证明。

```javascript
{
    "kind": 7375,
    "content": nip44_encrypt({
        "mint": "https://stablenut.umint.cash",
        "unit": "sat",
        "proofs": [
            // 一个或多个默认 cashu 格式的证明
            {
                "id": "005c2502034d4f12",
                "amount": 1,
                "secret": "z+zyxAVLRqN9lEjxuNPSyRJzEstbl69Jc1vtimvtkPg=",
                "C": "0241d98a8197ef238a192d47edf191a9de78b657308937b4f7dd0aa53beae72c46"
            }
        ],
        // 在此代币创建过程中被销毁的代币（有助于钱包状态转换）
        "del": [ "代币事件-id-1", "代币事件-id-2" ]
    }),
    "tags": []
}
```

 * `.content` 是 [NIP-44](44.md) 加密的负载：
   * `mint`：证明所属的铸币厂。
   * `proofs`：未编码的证明
   * `unit` 证明的基础单位（例如：`sat`、`usd`、`eur`）。如果省略，默认为 `sat`。
   * `del`：由此代币创建而销毁的代币 ID。这有助于状态转换。

当一个代币的一个或多个证明被花费时，代币事件应被 [NIP-09](09.md) 删除，并且如果同一代币事件中还有未花费的证明，则应创建一个新的代币事件，将未花费的证明转入并添加任何找零输出（找零输出应包含 `del` 字段）。

[NIP-09](09.md) 过程中创建的 `kind:5` _删除事件_ 必须有一个 `["k", "7375"]` 标签，以便对状态转换感兴趣的客户端轻松过滤。

### 花费历史事件
当余额发生变化时，客户端应发布 `kind:7376` 事件以创建交易历史。

```javascript
{
    "kind": 7376,
    "content": nip44_encrypt([
        [ "direction", "in" ], // in = 收到，out = 发出
        [ "amount", "1" ],
        [ "unit", "sat" ],
        [ "e", "<创建代币的事件-id>", "", "created" ]
    ]),
    "tags": [
        [ "e", "<创建代币的事件-id>", "", "redeemed" ]
    ]
}
```

* `direction` - 交易方向；`in` 表示收到资金，`out` 表示发出资金。
* `unit` 金额的基础单位（例如：`sat`、`usd`、`eur`）。如果省略，默认为 `sat`。

客户端必须添加 `e` 标签以创建对被销毁和创建的代币事件的引用，并附上标签含义的标记：
* `created` - 新的代币事件已创建。
* `destroyed` - 代币事件已被销毁。
* `redeemed` - [NIP-61](61.md) nutzap 已被兑换。

所有标签都可以 [NIP-44](44.md) 加密。客户端应保持带有 `redeemed` 标记的 `e` 标签不加密。

可以添加多个 `e` 标签，并且应该加密，但带有 `redeemed` 标记的标签除外。

## 流程
想要检查用户钱包信息的客户端首先从用户的中继获取 `kind:10019` 事件，如果没有找到事件，则应回退到使用用户的 [NIP-65](65.md) 中继。

### 获取钱包和代币列表
从这些中继中，客户端应获取钱包和代币事件。

`"kinds": [17375, 7375], "authors": ["<我的公钥>"]`

### 获取证明

### 花费代币
如果 Alice 从此代币事件中花费 4 聪
```javascript
{
    "kind": 7375,
    "id": "event-id-1",
    "content": nip44_encrypt({
        "mint": "https://stablenut.umint.cash",
        "unit": "sat",
        "proofs": [
            { "id": "1", "amount": 1 },
            { "id": "2", "amount": 2 },
            { "id": "3", "amount": 4 },
            { "id": "4", "amount": 8 },
        ]
    }),
    "tags": []
}
```

她的客户端：
* 必须转入未花费的证明：
```javascript
{
    "kind": 7375,
    "id": "event-id-2",
    "content": nip44_encrypt({
        "mint": "https://stablenut.umint.cash",
        "unit": "sat",
        "proofs": [
            { "id": "1", "amount": 1 },
            { "id": "2", "amount": 2 },
            { "id": "4", "amount": 8 },
        ],
        "del": [ "event-id-1" ]
    }),
    "tags": []
}
```
* 必须删除事件 `event-id-1`
* 应将 `event-id-1` 添加到已删除代币 ID 的 `del` 数组中
* 应创建一个 `kind:7376` 事件来记录花费
```javascript
{
    "kind": 7376,
    "content": nip44_encrypt([
        [ "direction", "out" ],
        [ "amount", "4" ],
        [ "unit", "sat" ],
        [ "e", "<event-id-1>", "", "destroyed" ],
        [ "e", "<event-id-2>", "", "created" ],
    ]),
    "tags": []
}
```

### 兑换报价（可选）
在铸币厂创建报价时，可以使用事件来维护报价 ID 的状态，该 ID 将用于检查报价是否已被支付。这些事件应创建带有 2 周过期标签 [NIP-40](40.md)（这大约是闪电支付可能处于传输中的最长时间）。

但是，应用开发者应尽可能使用本地状态，并且仅在其应用上下文中有意义时才发布此事件。

```javascript
{
    "kind": 7374,
    "content": nip44_encrypt("报价-id"),
    "tags": [
        [ "expiration", "<过期时间戳>" ],
        [ "mint", "<铸币厂-url>" ]
    ]
}
```

## 附录 1：验证证明
客户端可以选择性地验证证明，以确保它们不是在过时状态下工作；此逻辑由具体实现决定何时以及为何执行验证，但如果某些证明被检查并被认定为已花费，客户端应删除该代币并转入任何未花费的证明。
