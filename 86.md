NIP-86
======

中继管理 API
--------------------

`draft` `optional`

中继可以提供用于执行管理任务的 API。这通过基于 JSON-RPC 的请求-响应协议在 HTTP 上提供，使用与中继 WebSocket 相同的 URI。

当中继在与支持 WebSocket 升级的 URI 上接收到包含 `Content-Type` 头为 `application/nostr+json+rpc` 的 HTTP(s) 请求时，它应将请求解析为包含以下字段的 JSON 文档：

```json
{
  "method": "<method-name>",
  "params": ["<array>", "<of>", "<parameters>"]
}
```

然后应返回以下格式的响应：

```json
{
  "result": {"<arbitrary>": "<value>"},
  "error": "<optional error message, if the call has errored>"
}
```

以下是可能支持的**方法**列表：

* `supportedmethods`：
  - params: `[]`
  - result: `["<method-name>", "<method-name>", ...]`（包含所有其他支持方法名称的数组）
* `banpubkey`：
  - params: `["<32-byte-hex-public-key>", "<optional-reason>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `unbanpubkey`：
  - params: `["<32-byte-hex-public-key>", "<optional-reason>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `listbannedpubkeys`：
  - params: `[]`
  - result: `[{"pubkey": "<32-byte-hex>", "reason": "<optional-reason>"}, ...]`，对象数组
* `allowpubkey`：
  - params: `["<32-byte-hex-public-key>", "<optional-reason>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `unallowpubkey`：
  - params: `["<32-byte-hex-public-key>", "<optional-reason>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `listallowedpubkeys`：
  - params: `[]`
  - result: `[{"pubkey": "<32-byte-hex>", "reason": "<optional-reason>"}, ...]`，对象数组
* `createrole`：
  - params: `[id, label, description, color, order]`
  - result: `true`（布尔值 true）
* `editrole`：
  - params: `[id, label, description, color, order]`
  - result: `true`（布尔值 true）
* `deleterole`：
  - params: `[id]`
  - result: `true`（布尔值 true）
* `assignrole`：
  - params: `["<32-byte-hex-public-key>", "<role-id>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `unassignrole`：
  - params: `["<32-byte-hex-public-key>", "<role-id>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `listeventsneedingmoderation`：
  - params: `[]`
  - result: `[{"id": "<32-byte-hex>", "reason": "<optional-reason>"}]`，对象数组
* `allowevent`：
  - params: `["<32-byte-hex-event-id>", "<optional-reason>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `banevent`：
  - params: `["<32-byte-hex-event-id>", "<optional-reason>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `listbannedevents`：
  - params: `[]`
  - result: `[{"id": "<32-byte hex>", "reason": "<optional-reason>"}, ...]`，对象数组
* `changerelayname`：
  - params: `["<new-name>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `changerelaydescription`：
  - params: `["<new-description>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `changerelayicon`：
  - params: `["<new-icon-url>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `allowkind`：
  - params: `[<kind-number>]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `disallowkind`：
  - params: `[<kind-number>]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `listallowedkinds`：
  - params: `[]`
  - result: `[<kind-number>, ...]`，数字数组
* `blockip`：
  - params: `["<ip-address>", "<optional-reason>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `unblockip`：
  - params: `["<ip-address>"]`
  - result: `true`（始终设置为 `true` 的布尔值）
* `listblockedips`：
  - params: `[]`
  - result: `[{"ip": "<ip-address>", "reason": "<optional-reason>"}, ...]`，对象数组

### 授权

请求必须包含带有有效 [NIP-98](./98.md) 事件的 `Authorization` 头，其中 `payload` 标签为必填。`u` 标签为中继 URL。

如果未提供 `Authorization` 或提供的授权无效，端点应返回 401 响应。
