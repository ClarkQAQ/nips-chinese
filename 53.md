NIP-53
======

直播与空间
----------

`draft` `optional`

本 NIP 引入了用于推广直播空间和公钥参与其中的事件类型。

## 直播

定义了一种特殊事件 `kind:30311` "直播事件"，作为一种_可寻址事件_，其标签用于推广直播的内容和参与者。

每个 `p` 标签应有一个**可显示的**标记名称，表示用户在事件中的当前角色（例如 `主持人`、`演讲者`、`参与者`），relay 信息可以为空。该事件将随着参与者的加入和离开而不断更新。

例如：

```yaml
{
  "kind": 30311,
  "tags": [
    ["d", "<唯一标识符>"],
    ["title", "<活动名称>"],
    ["summary", "<描述>"],
    ["image", "<预览图片 URL>"],
    ["t", "hashtag"]
    ["streaming", "<url>"],
    ["recording", "<url>"], // 用于在活动结束后放置编辑好的录像
    ["starts", "<Unix 时间戳（秒）>"],
    ["ends", "<Unix 时间戳（秒）>"],
    ["status", "<planned, live, ended>"],
    ["current_participants", "<number>"],
    ["total_participants", "<number>"],
    ["p", "91cf9..4e5ca", "wss://provider1.com/", "主持人", "<证明>"],
    ["p", "14aeb..8dad4", "wss://provider2.com/nostr", "演讲者"],
    ["p", "612ae..e610f", "ws://provider3.com/ws", "参与者"],
    ["relays", "wss://one.com", "wss://two.com", /*...*/],
    ["pinned", "<置顶直播聊天消息的事件 ID>"],
  ],
  "content": "",
  // other fields...
}
```

每个活动应使用不同的 `d` 标签。所有其他标签均为可选。

直播提供商应保持参与者列表较小（例如不超过 1000 个用户），并在达到限制时选择哪些参与者被列入事件中。客户端不应期望获得完整列表。活动结束后，可以删除或更新事件以总结活动并提供异步内容（例如活动录像）。

客户端应订阅 `kind:30311` 事件，通常是针对特定关注列表和状态。客户端可以显示活动中参与者的角色以及加入活动的接入点。

直播活动管理客户端应在活动期间持续更新 `kind:30311`。客户端可以选择将超过 1 小时未更新的 `status=live` 事件视为 `ended`。当状态变更到 `live` 或从 `live` 变更为其他状态时，`starts` 和 `ends` 时间戳应被更新。

活动必须使用 [NIP-19](19.md) 的 `naddr` 编码以及 `a` 标签进行链接。

### 参与同意证明

事件所有者可以在每个 `p` 标签中添加第 5 项作为证明，以表明参与者同意加入该活动。证明是由每个 `p` 的私钥对事件的完整 `a` 标签（`kind:pubkey:dTag`）进行签名后的 SHA256 值，以十六进制编码。

如果证明可用，客户端可以只显示参与者；如果证明不可用，客户端可以将参与者显示为"已邀请"。

此功能对于防止恶意事件所有者未经对方同意将大户添加到活动中、以吸引其追随者进入恶意陷阱非常重要。

### 直播聊天消息

事件 `kind:1311` 是直播聊天的频道消息。客户端必须包含活动的 `a` 标签。`e` 标签表示此帖子回复的直接父消息。

```yaml
{
  "kind": 1311,
  "tags": [
    ["a", "30311:<社区事件作者公钥>:<社区 d-标识符>", "<可选 relay URL>"],
  ],
  "content": "Zaps to live streams is beautiful.",
  // other fields...
}
```

当在 `.content` 中引用事件时，可以使用 `q` 标签配合 [NIP-21](21.md)。

```json
["q", "<event-id> or <event-address>", "<relay-url>", "<pubkey-if-a-regular-event>"]
```

主持人可以选择通过更新直播事件 `kind 30311` 中的 `pinned` 标签来置顶一个或多个直播聊天消息。

### 示例

#### 直播

```json
{
  "id": "57f28dbc264990e2c61e80a883862f7c114019804208b14da0bff81371e484d2",
  "pubkey": "1597246ac22f7d1375041054f2a4986bd971d8d196d7997e48973263ac9879ec",
  "created_at": 1687182672,
  "kind": 30311,
  "tags": [
    ["d", "demo-cf-stream"],
    ["title", "Adult Swim Metalocalypse"],
    ["summary", "Live stream from IPTV-ORG collection"],
    ["streaming", "https://adultswim-vodlive.cdn.turner.com/live/metalocalypse/stream.m3u8"],
    ["starts", "1687182672"],
    ["status", "live"],
    ["t", "animation"],
    ["t", "iptv"],
    ["image", "https://i.imgur.com/CaKq6Mt.png"]
  ],
  "content": "",
  "sig": "5bc7a60f5688effa5287244a24768cbe0dcd854436090abc3bef172f7f5db1410af4277508dbafc4f70a754a891c90ce3b966a7bc47e7c1eb71ff57640f3d389"
}
```

#### 直播聊天消息

```json
{
  "id": "97aa81798ee6c5637f7b21a411f89e10244e195aa91cb341bf49f718e36c8188",
  "pubkey": "3f770d65d3a764a9c5cb503ae123e62ec7598ad035d836e2a810f3877a745b24",
  "created_at": 1687286726,
  "kind": 1311,
  "tags": [
    ["a", "30311:1597246ac22f7d1375041054f2a4986bd971d8d196d7997e48973263ac9879ec:demo-cf-stream", "", "root"]
  ],
  "content": "Zaps to live streams is beautiful.",
  "sig": "997f62ddfc0827c121043074d50cfce7a528e978c575722748629a4137c45b75bdbc84170bedc723ef0a5a4c3daebf1fef2e93f5e2ddb98e5d685d022c30b622"
}
```

## 会议空间

会议空间包含一个或多个视频/音频房间，用户可以加入并参与直播。

### 会议空间事件（kind:30312）

定义了一种特殊事件 `kind:30312` "空间主机"，用于配置和定义虚拟互动空间的属性。每个空间都有一个唯一标识符，可以托管多个事件/会议。

```yaml
{
  "kind": 30312,
  "tags": [
    ["d", "<唯一标识符>"],              // 必需：房间标识符
    ["room", "<房间名称>"],            // 必需：显示名称
    ["summary", "<描述>"],              // 可选：房间描述
    ["image", "<预览图片 URL>"],          // 可选：房间图片
    ["status", "<open, private, closed>"],     // 必需：房间可访问性
    ["service", "<url>"],                      // 必需：访问房间的 URL
    ["endpoint", "<url>"],                     // 可选：状态/信息的 API 端点
    ["t", "<hashtag>"],                        // 可选：允许多个话题标签
    ["p", "<pubkey>", "<url>", "<role>", "<proof>"],  // 必需：至少一个提供者
    ["relays", "<url>", "<url>", /*...*/]     // 可选：首选 relay
  ],
  "content": ""  // 通常为空，可包含额外元数据
}
```

空间属性：
* 必须为开放（open）、私有（private）或关闭（closed）之一。关闭表示房间不运营。
* 可以为私有房间指定访问控制策略（例如仅限邀请、需付费）
* 可以在不使用时持久存在
* 必须至少有一个具有"主持人"角色的提供者
* 可以有多个具有不同角色的提供者

提供者角色（p 标签）：
* 主持人：完整的房间管理能力
* 管理员：房间审核管理能力
* 演讲者：允许演示/发言
* 可选的证明字段用于角色验证

### 会议室事件（kind:30313）

定义了一种 kind:30313 的特殊事件，表示房间内已安排或正在进行的会议。它必须使用 `d` 标签引用其父房间。

```yaml
{
  "kind": 30313,
  "tags": [
    ["d", "<事件唯一标识符>"],        // 必需：事件标识符
    ["a", "30312:<pubkey>:<room-id>", "wss://nostr.example.com"], // 必需：引用父空间，来自 30312 的 'd'
    ["title", "<会议标题>"],              // 必需：会议标题
    ["summary", "<描述>"],              // 可选：会议描述
    ["image", "<预览图片 URL>"],          // 可选：会议图片
    ["starts", "<Unix 时间戳>"],            // 必需：开始时间
    ["ends", "<Unix 时间戳>"],              // 可选：结束时间
    ["status", "<planned, live, ended>"],      // 必需：会议状态
    ["total_participants", "<number>"],        // 可选：总注册人数
    ["current_participants", "<number>"],      // 可选：当前活跃人数
    ["p", "<pubkey>", "<url>", "<role>"],     // 可选：带有角色的参与者
  ],
  "content": ""  // 通常为空，可包含额外元数据
}
```

事件属性：
* 必须通过 `a` 标签引用父房间
* 必须有状态（planned/live/ended）
* 必须有开始时间
* 可以跟踪参与者数量
* 可以包含特定于该事件的参与者角色

事件管理：
* 客户端应在直播期间定期更新事件状态
* 超过 1 小时未更新的事件可被视为已结束
* 状态变更时，`starts` 和 `ends` 时间戳应被更新

### 示例

#### 会议空间（kind:30312）

```yaml
{
  "kind": 30312,
  "tags": [
    ["d", "main-conference-room"],
    ["room", "Main Conference Hall"],
    ["summary", "Our primary conference space"],
    ["image", "https://example.com/room.jpg"],
    ["status", "open"],
    ["service", "https://meet.example.com/room"],
    ["endpoint", "https://api.example.com/room"],
    ["t", "conference"],
    ["p", "f7234bd4c1394dda46d09f35bd384dd30cc552ad5541990f98844fb06676e9ca", "wss://nostr.example.com/", "Owner"],
    ["p", "14aeb..8dad4", "wss://provider2.com/", "Moderator"],
    ["relays", "wss://relay1.com", "wss://relay2.com"]
  ],
  "content": ""
}
```

#### 会议室（kind:30313）

```yaml
{
  "kind": 30313,
  "tags": [
    ["d", "annual-meeting-2025"],
    ["a", "30312:f7234bd4c1394dda46d09f35bd384dd30cc552ad5541990f98844fb06676e9ca:main-conference-room", "wss://nostr.example.com"]
    ["title", "Annual Company Meeting 2025"],
    ["summary", "Yearly company-wide meeting"],
    ["image", "https://example.com/meeting.jpg"],
    ["starts", "1676262123"],
    ["ends", "1676269323"],
    ["status", "live"],
    ["total_participants", "180"],
    ["current_participants", "175"],
    ["p", "91cf9..4e5ca", "wss://provider1.com/", "Speaker"],
  ],
  "content": ""
}
```

### 房间在场

新事件 `kind: 10312` 提供了一个表示听众在场的事件。

在场事件应定期更新，客户端应过滤掉超过一定时间窗口的在场事件。

**kind `10312` 是常规可替换事件，因此一次只能在一个房间表示在场。**

```json
{
  "kind": 10312,
  "tags": [
    ["a" , "<room-a-tag>", "<relay-hint>", "root"],
    ["hand", "1"] // 举手标志
  ]
}
```
