> __警告__ `不推荐`: 试试 [NIP-29](29.md)

NIP-72
======

审核制社区（Reddit 风格）
------------------------

`draft` `unrecommended` `optional`

本 NIP 的目标是启用公共社区。它定义了可替换事件 `kind:34550` 来定义社区以及当前审核员/管理员列表。想要在社区中发帖的用户，只需用社区的 `a` 标签标记任何 Nostr 事件即可。审核员可以发出批准事件 `kind:4550`。

## 社区定义

`Kind:34550` 应包含任何有助于定义社区和审核员集合的字段。`relay` 标签可用于描述下载请求和批准的首选 relay。社区定义事件的 `d` 标签可以兼作其名称，但如果提供了 `name` 标签，则应显示它而不是 `d` 标签。

```yaml
{
  "created_at": <Unix timestamp in seconds>,
  "kind": 34550,
  "tags": [
    ["d", "<community-d-identifier>"],
    ["name", "<Community name>"],
    ["description", "<Community description>"],
    ["image", "<Community image url>", "<Width>x<Height>"],

    //.. other tags relevant to defining the community

    // moderators
    ["p", "<32-bytes hex of a pubkey1>", "<optional recommended relay URL>", "moderator"],
    ["p", "<32-bytes hex of a pubkey2>", "<optional recommended relay URL>", "moderator"],
    ["p", "<32-bytes hex of a pubkey3>", "<optional recommended relay URL>", "moderator"],

    // relays used by the community (w/optional marker)
    ["relay", "<relay hosting author kind 0>", "author"],
    ["relay", "<relay where to send and receive requests>", "requests"],
    ["relay", "<relay where to send and receive approvals>", "approvals"],
    ["relay", "<relay where to post requests to and fetch approvals from>"]
  ],
  // other fields...
}
```

## 向社区发帖

[NIP-22](22.md) 的 kind 1111 事件应用于向社区发布文本笔记，其中 `A` 标签始终限定在社区定义范围内。

### 顶级帖子

对于顶级帖子，大写和小写的 NIP-22 标签都应指向社区定义本身。

```yaml
{
  "kind": 1111,
  "tags": [
    ["A", "34550:<community-author-pubkey>:<community-d-identifier>", "<optional-relay-url>"],
    ["a", "34550:<community-author-pubkey>:<community-d-identifier>", "<optional-relay-url>"],
    ["P", "<community-author-pubkey>", "<optional-relay-url>"],
    ["p", "<community-author-pubkey>", "<optional-relay-url>"],
    ["K", "34550"],
    ["k", "34550"],
  ],
  "content": "Hi everyone. It's great to be here!",
  // other fields...
}
```

### 嵌套回复

对于嵌套回复，大写标签仍应指向社区定义，而小写标签应指向父帖子或回复。

```yaml
{
  "kind": 1111,
  "tags": [
    // community definition itself
    ["A", "34550:<community-author-pubkey>:<community-d-identifier>", "<optional-relay-url>"],
    ["P", "<community-author-pubkey>", "<optional-relay-url>"],
    ["K", "34550"],

    // parent post or reply
    ["e", "<parent-event-id>", "<optional-relay-url>"],
    ["p", "<parent-event-author-pubkey>", "<optional-relay-url>"],
    ["k", "<parent-event-kind>"] // most likely "1111"
  ],
  "content": "Agreed! Welcome everyone!",
  // other fields...
}
```

### 向后兼容性说明

以前，kind 1 事件用于社区中的帖子，使用指向社区的 "a" 标签。为了向后兼容，客户端仍然可以查询 kind 1 事件，但不应将其用于新帖子。相反，客户端应使用具有如上所述的 `A` 和 `a` 标签的 kind 1111 事件。

## 审核

任何人都可以发出批准事件，以表达他们认为某个帖子适合某个社区的立场。客户端可以选择认可哪些批准事件，但应至少使用由群组定义的审核员发布的那些。

一个批准事件必须包含一个或多个社区 `a` 标签、一个指向帖子的 `e` 或 `a` 标签，以及帖子作者的 `p` 标签（用于批准通知）。`a` 标签前缀可用于区分社区和可替换事件指针（社区 `a` 标签始终以 `34550` 开头）。

该事件还应包含 `.content` 中 JSON 字符串化的 `发帖请求` 事件，以及一个带有原始帖子事件类型的 `k` 标签，以允许按类型筛选已批准的帖子。

审核员可以随时使用 [NIP-09 事件删除请求](09.md) 请求删除其对某个帖子的批准。

```yaml
{
  "pubkey": "<32-bytes lowercase hex-encoded public key of the event creator>",
  "kind": 4550,
  "tags": [
    ["a", "34550:<event-author-pubkey>:<community-d-identifier>", "<optional-relay-url>"],
    ["e", "<post-id>", "<optional-relay-url>"],
    ["p", "<port-author-pubkey>", "<optional-relay-url>"],
    ["k", "<post-request-kind>"]
  ],
  "content": "<the full approved event, JSON-encoded>",
  // other fields...
}
```

建议多个审核员批准帖子，以避免在审核员从所有者列表中被移除时帖子从社区中被删除。如果必须轮换整个审核员列表，新的审核员组必须重新签署过去帖子的批准，否则社区将重新启动。所有者也可以定期复制并重新签署每个审核员的批准事件，以确保帖子不会随审核员一起消失。

可替换事件的批准可以通过三种方式创建：

1. 如果审核员希望批准可替换事件的每个单独更改，则将其作为 `e` 标签标记
2. 如果审核员授权可替换事件作者在无需额外批准的情况下进行更改，则将其作为 `a` 标签标记
3. 同时使用 `e` 和 `a` 标签标记可替换事件，这使客户端能够显示事件的原始版本和更新版本，并在 UI 中做出适当说明。

由于 relay 被指示删除可替换事件的旧版本，使用 `e` 标签的批准的 `content` 必须包含事件的特定版本，否则客户端可能无法找到该版本内容的任何位置。

客户端应评估任何非 `34550:*` 的 `a` 标签，作为需要针对所有 `34550:*` `a` 标签批准的帖子。

## 跨帖发布

客户端可以通过使用如上所述的 `a` 标签向一个或多个社区发布 NIP 18 的 `kind 6` 或 `kind 16` 转发帖子来支持跨帖发布。转发帖子的 `content` 必须是原始事件，而不是批准事件。
