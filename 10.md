NIP-10
======

文本笔记与讨论串
----------------------

`draft` `optional`

本 NIP 将 `kind:1` 定义为一个简单的纯文本笔记。

## 摘要

`.content` 属性包含一些人类可读的文本。

`e` 标签可用于定义笔记讨论串的根与回复。它们应按回复栈从根到直接父级排序。

当在 `.content` 中使用 [NIP-21](21.md) 引用事件时，可以使用 `q` 标签。

```json
["q", "<event-id> or <event-address>", "<relay-url>", "<pubkey-if-a-regular-event>"]
```

`e` 和 `q` 标签的作者应作为 `p` 标签添加，以通知新的回复或引用。

不应使用诸如 markdown 和 HTML 之类的标记语言。

## 带标记的 "e" 标签（推荐）

带有 `e` 标签的 Kind 1 事件是对其他 kind 1 事件的回复。Kind 1 回复不得用于回复其他 kind，应改用 [NIP-22](22.md)。

`["e", <event-id>, <relay-url>, <marker>, <pubkey>]`

其中：

 * `<event-id>` 是被引用事件的 id。
 * `<relay-url>` 是与引用关联的推荐 relay 的 URL。客户端应添加有效的 `<relay-url>` 字段，但也可以将其留空为 `""`。
 * `<marker>` 是可选的，如果存在，则为 `"reply"`、`"root"` 之一。
 * `<pubkey>` 是可选的，应为被引用事件作者的公钥。

标记为 `"reply"` 的表示所回复的回复事件的 id。标记为 `"root"` 的表示所回复的回复讨论串的根 id。对于顶级回复（直接回复根事件的回复），应仅使用 `"root"` 标记。

对讨论串根的直接回复应只有一个类型为 "root" 的带标记 "e" 标签。

>此方案更优，因为它允许事件提及他人，而不会将其与 `<reply-id>` 或 `<root-id>` 混淆。

`<pubkey>` 应为被 `e` 标签引用的事件的作者的公钥，这在 outbox 模型中用于在 relay 提示未能解析该事件时，从作者写入的 relay 中搜索该事件。

## "p" 标签

在文本事件中使用，包含一系列公钥，用于记录谁参与了一个回复讨论串。

当回复文本事件 E 时，回复事件的 "p" 标签应包含 E 的所有 "p" 标签以及被回复事件的 `"pubkey"`。

示例：假设一个由 `a1` 创作的文本事件，带有 "p" 标签 [`p1`, `p2`, `p3`]，那么回复的 "p" 标签应为 [`a1`, `p1`, `p2`, `p3`]，顺序不限。

## 已弃用的位置型 "e" 标签

此方案已不再常用，在此仅用于保持与网络中较旧事件的向后兼容。

位置型 `e` 标签已被弃用，因为当一个事件引用另一个事件但不是回复时，它们会产生难以甚至无法解决的歧义。

它们使用没有任何标记的简单 `e` 标签。

`["e", <event-id>, <relay-url>]` 遵循 NIP-01。

其中：

 * `<event-id>` 是被引用事件的 id。
 * `<relay-url>` 是与引用关联的推荐 relay 的 URL。许多客户端将此字段视为可选。

**事件中 "e" 标签的位置表示特定含义如下**：

 * 无 "e" 标签：<br>
 此事件不是对任何其他事件的回复，也不引用任何其他事件。

 * 一个 "e" 标签：<br>
 `["e", <id>]`：此事件所回复的事件的 id。

 * 两个 "e" 标签：`["e", <root-id>]`, `["e", <reply-id>]` <br>
 `<root-id>` 是回复链根部的事件的 id。`<reply-id>` 是此事件所回复的文章的 id。

 * 多个 "e" 标签：`["e", <root-id>]` `["e", <mention-id>]`, ..., `["e", <reply-id>]`<br>
可以有任意数量的 `<mention-ids>`。这些是可能处于（也可能不处于）回复链中的事件的 id。它们是从此事件中引用的。`root-id` 和 `reply-id` 同上。
