NIP-62
======

消失请求
-----------------

`draft` `optional` `relay`

本 NIP 提供了一种 Nostr 原生方式来请求完全重置密钥在 Web 上的指纹。此程序在某些司法管辖区具有法律约束力，因此，本 NIP 的支持者应真正从其数据库中删除事件。

## 向中继请求消失

Kind `62` 请求特定中继删除 `.pubkey` 在 `.created_at` 之前的所有内容，包括 [NIP-09](09.md) 删除事件。

```yaml
{
  "kind": 62,
  "pubkey": <32字节十六进制编码的事件创建者公钥>,
  "tags": [
    ["relay", "<中继 url>"]
  ],
  "content": "<原因或说明>",
  //...其他字段
}
```

标签列表必须包含至少一个 `relay` 值。

内容可以包含向中继运营者说明的原因或法律通知。

如果事件中标记了其服务 URL，中继必须完全删除 `.pubkey` 的任何事件。

如果事件中标记了其服务 URL，中继应删除所有 p 标签标记了 `.pubkey` 的 [NIP-59](59.md) 礼物包装，即删除所有发送给该公钥的私信。

中继必须确保已删除的事件不能被重新广播到该中继。

中继可以存储已签名的消失请求以备记录。

付费中继或限制谁可以发布的中继，无论用户状态如何，都必须遵守消失请求。

发布针对消失请求的删除请求事件（Kind `5`）没有效果。客户端和中继没有义务支持"取消消失请求"功能。

客户端应仅将此事件发送到目标中继。

## 全局消失请求

要请求所有中继删除所有内容，事件必须包含一个值为大写的 `ALL_RELAYS` 的 `relay` 标签。

```yaml
{
  "kind": 62,
  "pubkey": <32字节十六进制编码的事件创建者公钥>,
  "tags": [
    ["relay", "ALL_RELAYS"]
  ],
  "content": "<原因>",
  //...其他字段
}
```

客户端应尽可能多地向中继广播此事件。
