NIP-54
======

维基
----

`draft` `optional`

本 NIP 定义了 `kind:30818`（一种_可寻址事件_），用于描述（或百科条目）特定主题，并且预计多人将编写关于完全相同主题的文章，可能略有变化或完全独立的内容。

文章通过小写、规范化的 `d` 标签来标识。

## 文章
```json
{
  "content": "A wiki is a hypertext publication collaboratively edited and managed by its own audience.",
  "tags": [
    ["d", "wiki"],
    ["title", "Wiki"]
  ]
}
```

## `d` 标签规范化规则

- 所有具有大小写变体的字母必须转换为小写。
- 空白必须转换为 `-`。
- 标点符号和符号应移除。
- 多个连续的 `-` 应合并为单个 `-`。
- 开头和结尾的 `-` 应移除。
- 非 ASCII 字母（例如日文、中文、阿拉伯文、西里尔文）必须作为 UTF-8 保留。
- 数字必须保留。

例如：
- `"Wiki Article"` → `"wiki-article"`
- `"What's Up?"` → `"whats-up"`
- `"  Hello  World  "` → `"hello-world"`
- `"Article 1"` → `"article-1"`
- `"ウィキペディア"` → `"ウィキペディア"`（日文，大小写不变）
- `"Ñoño"` → `"ñoño"`（西班牙文，小写化）
- `"Москва"` → `"москва"`（俄文，小写化）
- `"日本語 Article"` → `"日本語-article"`（混合文字）

## 内容

`content` 应为 [Djot](https://djot.net/)，具有两个特殊功能：

1. 链接可以具有 [NIP-21](21.md) 格式的目标 URI，如 `[Bob](nostr:npub1...)`。
2. 当引用式链接找不到引用时，它应改为链接到该名称的维基文章（维基链接行为）。目标按照上述 `d` 标签规范化规则进行规范化。

例如：

```djot
Bitcoin is a [cryptocurrency][] invented by [Satoshi Nakamoto][].

See also: [proof of work][] and [lightning network][Lightning Network].

[Satoshi Nakamoto]: nostr:npub1satoshi...
```

在上述文章中：
- `[cryptocurrency][]` 链接到 `d` 标签为 `"cryptocurrency"` 的维基文章（未定义引用，因此成为维基链接）
- `[Satoshi Nakamoto][]` 链接到 npub（定义了引用）
- `[proof of work][]` 链接到 `d` 标签为 `"proof-of-work"` 的文章
- `[lightning network][Lightning Network]` 链接到 `"lightning-network"`，显示为 "lightning network"

维基链接也适用于非拉丁文字，遵循相同的规范化规则：
- `[ビットコイン][]` → 链接到 `d` 标签为 `"ビットコイン"` 的文章
- `[Japanese Article][日本語 記事]` → 链接到 `"日本語-記事"`，显示为 "Japanese Article"
- `[Биткойн][]` → 链接到 `d` 标签为 `"биткойн"` 的文章（西里尔文，小写化）

[NIP-21](21.md) 的 `nostr:` 链接也可以用于链接到个人资料或任意 Nostr 事件。不建议链接到文章的特定版本——应优先使用维基链接语法，因为应留给读者及其客户端决定他们想要阅读任何给定文章的哪个版本。

## 可选的额外标签

  - `title`：用于显示标题与 `d` 标签不同时。
  - `summary`：用于在列表中显示。
  - `a` 和 `e`：用于引用维基文章所分支（fork）自的原始事件。

## 合并请求

事件 `kind:818` 表示从分支文章合并到源文章的请求。它指向一个公钥，并引用原始文章和修改后的事件。

```json
{
  "content": "I added information about the block size limit",
  "kind": 818,
  "tags": [
    ["a", "30818:<destination-pubkey>:bitcoin", "<relay-url>"],
    ["e", "<version-against-which-the-modification-was-made>", "<relay-url>"],
    ["p", "<destination-pubkey>"],
    ["e", "<version-to-be-merged>", "<relay-url>", "source"]
  ]
}
```

- `.content`：可选的解释，说明为何请求合并。
- `a` 标签：应被修改的文章的标签（即此合并请求的目标）。
- `e` 标签：可选的，此修改所基于的文章版本。
- 带有 `source` 标记的 `e` 标签：应被合并的事件 ID。此事件 ID 必须是本 NIP 中定义的 `kind:30818`。

目标公钥可以创建 [NIP-25](25.md) 反应，使用 `+` 或 `-` 标记 `kind:818` 事件来接受或拒绝合并请求。

## 重定向

事件 `kind:30819` 也被定义为表示"维基重定向"，即如果有人认为"BTC"应重定向到"Bitcoin"，他们可以发布这些事件之一，而不是复制内容。这些事件可用于在客户端上自动重定向文章之间，也可用于生成众包的"消歧义"页面（[在 Wikipedia 中常见](https://en.wikipedia.org/wiki/Help:Disambiguation)）。

```json
{
  "kind": 30819,
  "tags": [
    ["d", "btc"],
    ["a", "30818:<pubkey>:bitcoin", "<relay-url>"]
  ],
  "content": ""
}
```

## 如何决定显示哪篇文章

由于每个名称可能有多篇文章，客户端必须进行某种优先级排序。此标准应在用户和客户端之间有所不同，但下面描述了一些可用的方法：

### 反应

[NIP-25](25.md) 反应非常简单，可用于在维基文章作者及其内容之间创建简单的信任网络。虽然单纯计算原始"点赞"数量效果不佳，但对任何维基文章事件使用 `+` 反应可以被解读为对该文章的推荐，以及对该文章作者的部分推荐。当遵循 2 或 3 层深度推荐时，突然间大部分文章可能有某种形式的标记。

### Relay

可以创建 kind 10102 的 [NIP-51](51.md) relay 列表，然后维基客户端可以使用这些列表来确定首先查询哪些 relay，并相对于从其他 relay 获取的其他事件进行不同的排名。

### 联系人列表

[NIP-02](02.md) 联系人列表可以构成推荐系统的基础，然后通过嵌套查询使用 relay 列表和反应列表进行扩展。这些列表仅因为其广泛使用而成为良好的起点。

### 维基相关联系人列表

[NIP-51](51.md) 列表也可以用于创建一个仅在维基作者或维基策展上下文中被信任的用户列表。

## 分支

维基事件可以使用 `fork` 标记标记其他维基事件，以指明此事件来自不同的版本。`a` 和 `e` 标签都应使用并应用 `fork` 标记，以标识它分支自的确切版本。

## 引用/推崇

维基事件可以使用 `defer` 标记标记其他维基事件，以表明它认为他人的条目是其自身的"更好"版本。如果使用 `defer` 标记，`a` 和 `e` 标签都应使用。

这是一个比 `+` 反应更强的信任信号。

当用户编辑他人的条目时，此标记非常有用；如果原始作者包含了编辑者的更改，而编辑者不想保留/维护独立版本，则 `defer` 标签可以有效被视为编辑者版本的"删除"，并将该公钥的 WoT 权重置于原始作者版本之后。

## 为什么选择 Djot？

[Djot](https://djot.net/) 是由 John MacFarlane（Pandoc 的作者和 CommonMark 的合著者）创建的标记语言。选择它基于以下原因：

- **定义明确的规范**：与 Markdown（许多不兼容的方言）或 Asciidoc（规范绑定到 Ruby 实现）不同，Djot 有清晰、独立的规范。
- **原生实现**：可在 JavaScript、Lua、Rust、Go 和其他语言中使用，无需转译。
- **丰富功能**：支持上标、下标、脚注、表格、定义列表和数学公式——这些功能对百科类内容非常有用。
- **熟悉的语法**：与基本 Markdown 相似，易于学习。
- **快速解析**：设计用于高效的线性时间解析。
