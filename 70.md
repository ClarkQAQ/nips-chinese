NIP-70
======

受保护事件
----------

`草稿` `可选` `中继`

当存在 `"-"` 标签时，意味着该事件是"受保护的"。

受保护事件是只能由其作者发布到中继的事件。这通过中继确保作者在发布自己的事件之前已[认证](42.md)，或者直接完全拒绝带有 `["-"]` 的事件来实现。

中继的默认行为必须是拒绝任何包含 `["-"]` 的事件。

希望接受此类事件的中继必须首先要求客户端执行 [NIP-42](42.md) 的 `AUTH` 流程，然后检查认证客户端是否与正在发布的事件具有相同的公钥，并且仅在这种情况下接受该事件。

受保护事件的转发不得嵌入被转发的事件。如果受保护事件的转发仍然嵌入了该事件，中继应直接拒绝它。

## 标签

该标签是一个只有单个条目的简单标签：`["-"]`。它可以添加到任何事件中。

## 示例流程

- 用户 `79be667ef9dcbbac55a06295ce870b07029bfcdb2dce28d959f2815b16f81798` 连接到中继 `wss://example.com`：

```yaml
/* 客户端： */
["EVENT",{"id":"cb8feca582979d91fe90455867b34dbf4d65e4b86e86b3c68c368ca9f9eef6f2","pubkey":"79be667ef9dcbbac55a06295ce870b07029bfcdb2dce28d959f2815b16f81798","created_at":1707409439,"kind":1,"tags":[["-"]],"content":"hello members of the secret group","sig":"fa163f5cfb75d77d9b6269011872ee22b34fb48d23251e9879bb1e4ccbdd8aaaf4b6dc5f5084a65ef42c52fbcde8f3178bac3ba207de827ec513a6aa39fa684c"}]
/* 中继： */
["AUTH", "<challenge>"]
["OK", "cb8feca582979d91fe90455867b34dbf4d65e4b86e86b3c68c368ca9f9eef6f2", false, "auth-required: this event may only be published by its author"]
/* 客户端： */
["AUTH", {}]
["EVENT",{"id":"cb8feca582979d91fe90455867b34dbf4d65e4b86e86b3c68c368ca9f9eef6f2","pubkey":"79be667ef9dcbbac55a06295ce870b07029bfcdb2dce28d959f2815b16f81798","created_at":1707409439,"kind":1,"tags":[["-"]],"content":"hello members of the secret group","sig":"fa163f5cfb75d77d9b6269011872ee22b34fb48d23251e9879bb1e4ccbdd8aaaf4b6dc5f5084a65ef42c52fbcde8f3178bac3ba207de827ec513a6aa39fa684c"}]
["OK", "cb8feca582979d91fe90455867b34dbf4d65e4b86e86b3c68c368ca9f9eef6f2", true, ""]
```

## 为何需要它

在某些情况下，防止事件无限制地传播到所有可能的中继，并将其限制在特定人群或半封闭的社区中继中，是有益的。即使信息是公开的，将其分隔在不同的中继中也可能是合理的。

当发布者与中继有某种关系，并信任中继不会将其发布的事件泄露给任何人时，也可以使用此机制创建封闭的访问信息流。

尽管最终无法限制信息在互联网上的传播（例如，封闭群组的成员可能想要获取本应受限的事件并将其重新发布到其他中继），但大多数中继会乐于不助长这些所谓"海盗"的行为，以尊重作者的原始决定，因此如果有办法的话，会很乐意拒绝这些重新发布的行为。

本 NIP 为这些作者和中继提供了明确发出信号的手段，表明某个事件不应被第三方重新发布。
