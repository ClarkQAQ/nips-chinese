> __警告__ `不推荐`: 这已经完全失控了，建议改用特定用例的微型标准

NIP-90
======

数据自动售货机
--------------------

`草案` `不推荐` `可选`

本 NIP 定义了客户与服务提供者之间进行按需计算的交互。

金钱进，数据出。

## 种类
本 NIP 保留了 `5000-7000` 范围用于数据自动售货机使用。

| 种类       | 描述               |
| ---------- | ------------------ |
| 5000-5999  | 作业请求种类       |
| 6000-6999  | 作业结果           |
| 7000       | 作业反馈           |

作业结果始终使用比作业请求种类高 `1000` 的种类编号。（例如，请求：`kind:5001` 得到结果：`kind:6001`）。

作业请求类型[另行定义](https://github.com/nostr-protocol/data-vending-machines/tree/master/kinds)。

## 原理
Nostr 可以作为数据处理的 marketplace，用户可以请求以特定方式处理作业（例如，"语音转文字"、"摘要"等），但他们不一定关心"谁"来处理数据。

本 NIP 不应与 1:1 市场混淆；相反，它描述了一种流程：用户宣布期望的输出、支付意愿，然后服务提供者竞争以最佳方式满足作业要求。

### 角色
本 NIP 描述的工作流程中有两个角色：
* 客户（请求作业的 npub）
* 服务提供者（执行作业的 npub）

## 作业请求（`kind:5000-5999`）
由客户发布的处理数据请求。此事件表示客户有兴趣接收某种计算的结果。

```yaml
{
    "kind": 5xxx, // 在 5000-5999 范围内的种类
    "content": "",
    "tags": [
        [ "i", "<data>", "<input-type>", "<relay>", "<marker>" ],
        [ "output", "<mime-type>" ],
        [ "relays", "wss://..." ],
        [ "bid", "<msat-amount>" ],
        [ "t", "bitcoin" ]
    ],
    // 其他字段...
}
```

所有标签都是可选的。

* `i` 标签：作业的输入数据（零个或多个输入）
    * `<data>`：输入的参数
    * `<input-type>`：此参数应如何被解释。必须为以下之一：
        * `url`：一个 URL，用于获取待处理的数据
        * `event`：一个 Nostr 事件 ID
        * `job`：具有指定事件 ID 的先前作业的输出。决定基于哪个输出继续由服务提供者决定（例如，等待客户发信号、等待付款等）
        * `text`：`<data>` 即为输入值，无需解析
    * `<relay>`：如果输入类型为 `event` 或 `job`，则为发布事件/作业的中继，否则为可选或空字符串
    * `<marker>`：可选字段，指示此输入如何用于作业上下文
* `output`：期望的输出格式。不同的作业请求 `kind` 对此有更精确的定义。
* `param`：作业的可选参数，以键（第一个参数）/值（第二个参数）形式表示。不同的作业请求 `kind` 对此有更精确的定义。（例如 `[ "param", "lang", "es" ]`）
* `bid`：客户可以指定其愿意支付的最高金额（以毫聪为单位）
* `relays`：服务提供者应发布响应的中继列表
* `p`：客户感兴趣的服务提供者。其他服务提供者仍可能选择处理该作业

### 加密参数

如果用户希望保密输入参数，他们可以使用服务提供者的 'p' 标签加密 `i` 和 `param` 标签，并将其添加到 content 字段。添加一个 `encrypted` 标签。私有标签的加密将使用 [NIP-04 - 加密直接消息加密](04.md)，使用用户的私钥和服务提供者的公钥生成共享秘密。

```json
[
  ["i", "法国的首都是什么？", "text"],
  ["param", "model", "LLaMA-2"],
  ["param", "max_tokens", "512"],
  ["param", "temperature", "0.5"],
  ["param", "top-k", "50"],
  ["param", "top-p", "0.7"],
  ["param", "frequency_penalty", "1"]
]
```

这些参数数据将被加密并添加到 `content` 字段，并且应该存在 `p` 标签。

```yaml
{
  "content": "BE2Y4xvS6HIY7TozIgbEl3sAHkdZoXyLRRkZv4fLPh3R7LtviLKAJM5qpkC7D6VtMbgIt4iNcMpLtpo...",
  "tags": [
    ["p", "04f74530a6ede6b24731b976b8e78fb449ea61f40ff10e3d869a3030c4edc91f"],
    ["encrypted"]
  ],
  // 其他字段...
}
```

## 作业结果（`kind:6000-6999`）

服务提供者发布作业结果，提供作业处理的输出。他们应标记原始作业请求事件 ID 以及客户的公钥。

```yaml
{
  "pubkey": "<服务提供者公钥>",
  "content": "<payload>",
  "kind": 6xxx,
  "tags": [
    ["request", "<作业请求>"],
    ["e", "<作业请求 ID>", "<中继提示>"],
    ["i", "<输入数据>"],
    ["p", "<客户公钥>"],
    ["amount", "请求支付金额", "<可选的 bolt11>"]
  ],
  // 其他字段...
}
```

* `request`：作业请求事件的 JSON 字符串化版本。
* `amount`：服务提供者请求支付的毫聪数。可选的第三个值可以是 bolt11 发票。
* `i`：请求中指定的原始输入。

### 加密输出

如果请求有加密参数，则输出也应加密并放置在 `content` 字段中。如果输出是加密的，则避免包含带有明文输入数据的 `i` 标签。添加一个 `encrypted` 标签来标记输出内容为已加密。

```yaml
{
  "pubkey": "<服务提供者公钥>",
  "content": "<加密后的载荷>",
  "kind": 6xxx,
  "tags": [
    ["request", "<作业请求>"],
    ["e", "<作业请求 ID>", "<中继提示>"],
    ["p", "<客户公钥>"],
    ["amount", "请求支付金额", "<可选的 bolt11>"],
    ["encrypted"]
  ],
  // 其他字段...
}
```

## 作业反馈

服务提供者可以向客户反馈有关作业的信息。

```yaml
{
  "kind": 7000,
  "content": "<空或载荷>",
  "tags": [
    ["status", "<状态>", "<额外信息>"],
    ["amount", "请求支付金额", "<bolt11>"],
    ["e", "<作业请求 ID>", "<中继提示>"],
    ["p", "<客户公钥>"],
  ],
  // 其他字段...
}
```

* `content`：为空或作业结果（例如用于部分结果样本）
* `amount` 标签：如[作业结果](#作业结果kind6000-6999)部分所定义。
* `status` 标签：服务提供者应说明此反馈状态所指的内容。[作业反馈状态](#作业反馈状态)定义了状态。可以作为额外参数添加更多人类可读的信息。

* 注意：如果输入参数要求输入被加密，则 `content` 字段将包含使用 `p` 标签作为密钥的加密载荷。

### 作业反馈状态

| 状态                | 描述                                                            |
| ------------------- | --------------------------------------------------------------- |
| `payment-required`  | 服务提供者要求先付款才能继续。                                   |
| `processing`        | 服务提供者正在处理作业。                                         |
| `error`             | 服务提供者无法处理作业。                                         |
| `success`           | 服务提供者已成功处理作业。                                       |
| `partial`           | 服务提供者已部分处理作业。`.content` 可能包含部分结果的样本。    |

任何作业反馈事件都可能在 `.content` 字段中包含结果，如[作业结果](#作业结果kind6000-6999)部分所述。这对于服务提供者提供已处理的部分结果样本非常有用。

## 协议流程

* 客户发布一个作业请求（例如 `kind:5000` 语音转文字）。
* 服务提供者可以提交 `kind:7000` 作业反馈事件（例如 `payment-required`、`processing`、`error` 等）。
* 完成后，服务提供者使用 `kind:6000` 作业结果事件发布作业结果。
* 在任何时候，如果有按服务提供者指示待支付的 `amount`，用户可以支付包含的 `bolt11` 或对服务提供者发送给用户的作业结果事件进行 zap。

作业反馈（`kind:7000`）和作业结果（`kind:6000-6999`）事件可能包含 `amount` 标签，这可以被解释为建议付款。服务提供者必须使用 `payment-required` 反馈事件来表明需要付款，并且在收到付款之前不会执行进一步操作。

客户可以随时支付包含的 `bolt11` 发票或对请求付款的事件进行 zap，服务提供者如果选择包含 bolt11 发票，则应同时监控这两种方式。

### 关于协议流程的说明
该流程故意设计为模棱两可，允许客户和服务提供者之间的交互具有极大的灵活性，以便服务提供者可以根据自己的决策/风险感知来建模其行为。

一些服务提供者可能会选择提交 `payment-required` 作为第一个反应，然后再发送 `processing` 或在交付结果之前；一些可能会选择提供部分结果（例如样本），然后发送 `payment-required` 以交付其余结果；还有一些服务提供者可能会选择基于 npub 的过往行为评估付款可能性，从而在请求付款之前先提供作业结果，以获得最佳用户体验。

本 NIP 不定义各个自动售货机应如何选择经营其业务。

## 取消
可以通过发布一个 `kind:5` 删除请求事件并标记原始作业请求事件来取消作业请求。

## 附录 1：作业链
客户可以请求多个作业以链式方式处理，其中一个作业的输出是另一个作业的输入（例如，播客转录 -> 转录内容的摘要）。这是通过指定另一个作业的事件 ID 作为 `job` 类型的输入来实现的。

服务提供者可以在看到前一个作业的结果时立即开始处理后续作业，但他们很可能先等待 zap 的发布。这引入了风险：作业 #1 的服务提供者可能会延迟发布 zap 事件以获取优势。此风险由服务提供者自行缓解，或由他们决定前一个作业的服务提供者的结果是否足够好，从而无需等待明确的 zap 即可假设作业已被接受。

这为服务提供者提供了更高层次的灵活性（精明的服务提供者无论如何都会利用这种灵活性）。

## 附录 2：服务提供者可发现性
服务提供者可以使用 NIP-89 公告来宣传他们对作业种类的支持：

```yaml
{
  "kind": 31990,
  "pubkey": "<pubkey>",
  "content": "{
    \"name\": \"翻译 DVM\",
    \"about\": \"我是一个专注于翻译比特币内容的 DVM。\"
  }",
  "tags": [
    ["k", "5005"], // 例如翻译
    ["t", "bitcoin"] // 例如可选地宣传其专注于比特币音频转录，不会混淆"Drivechains"和"Ridechains"
  ],
  // 其他字段...
}
```

客户可以使用 NIP-89 来查看他们的关注对象使用了哪些服务提供者。
