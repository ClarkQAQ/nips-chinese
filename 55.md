NIP-55
======

Android 签名器应用
--------------------------

`draft` `optional`

## 原理

本 NIP 描述了一种在同一设备上的 Android 签名器与任何 Nostr 客户端之间进行双向通信的方法，使得客户端无需处理用户的私钥。签名器是一个 Android 应用；客户端可以是另一个 Android 应用或一个网页。

## 术语

- **用户**: 尝试使用 Nostr 的人。
- **客户端**: 发送签名请求的面向用户的应用。
- **签名器**: 持有用户私钥并响应 _客户端_ 请求的 Android 应用。
- **用户公钥**: 代表 _用户_ 的公钥，由 `get_public_key` 返回。
- **包名**: _签名器_ 的 Android 包名（例如 `com.example.signer`），_客户端_ 用它来寻址后续请求。

本 NIP 中的所有公钥均为十六进制格式。

## 通信方式

_客户端_ 可以通过三种方式与 _签名器_ 通信：

- **Intent** — 供 Android 客户端使用。_签名器_ 会打开，_用户_ 手动批准或拒绝请求，结果返回给 _客户端_。
- **Content Resolver** — 供 Android 客户端使用。在后台运行，无需打开 _签名器_，但仅对 _用户_ 之前已选择记住的权限有效。
- **Web** — 供 Web 客户端使用。结果通过 `callbackUrl` 返回或复制到剪贴板。

## 设置

为了检测并调用 _签名器_，Android _客户端_ 必须在其 `AndroidManifest.xml` 中声明 `nostrsigner` scheme：

```xml
<queries>
  <intent>
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.BROWSABLE" />
    <data android:scheme="nostrsigner" />
  </intent>
</queries>
```

然后它可以检查是否已安装 _签名器_：

```kotlin
fun isExternalSignerInstalled(context: Context): Boolean {
  val intent = Intent(Intent.ACTION_VIEW, Uri.parse("nostrsigner:"))
  return context.packageManager.queryIntentActivities(intent, 0).isNotEmpty()
}
```

## 发起连接

1. _客户端_ 发送一个 `get_public_key` 请求。
2. _签名器_ 以 `用户公钥` 及其 `包名` 作为响应。
3. _客户端_ 存储这两者，并且在 _用户_ 保持登录状态期间不应再次调用 `get_public_key`。
4. _客户端_ 将所有后续请求寻址到该 `包名`。

_客户端_ 可以在 `get_public_key` 中传递一个 `permissions` 列表，以便 _用户_ 可以预先授权方法（供 Content Resolver 在后台应答使用）。每个权限是一个包含 `type`（方法名）以及 `sign_event` 的可选 `kind` 的对象：

```json
[{ "type": "sign_event", "kind": 22242 }, { "type": "nip44_decrypt" }]
```

## 方法

每个请求都有一个 `type`（方法名）和一个载荷。载荷根据不同传输方式以不同方式传递（见下文），但其含义是通用的：

| 方法                | 载荷            | 额外参数                    | 结果                            |
| ------------------- | --------------- | ------------------------- | ------------------------------- |
| `get_public_key`    | _（空）_        | `permissions`             | `用户公钥`                      |
| `sign_event`        | event JSON      | `current_user`            | 签名，以及已签名事件            |
| `nip04_encrypt`     | 明文            | `pubkey`, `current_user`  | 密文                            |
| `nip44_encrypt`     | 明文            | `pubkey`, `current_user`  | 密文                            |
| `nip04_decrypt`     | 密文            | `pubkey`, `current_user`  | 明文                            |
| `nip44_decrypt`     | 密文            | `pubkey`, `current_user`  | 明文                            |
| `decrypt_zap_event` | event JSON      | `current_user`            | 解密后的事件 JSON               |

- `current_user` 是当前登录 _客户端_ 的 `用户公钥`，为十六进制格式。
- `pubkey` 是用于加密/解密时对方的公钥，为十六进制格式。
- `id` 是一个可选的客户端选择的字符串，会在结果中原样返回，用于在发送多个请求而不等待时匹配响应。

## 使用 Intent

载荷是 `nostrsigner:` URI 数据；所有其他参数作为 Intent extra 传递。结果通过 `registerForActivityResult` / `rememberLauncherForActivityResult` 返回。

```kotlin
val intent = Intent(Intent.ACTION_VIEW, Uri.parse("nostrsigner:$payload")).apply {
  `package` = signerPackageName        // 仅在 get_public_key 时省略
  putExtra("type", "sign_event")
  putExtra("id", event.id)             // 可选，原样返回
  putExtra("current_user", userPubkey)
}
launcher.launch(intent)
```

结果从返回的 Intent 的 extra 中读取：

| Extra      | 描述                                                  |
| ---------- | ----------------------------------------------------- |
| `result`   | 方法结果（公钥、签名、密文等）                        |
| `id`       | 请求中发送的 `id`                                      |
| `event`    | 已签名的事件 JSON（仅 `sign_event`）                  |
| `package`  | _签名器_ 包名（仅 `get_public_key`）                  |
| `rejected` | 当 _用户_ 拒绝请求时为 `true`                         |

`resultCode` 为 `Activity.RESULT_OK` 以外的值表示 _签名器_ 失败（例如崩溃），而非 _用户_ 拒绝了请求。当 _用户_ 拒绝时，_签名器_ 返回 `RESULT_OK` 并将 `rejected` extra 设为 `true`。

```kotlin
onResult = { result ->
  if (result.resultCode != Activity.RESULT_OK) {
    // 签名器错误（例如崩溃）
  } else if (result.data?.getBooleanExtra("rejected", false) == true) {
    // 用户拒绝了请求
  } else {
    val value = result.data?.getStringExtra("result")
  }
}
```

要签署多个事件而不为每个事件重新打开 _签名器_，_客户端_ 添加以下标志，_签名器_ 返回一个结果数组。_签名器_ 必须声明 `android:launchMode="singleTop"` 才能使用此功能。

```kotlin
intent.addFlags(Intent.FLAG_ACTIVITY_SINGLE_TOP or Intent.FLAG_ACTIVITY_CLEAR_TOP)

// 签名器应答：
intent.putExtra("results", listOf(Result(package = signerPackageName, result = signature, id = intentId)).toJson())
```

## 使用 Content Resolver

_客户端_ 查询 `content://<package-name>.<TYPE>`（例如 `SIGN_EVENT`、`NIP44_ENCRYPT`）。查询的 `selectionArgs` 按 `[payload, pubkey, current_user]` 的顺序携带载荷和参数：

```kotlin
val result = context.contentResolver.query(
  Uri.parse("content://com.example.signer.SIGN_EVENT"),
  listOf(eventJson, "", loggedInUserPubkey),
  null, null, null,
)
```

游标返回一个 `result` 列（对于 `sign_event`，还会返回一个包含已签名事件 JSON 的 `event` 列）。以下情况查询返回 `null`，或存在 `rejected` 列：

- _用户_ 未对此请求启用"记住我的选择"，
- _签名器_ 中不存在该 `用户公钥`，
- 请求 `type` 不被识别，或
- _用户_ 选择始终拒绝此请求（返回 `rejected` 列——_客户端_ 不应回退到 Intent）。

```kotlin
if (result == null) return
if (result.getColumnIndex("rejected") > -1) return
if (result.moveToFirst()) {
  val value = result.getString(result.getColumnIndex("result"))
  // 对于 sign_event 还有：result.getColumnIndex("event")
}
```

客户端应将 `用户公钥` 存储在本地，并在用户登录后避免调用 `get_public_key`。

## 使用 Web 应用

Web 客户端无法从 Intent 接收结果，因此 _签名器_ 通过 `callbackUrl`（附加到 URL 上）返回结果，或者如果未提供回调 URL，则通过复制到剪贴板返回。请求是一个 `nostrsigner:` URL，其路径为载荷，查询字符串携带参数：

```
nostrsigner:<payload>?type=<method>&pubkey=<hex_pubkey>&callbackUrl=https://example.com/?result=
```

查询参数：

- `type` — 方法名。
- `pubkey` — 对方的十六进制公钥（加密/解密方法）。
- `callbackUrl` — _签名器_ 追加结果的位置。如果省略，结果被复制到剪贴板。
- `returnType` — `signature` 或 `event`。
- `compressionType` — `none`（默认）或 `gzip`。使用 `gzip` 时，返回的事件为 `"Signer1"` + Base64(gzip(event json))；这在 Intent 和 URL 存在长度限制时很有用。

```js
window.href = `nostrsigner:${eventJson}?compressionType=none&returnType=signature&type=sign_event&callbackUrl=https://example.com/?event=`;
```

> 考虑在 Web 应用中使用 [NIP-46: Nostr 远程签名](46.md)。使用此处的方法时，Web 客户端无法在后台调用 _签名器_，因此 _用户_ 每次请求都会看到一个弹出窗口。

### 示例

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Test</h1>

    <script>
        window.onload = function() {
            var params = new URL(window.location.href).searchParams;
            var result = params.get("event");
            if (result) alert(result);

            var json = { kind: 1, content: "test" };
            var encodedJson = encodeURIComponent(JSON.stringify(json));
            var anchor = document.createElement("a");
            anchor.href = `nostrsigner:${encodedJson}?compressionType=none&returnType=signature&type=sign_event&callbackUrl=https://example.com/?event=`;
            anchor.textContent = "Open External Signer";
            document.body.appendChild(anchor);
        }
    </script>
</body>
</html>
```
