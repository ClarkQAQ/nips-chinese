NIP-77
======

Negentropy 同步
------------------

`draft` `optional` `relay`

本文档描述了用于同步事件的协议扩展。它适用于客户端-中继和中继-中继两种场景。如果同步双方拥有共同的事件，则该协议比传输全部事件（甚至仅传输事件 ID）使用更少的带宽。

它是围绕 [Negentropy](https://github.com/hoytech/negentropy) 协议的 Nostr 友好封装，该协议使用一种称为[基于范围集合协调](https://logperiodic.com/rbsr.html)的技术。

由于 Negentropy 是一种二进制协议，此封装对其消息进行十六进制编码。Negentropy 协议 V1 的规范作为附录附于本 NIP 下方。

## 高层协议描述

我们将参与同步的双方称为客户端和中继（尽管发起方也可以是另一个中继而不是客户端）。

* (1) 客户端（发起方）选择一个过滤器，检索其本地拥有的与此过滤器匹配的事件集合（或使用缓存），并构造初始消息。
* (2) 客户端向中继发送一条 `NEG-OPEN` 消息，其中包含过滤器和初始消息。
* (3) 中继选择其本地拥有的与过滤器匹配的事件集合（或使用缓存）。
* (4) 中继构造响应并通过 `NEG-MSG` 消息返回给客户端。
* (5) 客户端解析消息，了解其拥有（而中继需要）的 ID 以及其需要（而中继拥有）的 ID。
  * 如果客户端希望继续，则构造新消息并通过 `NEG-MSG` 发送给中继。转到步骤 4。
  * 如果客户端希望停止，则发送 `NEG-CLOSE` 消息或断开 WebSocket 连接。

上述协议仅让客户端了解其拥有/需要的 ID，并不实际传输事件。根据这些 ID，客户端可以使用 `EVENT` 上传其拥有的事件，和/或使用 `REQ` 下载其需要的事件。这可以在同一 WebSocket 连接上与后续的 `NEG-MSG` 消息并行执行。如果客户端只对确定唯一事件的数量（即反应计数）感兴趣，则可以选择完全不下载/上传。

## Nostr 消息

### 初始消息（客户端到中继）：

```yaml
[
    "NEG-OPEN",
    <订阅 ID 字符串>,
    <过滤器>,
    <初始消息, 十六进制编码>
]
```

* 订阅 ID 用于双方标识消息所指的查询。它只需要足够长，以区别于此 WebSocket 连接上的任何其他并发订阅（每次 `NEG-OPEN` 递增一次的整数即可）。订阅 ID 与 `REQ` 订阅 ID 位于不同的命名空间中。如果对当前已打开的订阅 ID 发出了 `NEG-OPEN`，则先关闭现有订阅。
* 过滤器的描述见 [NIP-01](01.md)。
* `initialMessage` 是初始的 Negentropy 二进制消息，十六进制编码。参见附录。

### 错误消息（中继到客户端）：

如果中继无法处理请求，则向客户端返回错误：

```yaml
[
    "NEG-ERR",
    <订阅 ID 字符串>,
    <原因代码字符串>
]
```

错误原因采用与 NIP-01 相同的格式。它们应以机器可读的单词前缀开头，后跟 `:` 和包含更多信息的人类可读消息。

当前建议的错误原因有：

* `blocked`
  * 中继可以选择拒绝需要处理过多记录或记录过旧的查询
  * 可选择在响应的第四个元素中返回可处理的最大记录数
  * 示例：`blocked: this query is too big`
* `closed`
  * 由于 `NEG-OPEN` 查询可能有状态，中继可以选择对不活动查询超时以回收内存资源
  * 示例：`closed: you took too long to respond!`

发出 `NEG-ERR` 后，该订阅视为已关闭。

### 后续消息（双向）：

中继和客户端交替向对方发送 `NEG-MSG`：

```yaml
[
    "NEG-MSG",
    <订阅 ID 字符串>,
    <消息, 十六进制编码>
]
```

* `message` 是一条 Negentropy 二进制消息，十六进制编码。两个消息方向使用相同的格式。参见附录。

### 关闭消息（客户端到中继）：

完成后，客户端应通过 `NEG-CLOSE` 告知中继释放其资源：

```yaml
[
    "NEG-CLOSE",
    <订阅 ID 字符串>
]
```


## 附录：Negentropy 协议 V1

### 准备

有两个协议参与者：客户端和服务器。客户端创建初始消息并将其传输给服务器，服务器回复自己的消息。客户端继续查询服务器直到满意为止，然后终止协议。两个方向的消息具有相同的格式。

每个参与者都拥有一个记录集合。一条记录由一个 64 位数字时间戳和一个 256 位 ID 组成。每个参与者首先按时间戳升序对其条目进行排序。如果两个时间戳相等，则按 ID 的字节字典序升序排序。条目不得使用最大 uint64 值（`2**64 - 1`）作为时间戳，因为这保留为特殊的"无穷大"值。

该协议的目标是让客户端了解其拥有而服务器没有的 ID 集合，以及服务器拥有而客户端没有的条目集合。

### `Varint`

变长整数（可变大小的无符号整数）以基 128 数字表示，最高有效数字在前，尽可能使用最少的数字。除最后一个字节外，每个字节的第八位（高位）均置位。

    Varint := <Digit+128>* <Digit>

### `Id`

ID 表示为长度为 `32` 的字节串：

    Id := Byte{32}

### `Message`

协调消息由一个协议版本字节后跟一个有序的范围列表组成：

    Message := <protocolVersion (Byte)> <Range>*

当前协议版本为 1，由字节 `0x61` 表示。协议版本 2 将为 `0x62`，依此类推。如果服务器收到无法处理的协议版本的消息，它应回复一个包含其支持的最高协议版本的单个字节，允许客户端降级并重试其消息。

每个 Range 对应时间戳/ID 空间中的一个连续部分。第一个 Range 从时间戳 0 和 ID 0 字节开始。Range 始终相邻（无间隔）。如果最后一个 Range 未在特殊的无穷大值处结束，则会附加一个隐式的跳过至无穷大 Range。这意味着 Range 列表始终覆盖整个时间戳/ID 空间。

### `Range`

一个 Range 由一个上界、一个模式和一个载荷组成：

    Range := <upperBound (Bound)> <mode (Varint)> <payload (Skip | Fingerprint | IdList)>

载荷的内容由模式决定：

* 如果 `mode = 0`，则载荷为 `Skip`，表示发送方不希望进一步处理此 Range。此载荷为空：

      Skip :=

* 如果 `mode = 1`，则载荷为 `Fingerprint`，它是发送方在此 Range 内拥有的所有 ID 的[摘要](#指纹算法)：

      Fingerprint := Byte{16}

* 如果 `mode = 2`，则载荷为 `IdList`，即发送方在此 Range 内拥有的所有 ID 的可变长度列表：

      IdList := <length (Varint)> <ids (Id)>*


### `Bound`

每个 Range 由*包含*的下界和*排除*的上界指定。如上定义，每个 Range 仅包含一个上界：Range 的下界是前一个 Range 的上界，对于第一个 Range 则为时间戳 0/ID 0。

Bound 由一个编码时间戳和一个可变长度的 ID 消歧前缀组成（当多个条目具有相同时间戳时使用）：

    Bound := <encodedTimestamp (Varint)> <length (Varint)> <idPrefix (Byte)>*

* 时间戳被特殊编码。无穷大时间戳编码为 `0`。所有其他值编码为 `1 + offset`，其中 offset 是此时间戳与前一个编码时间戳之间的差值。初始 offset 从 `0` 开始，并在每条消息开始时重置。

  Offset 始终为非负值，因为上界的时间戳大于或等于下界的时间戳，消息中的 Range 始终按升序编码，且 Range 从不重叠。

* `idPrefix` 的大小由 `length` 编码，可以在 `0` 到 `32` 字节之间（含）。这允许实现使用尽可能短的前缀来分隔此 Range 的第一条记录与前一个 Range 的最后一条记录。如果这些记录的时间戳不同，则长度应为 0，否则长度应为它们共同 ID 前缀的字节长度加 1。

  如果 `idPrefix` 长度小于 `32`，则省略的尾部字节隐式为 0 字节。


### 指纹算法

Range 的指纹通过以下算法计算：

* 计算元素 ID（解释为 32 字节小端无符号整数）的模 2<sup>256</sup> 加法
* 连接 Range 中的元素数量，编码为 [Varint](#varint)
* 使用 SHA-256 进行哈希
* 取前 16 字节
