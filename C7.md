NIP-C7
======

聊天
-----

`draft` `optional`

一个聊天消息是一个 `kind 9` 事件。

```json
{
  "kind": 9,
  "content": "GM",
  "tags": []
}
```

对 `kind 9` 的回复是一个额外的 `kind 9` 事件，使用 `q` 标签引用父消息。

```json
{
  "kind": 9,
  "content": "nostr:nevent1...\nyes",
  "tags": [
    ["q", <event-id>, <relay-url>, <pubkey>]
  ]
}
```

将"聊天视图"渲染为有序事件流的客户端必须仅获取 `kind 9` 事件，以防止跨实现的上下文缺失。其他内容类型可以按照 [NIP 18](./18.md) 在 `kind 9` 中被引用。
