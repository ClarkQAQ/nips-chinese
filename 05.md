NIP-05
======

将 Nostr 密钥映射到基于 DNS 的互联网标识符
----------------------------------------------------

`final` `optional`

在 kind 为 `0`（`用户元数据`）的事件中，可以指定键 `"nip05"`，其值为一个[互联网标识符](https://datatracker.ietf.org/doc/html/rfc5322#section-3.4.1)（类似电子邮件的地址）。尽管上面链接到了一个非常宽松的"互联网标识符"规范，但 `<local-part>` 部分必须仅使用字符 `a-z0-9-_.`。

客户端收到后，将标识符拆分为 `<local-part>` 和 `<domain>`，并使用这些值向 `https://<domain>/.well-known/nostr.json?name=<local-part>` 发起 GET 请求。

结果应是一个 JSON 文档对象，包含一个键 `"names"`，该键应是一个将名称映射为十六进制格式公钥（小写）的映射。如果给定 `<name>` 的公钥与 `user metadata` 事件中的 `pubkey` 匹配，则客户端得出结论：该公钥确实可以通过其标识符引用。

### 示例

如果客户端看到如下事件：

```yaml
{
  "pubkey": "b0635d6a9851d3aed0cd6c495b282167acf761729078d975fc341b22650b07b9",
  "kind": 0,
  "content": "{\"name\": \"bob\", \"nip05\": \"bob@example.com\"}"
  // other fields...
}
```

它将向 `https://example.com/.well-known/nostr.json?name=bob` 发起 GET 请求，并收到类似如下的响应：

```json
{
  "names": {
    "bob": "b0635d6a9851d3aed0cd6c495b282167acf761729078d975fc341b22650b07b9"
  }
}
```

或带有**推荐**的 `"relays"` 属性：

```json
{
  "names": {
    "bob": "b0635d6a9851d3aed0cd6c495b282167acf761729078d975fc341b22650b07b9"
  },
  "relays": {
    "b0635d6a9851d3aed0cd6c495b282167acf761729078d975fc341b22650b07b9": [ "wss://relay.example.com", "wss://relay2.example.com" ]
  }
}
```

如果公钥与 `"names"` 中给出的公钥匹配（如上例所示），则说明关联正确，`"nip05"` 标识符有效且可以显示。

推荐的 `"relays"` 属性可以包含一个对象，以公钥为属性，以中继 URL 数组为值。当存在该属性时，可用于帮助客户端了解特定用户可能出现在哪些中继上。根据查询字符串动态提供 `/.well-known/nostr.json` 文件的 Web 服务器，在提供任何名称的数据时，应同时在相同响应中提供该名称的中继数据。

## 通过 NIP-05 标识符查找用户

客户端可以实现从_互联网标识符_查找用户公钥的支持，流程与上述相同，但顺序相反：客户端首先获取 _well-known_ URL，从中获得用户的公钥，然后尝试获取该用户的 kind `0` 事件，并检查该事件是否包含匹配的 `"nip05"`。

## 说明

### 身份识别，而非验证

NIP-05 的目的不是_验证_用户，而仅仅是_识别_他们，以便于交换联系人信息或搜索用户。
例外情况是那些拥有（如公司）或关联（如项目）知名域名的人，他们可以利用 NIP-05 作为自身与该域名关系的证明，进而证明与背后组织的关系，从而获得一定的信任度。

### 用户发现实现建议

客户端可以利用此功能让用户搜索其他个人资料。如果客户端有搜索框或类似功能，用户可以输入"bob@example.com"，客户端将识别该输入并执行适当的查询以获取公钥，然后将其推荐给用户。

### 客户端必须始终关注公钥，而非 NIP-05 地址

例如，如果发现 `bob@bob.com` 的公钥为 `abc...def`，用户点击按钮关注该个人资料后，客户端必须将 `abc...def` 作为主要引用，而不是 `bob@bob.com`。如果将来某个时候，地址 `https://bob.com/.well-known/nostr.json?name=bob` 开始返回公钥 `1d2...e3f`，客户端不得替换用户已关注列表中 `abc...def`（但应停止为该用户显示"bob@bob.com"，因为该地址已成为无效的 `"nip05"` 属性）。

### 公钥必须使用十六进制格式

密钥必须以十六进制格式返回，小写形式。NIP-19 `npub` 格式的密钥仅用于客户端 UI 中的显示，不用于本 NIP。

### 仅显示域名作为标识符

客户端可以将标识符 `_@domain` 视为"根"标识符，并选择仅显示为 `<domain>`。例如，如果 Bob 拥有 `bob.com`，他可能不想要 `bob@bob.com` 这样的标识符，因为这是冗余的。相反，Bob 可以使用标识符 `_@bob.com`，并期望 Nostr 客户端在所有用途中将其显示和视为 `bob.com`。

### `/.well-known/nostr.json?name=<local-part>` 格式的设计理由

通过将 `<local-part>` 作为查询字符串而非路径的一部分添加，该协议可以同时支持按需生成 JSON 的动态服务器和包含可容纳多个名称的 JSON 文件的静态服务器。

### 允许 JavaScript 应用访问

JavaScript Nostr 应用可能受到浏览器 [CORS][] 策略的限制，无法访问用户域名上的 `/.well-known/nostr.json`。当 CORS 阻止 JS 加载资源时，JS 程序会将其视为与资源不存在相同的网络故障，因此纯 JS 应用无法向用户明确告知故障是由 CORS 问题引起的。在请求 `/.well-known/nostr.json` 文件时遇到网络故障的 JS Nostr 应用，可以建议用户检查其服务器的 CORS 策略，例如：

```bash
$ curl -sI https://example.com/.well-known/nostr.json?name=bob | grep -i ^Access-Control
Access-Control-Allow-Origin: *
```

用户应确保其 `/.well-known/nostr.json` 使用 HTTP 标头 `Access-Control-Allow-Origin: *` 提供，以确保它可以被现代浏览器中运行的纯 JS 应用验证。

[CORS]: https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS

### 安全约束

`/.well-known/nostr.json` 端点不得返回任何 HTTP 重定向。

请求方必须忽略 `/.well-known/nostr.json` 端点返回的任何 HTTP 重定向。
