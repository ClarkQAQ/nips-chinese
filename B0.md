NIP-B0
======

网页书签
---------------

`draft` `optional`

本 NIP 定义了 `kind:39701`，用于将 URI 作为可编辑的网页书签，并使用 HTTP 方案。

### 格式

该格式使用 `kind:39701`。

`.content` 应为网页书签的详细描述。它可以是空字符串。

`d` 标签就是它们的 URL（不含方案），始终且处处默认为 `https://` 或 `http://`。

通过这种方式，客户端可以通过 `d` 标签查询网页书签事件。

### 元数据

可以根据需要在事件中添加元数据字段作为标签。

* `"t"`，用于"标签"/"话题标签"（即事件可能相关的主题）
* `"published_at"`，用于书签首次发布时的 Unix 秒时间戳（字符串形式）
* `"title"`，书签的标题，可用作 HTML 链接元素的属性

## 示例事件

```yaml
{
  "kind": 39701,
  "id": "d7a92714f81d0f712e715556aee69ea6da6bfb287e6baf794a095d301d603ec7",
  "pubkey": "2729620da105979b22acfdfe9585274a78c282869b493abfa4120d3af2061298",
  "created_at": 1738869705,
  "tags": [
    // Required tags
    ["d", "alice.blog/post"],
    // Optional tags    
    ["published_at", "1738863000"],
    ["title", "Blog insights by Alice"],  
    ["t", "post"],
    ["t", "insight"]
  ],
  "content": "A marvelous insight by Alice about the nature of blogs and posts.",
  "sig": "36d34e6448fe0223e9999361c39c492a208bc423d2fcdfc2a3404e04df7c22dc65bbbd62dbe8a4373c62e4d29aac285b5aa4bb9b4b8053bd6207a8b45fbd0c98"
}
```

### 回复与评论

对 `kind 39701` 的回复必须使用 `kind 1111` 事件作为评论，遵循 [NIP-22](22.md)。
