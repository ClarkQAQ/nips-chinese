> __警告__ `不推荐`: 已弃用，推荐使用 [NIP-B7](B7.md)

NIP-96
======

HTTP 文件存储集成
-----------------------------

`草案` `不推荐` `可选`

## 介绍

本 NIP 定义了一个用于 HTTP 文件存储服务器的 REST API，旨在与 nostr 网络结合使用。
该 API 将使 nostr 用户能够上传文件，之后在 nostr 笔记中通过 URL 引用这些文件。

该规范不通过 WebSocket 使用常规 nostr 事件进行
存储、请求或检索数据，因为为简单起见，服务器
不需要了解任何 nostr 中继的信息。

## 服务器适配

希望被 nostr 用户访问的文件存储服务器应选择加入，通过在 `/.well-known/nostr/nip96.json` 提供 https 路由，其中包含 `api_url`：

```yaml
{
  // 必需
  // 文件上传和删除从此 url 提供
  // 如果 "download_url" 字段不存在或为空字符串，则下载也由此提供
  "api_url": "https://your-file-server.example/custom-api-path",
  // 可选
  // 如果不存在，则下载从 api_url 提供
  "download_url": "https://a-cdn.example/a-path",
  // 可选
  // 注意：此字段并非供 HTTP 服务器设置。
  // 如果你是 nostr 中继，使用你的 /.well-known/nostr/nip96.json
  // 仅用于重定向到其他某人的 HTTP 文件存储服务器的 /.well-known/nostr/nip96.json
  // 在这种情况下，"api_url" 字段必须为空字符串
  "delegated_to_url": "https://your-file-server.example",
  // 可选
  "supported_nips": [60],
  // 可选
  "tos_url": "https://your-file-server.example/terms-of-service",
  // 可选
  "content_types": ["image/jpeg", "video/webm", "audio/*"],
  // 可选
  "plans": {
    // "free" 是唯一标准化的计划键
    // 客户端可以通过其存在来了解服务器是否提供免费存储
    "free": {
      "name": "免费套餐",
      // 默认为 true
      // 所有计划必须支持 NIP-98 上传
      // 但某些计划也可能允许无需验证的上传
      "is_nip98_required": true,
      "url": "https://...", // 计划的登录页面（如果有）
      "max_byte_size": 10485760,
      // 以天为单位的保留范围 / 0 表示无过期
      // [7, 0] 表示可以从 7 天到无限期保留
      // [0, 0] 表示永不过期
      // 提前过期可能是因为低流量或其他因素
      "file_expiration": [14, 90],
      "media_transformations": {
        "image": [
          "resizing"
        ]
      }
    }
  }
}
```

### 中继提示

注意：本节并非供 HTTP 服务器使用。

一个 nostr 中继可以通过
添加一个 `/.well-known/nostr/nip96.json` 文件，其中包含 "delegated_to_url" 字段
指向托管其自身 `/.well-known/nostr/nip96.json` 的 HTTP 文件存储服务器
的 URL，来重定向到其他人的 HTTP 文件存储服务器。在这种情况下，"api_url" 字段必须
为空字符串，并且所有其他字段必须不存在。

如果 nostr 中继本身就是 HTTP 文件存储服务器，
则必须改用 "api_url" 字段。

### 支持的文件存储服务器列表

参见 https://github.com/aljazceru/awesome-nostr#nip-96-file-storage-servers。

## 认证

当指示需要时，客户端必须添加一个 [NIP-98](98.md) `Authorization` 头（**可选地**将编码后的 `payload` 标签设置为文件的 base64 编码的 256 位 SHA-256 哈希值——不是整个请求体的哈希值）。

## 上传

`POST $api_url` 格式为 `multipart/form-data`。

**需要 AUTH**

表单字段列表：

- `file`：**必需** 要上传的文件
- `caption`：**推荐** 宽松的描述；
- `expiration`：UNIX 时间戳（秒）。如果文件应永久存储则为空字符串。服务器不需要遵循此设置。
- `size`：文件字节大小。这只是一个值，服务器可以用它在文件大小超过服务器限制时提前拒绝。
- `alt`：**推荐** 面向视障用户的严格描述文本。
- `media_type`："avatar" 或 "banner"。告知服务器该文件将用作头像或横幅。如果不存在，服务器将将其解释为普通上传，不作特殊处理。
- `content_type`：MIME 类型，如 "image/jpeg"。这只是一个值，服务器可以用它在 MIME 类型不受支持时提前拒绝。
- `no_transform`："true" 要求服务器不要对文件进行转换，并按原样提供上传的文件，可能被拒绝。

其他自定义表单数据字段可能取决于特定 `服务器` 的支持。
`服务器` 不需要存储 `客户端` 发送的任何元数据。

文件中嵌入的 `filename` 可能不被 `服务器` 采用，服务器可能在内部仅存储 SHA-256 哈希值作为文件名，忽略额外的元数据。
哈希足以唯一标识文件，这就是为什么它将在 `下载` 和 `删除` 路由中使用。

`服务器` 必须将用户的 `pubkey` 字符串链接为文件的所有者，以便之后允许他们删除该文件。

`no_transform` 可用于将文件复制到多个服务器以实现冗余，客户端可以使用[服务器列表](#选择服务器)来查找可能包含相同文件的其他服务器。当上传文件并请求 `no_transform` 时，客户端应检查响应中的哈希值是否匹配，以检测文件是否被修改过。

### 响应码

- `200 OK`：文件上传已存在，但上传成功（已有的哈希）
- `201 Created`：文件上传成功（新的哈希）
- `202 Accepted`：文件上传正在等待处理，参见[延迟处理](#延迟处理)部分
- `413 Payload Too Large`：文件大小超出限制
- `400 Bad Request`：表单数据无效或不受支持
- `403 Forbidden`：用户不被允许上传，或上传的文件哈希与 `Authorization` 头 `payload` 标签中包含的哈希不匹配
- `402 Payment Required`：服务器要求付款，**此流程未定义**

上传响应是一个 JSON 对象，如下所示：

```yaml
{
  // "success" 表示成功，或 "error" 表示失败
  "status": "success",
  // 成功、失败或信息的自由文本消息
  "message": "上传成功。",
  // 可选。参见"延迟处理"部分
  "processing_url": "...",
  // 这使用 NIP-94 事件格式，但不需要
  // 填写 "id"、"pubkey"、"created_at" 和 "sig" 等字段
  //
  // 这包含下载 url（"url"）、
  // 原始文件哈希（"ox"）
  // 以及可选的服务器希望提供的所有文件元数据
  //
  // 如果上传失败，nip94_event 字段不存在
  "nip94_event": {
    // 必需标签："url" 和 "ox"
    "tags": [
      // 可以与 /.well-known/nostr/nip96.json 的 "download_url" 字段相同
      //（如果 "download_url" 不存在或为空，则为 "api_url" 字段）加上追加的
      // 原始文件哈希。
      //
      // 注意我们向 `ox` 值追加了 .png 文件扩展名
      //（这是可选的，但强烈建议添加扩展名，因为它将帮助 nostr 客户端
      // 通过正则表达式检测文件类型）
      //
      // 也可以是用于下载文件的任何 url
      //（无论是否使用 /.well-known/nostr/nip96.json 的 "download_url" 前缀），
      // 例如用于负载均衡目的。
      ["url", "https://your-file-server.example/custom-api-path/719171db19525d9d08dd69cb716a18158a249b7b3b3ec4bbdec5698dca104b7b.png"],
      // 原始文件的 SHA-256 哈希，在服务器转换之前。
      // 服务器必须存储它，即使它代表的是原始文件，因为
      // 用户可能尝试使用此值下载/删除转换后的文件
      ["ox", "719171db19525d9d08dd69cb716a18158a249b7b3b3ec4bbdec5698dca104b7b"],
      // 可选。经过任何服务器转换后保存的文件的 SHA-256 哈希。
      // 服务器可以但不需要存储此值。
      ["x", "543244319525d9d08dd69cb716a18158a249b7b3b3ec4bbde5435543acb34443"],
      // 可选。推荐用于帮助客户端在下载前轻松了解文件类型。
      ["m", "image/png"],
      // 可选。推荐用于帮助客户端在显示文件前预留足够的 UI 空间。
      ["dim", "800x600"]
      // ... 其他可选的 NIP-94 标签
    ],
    "content": ""
  },
  // ... 其他自定义字段（请考虑将它们添加到此 NIP 或 NIP-94 标签中）
}
```

请注意，如果服务器没有对接收到的文件进行任何转换，则 `nip94_event.tags.*.ox` 和 `nip94_event.tags.*.x` 字段将具有相同的值。服务器必须将保存的文件链接到**原始**文件的 SHA-256 哈希（在任何服务器转换之前）（即 `nip94_event.tags.*.ox` 标签值）。**原始**文件的 SHA-256 哈希将用于在下载或删除时标识保存的文件。

`客户端` 可以将同一文件上传到一个或多个 `服务器`。
上传成功后，`客户端` 可以选择性地生成并向任何 nostr `中继` 发送一个 [NIP-94](94.md) 事件，补全缺失的字段。

或者，`客户端` 可以在 nostr 笔记中分享或嵌入上述 URL，而不是使用 NIP-94。

`客户端` 也可以使用 `nip94_event` 中的标签来构建 `imeta` 标签。

### 延迟处理

有时服务器可能希望将上传的文件放入处理队列中进行延迟文件处理。

在这种情况下，服务器在处理未完成时应提供原始文件，然后在处理完成后将原始文件替换为处理后的文件。上传响应与通常相同，但某些可选的元数据如 `nip94_event.tags.*.x` 和 `nip94_event.tags.*.size` 将不可用。

预期已知的元数据结果应在响应中返回。
例如，如果文件处理会将文件从 "jpg" 更改为 "webp"，
请使用 ".webp" 扩展名作为 `nip94_event.tags.*.url` 字段值，并将 "image/webp" 设置为 `nip94_event.tags.*.m` 字段。
如果某些元数据在处理完成前未知，请从响应中省略它们。

上传响应可能包含一个 `processing_url` 字段，告知客户端可用于检查文件处理是否完成的临时 URL。

如果处理尚未完成，服务器应在 `processing_url` URL 处以 **200 OK** 响应以下 JSON：

```yaml
{
  // 应为 "processing"。如果是 "error"，则表示处理失败。
  "status": "processing",
  "message": "处理中。请稍后再检查更新状态。",
  "percentage": 15 // 处理百分比。0 到 100 之间的整数。
}
```

当处理完成时，服务器以 **201 Created** 状态和之前提到的常规成功 JSON 响应（现在**不包含** `processing_url` 字段），可能包含在处理之前不可用的可选元数据，位于 `nip94_event.tags.*` 字段中。

### 文件压缩

服务器可以应用文件压缩和其他转换，如元数据剥离。
然而，对于所有文件操作，如下载和删除，**原始**文件的 SHA-256 哈希是在 URL 字符串中标识文件的依据。

## 下载

`GET $api_url/<sha256-hash>(.ext)`

上传响应字段 `nip94_event.tags.*.url` 中告知的主要文件下载 URL
可以是该格式，也可以不是（可以是服务器想要的任何非标准 URL）。
如果不是，服务器仍然必须响应上一个段落中提到的标准 URL 的下载请求，
以便客户端只需知道 SHA-256 文件哈希就可以在任何兼容 NIP-96 的服务器上尝试下载文件。

请注意，"<sha256-hash>" 部分来自**原始**文件，**不是**来自**转换后**的文件（如果上传的文件经过任何服务器转换）。

`服务器` 需要支持 ".ext"（即"文件扩展名"）。对于`客户端`，建议（尽管可选）将其附加到路径中。
当存在时，`服务器` 可以用它来知道发送哪个 `Content-Type` 头（例如："Content-Type": "image/png" 对应 ".png" 扩展名）。
文件扩展名可能不存在，因为哈希是唯一标识文件所需的唯一定义。

示例：`$api_url/719171db19525d9d08dd69cb716a18158a249b7b3b3ec4bbdec5698dca104b7b.png`

### 媒体转换

`服务器` 可以响应一些媒体转换查询参数，并忽略它们不支持的参数，直接提供未经转换的原始媒体文件。

#### 图片转换

##### 调整大小

上传时，`服务器` 可以创建调整大小后的图片变体，如缩略图，保持原始宽高比。
`客户端` 可以使用 `w` 查询参数请求所需像素宽度的图片版本。
`服务器` 可以提供与参数值最接近的宽度变体，或即时生成的图片变体。

示例：`$api_url/<sha256-hash>.png?w=32`

## 删除

`DELETE $api_url/<sha256-hash>(.ext)`

**需要 AUTH**

请注意，`/<sha256-hash>` 部分来自**原始**文件，**不是**来自**转换后**的文件（如果上传的文件经过任何服务器转换）。

扩展名是可选的，因为文件哈希是唯一需要的文件标识。

`服务器` 应使用适当的 HTTP 响应码（403 Forbidden）拒绝非原始上传者的删除请求。

需要注意的是，可能有多个用户上传了同一文件（具有相同的哈希）。在这种情况下，删除不应真正删除文件，而只是从文件所有者列表中移除该用户的 `pubkey`（假设服务器只保留同一文件的一份副本，因为同一文件的多次上传会产生相同的文件哈希）。

成功响应是 200 OK，仅包含基本的 JSON 字段：

```json
{
  "status": "success",
  "message": "文件已删除。"
}
```

## 列出文件

`GET $api_url?page=x&count=y`

**需要 AUTH**

返回与已认证用户公钥关联的文件列表。

示例响应：

```yaml
{
  "count": 1, // 服务器页面大小，例如 max(1, min(server_max_page_size, arg_count))
  "total": 1, // 文件总数
  "page": 0,  // 当前页码
  "files": [
    {
      "tags": [
        ["ox", "719171db19525d9d08dd69cb716a18158a249b7b3b3ec4bbdec5698dca104b7b"],
        ["x", "5d2899290e0e69bcd809949ee516a4a1597205390878f780c098707a7f18e3df"],
        ["size", "123456"],
        ["alt", "一个让你开怀大笑的表情包"],
        ["expiration", "1715691139"],
        // ... 其他元数据
      ],
      "content": "哈哈有趣的表情包", // 标题
      "created_at": 1715691130 // 上传时间戳
    }
  ]
}
```

`files` 包含一个 NIP-94 事件数组。

### 查询参数

- `page` 页码（`offset=page*count`）
- `count` 每页条目数

## 选择服务器

注意：HTTP 文件存储服务器开发者可以跳过本节。本节针对客户端开发者。

文件服务器偏好事件是一个 kind 10096 的可替换事件，用于选择用户希望上传文件的一个或多个服务器。服务器以 `server` 标签形式列出：

```yaml
{
  "kind": 10096,
  "content": "",
  "tags": [
    ["server", "https://file.server.one"],
    ["server", "https://file.server.two"]
  ],
  // 其他字段...
}
```
