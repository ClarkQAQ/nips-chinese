# NIP-46

Nostr 远程签名
--------------------

## 变更

引入了 `remote-signer-key`，在 bunker URL 中传递，客户端必须区分 `remote-signer-pubkey` 和 `user-pubkey`，连接后必须调用 `get_public_key`，移除了 nip05 登录，create_account 移到了另一个 NIP。

## 设计原理

私钥应尽可能少地暴露给系统——应用、操作系统、设备——因为每个系统都会增加攻击面。

本 NIP 描述了一种远程签名器与 Nostr 客户端之间进行双向通信的方法。远程签名器可以是例如专门用于签名 Nostr 事件的硬件设备，而客户端则是一个普通的 Nostr 客户端。

## 术语

- **用户（user）**：试图使用 Nostr 的人。
- **客户端（client）**：用户正在查看并在其中点击按钮的面向用户的应用程序。该应用程序将向远程签名器发送请求。
- **远程签名器（remote-signer）**：运行在某处的守护进程或服务器，用于响应来自客户端的请求，也称为 "bunker"。
- **客户端密钥对/公钥（client-keypair/pubkey）**：由客户端生成的密钥。用于加密内容并与远程签名器通信。
- **远程签名器密钥对/公钥（remote-signer-keypair/pubkey）**：由远程签名器用于加密内容并与客户端通信的密钥。此密钥对可以与用户密钥对相同，但不一定必须相同。
- **用户密钥对/公钥（user-keypair/pubkey）**：代表用户的实际密钥（例如，将用于签名事件以响应 `sign_event` 请求）。远程签名器通常控制这些密钥。

本 NIP 中指定的所有公钥均采用十六进制格式。

## 概述

1. 客户端生成 `client-keypair`。此密钥对无需告知用户，因为它基本上是一次性的。客户端可以选择将其存储在本地，并应在注销时删除；
2. 建立连接（见下文），远程签名器获知 `client-pubkey`，客户端获知 `remote-signer-pubkey`。
3. 客户端使用 `client-keypair` 通过 `p` 标签标记并加密到 `remote-signer-pubkey` 来向远程签名器发送请求；
4. 远程签名器通过 `p` 标签标记并加密到 `client-pubkey` 来响应客户端。
5. 客户端请求 `get_public_key` 以获知 `user-pubkey`。

## 发起连接

有两种发起连接的方式：

### 由远程签名器发起的直接连接

远程签名器提供如下形式的连接令牌：

```
bunker://<remote-signer-pubkey>?relay=<wss://relay-to-connect-on>&relay=<wss://another-relay-to-connect-on>&secret=<optional-secret-value>
```

用户将此令牌传递给客户端，客户端随后通过指定的中继向远程签名器发送 `connect` 请求。以此方式连接时，客户端应在 `connect` 请求中包含 `optional_client_metadata`（参见[客户端元数据](#客户端元数据)），因为与 `nostrconnect://` 不同，此令牌不携带任何标识客户端的信息。可选 secret 仅可用于单次成功建立的连接，远程签名器应忽略使用旧 secret 建立新连接的尝试。

### 由客户端发起的直接连接

客户端使用 `nostrconnect://` 作为协议，以 `client-pubkey` 作为来源提供连接令牌。附加信息应作为查询参数传递：

- `relay`（必需）—— 一个或多个中继 URL，客户端在这些中继上监听来自远程签名器的响应。
- `secret`（必需）—— 一个短随机字符串，远程签名器应在其响应的 `result` 字段中返回该字符串。
- `perms`（可选）—— 客户端请求远程签名器批准的权限列表，以逗号分隔。
- `name`（可选）—— 客户端应用的名称。
- `url`（可选）—— 客户端应用的规范 URL。
- `image`（可选）—— 代表客户端应用的小图片。

示例：

```
nostrconnect://83f3b2ae6aa368e8275397b9c26cf550101d63ebaab900d19dd4a4429f5ad8f5?relay=wss%3A%2F%2Frelay1.example.com&perms=nip44_encrypt%2Cnip44_decrypt%2Csign_event%3A13%2Csign_event%3A14%2Csign_event%3A1059&name=My+Client&secret=0s8j2djs&relay=wss%3A%2F%2Frelay2.example2.com
```

用户将此令牌传递给远程签名器，远程签名器随后通过指定的中继向 `client-pubkey` 发送 `connect` *响应*事件。客户端从连接响应作者处发现 `remote-signer-pubkey`。必须提供 `secret` 值以避免连接欺骗，客户端必须验证 `connect` 响应返回的 `secret`。

## 请求事件 `kind: 24133`

```yaml
{
    "kind": 24133,
    "pubkey": <local_keypair_pubkey>,
    "content": <nip44(<request>)>,
    "tags": [["p", <remote-signer-pubkey>]],
}
```

`content` 字段是一个类似 JSON-RPC 的消息，经过 [NIP-44](44.md) 加密，结构如下：

```yaml
{
    "id": <random_string>,
    "method": <method_name>,
    "params": [array_of_strings]
}
```

- `id` 是一个随机字符串，用作请求 ID。此 ID 将包含在响应负载中返回。
- `method` 是方法/命令的名称（详见下文）。
- `params` 是字符串参数的位置数组。

### 方法/命令

以下是客户端发送给远程签名器的方法。

| 命令                     | 参数                                                                          | 结果                                                                 |
| ------------------------ | -------------------------------------------------                             | ---------------------------------------------------------------------- |
| `connect`                | `[<remote-signer-pubkey>, <optional_secret>, <optional_requested_perms>, <optional_client_metadata>]` | `"ack"` 或 `<required-secret-value>`                                   |
| `sign_event`             | `[<{kind, content, tags, created_at}>]`                                       | `json_stringified(<signed_event>)`                                     |
| `ping`                   | `[]`                                                                          | `"pong"`                                                               |
| `get_public_key`         | `[]`                                                                          | `<user-pubkey>`                                                        |
| `nip04_encrypt`          | `[<third_party_pubkey>, <plaintext_to_encrypt>]`                              | `<nip04_ciphertext>`                                                   |
| `nip04_decrypt`          | `[<third_party_pubkey>, <nip04_ciphertext_to_decrypt>]`                       | `<plaintext>`                                                          |
| `nip44_encrypt`          | `[<third_party_pubkey>, <plaintext_to_encrypt>]`                              | `<nip44_ciphertext>`                                                   |
| `nip44_decrypt`          | `[<third_party_pubkey>, <nip44_ciphertext_to_decrypt>]`                       | `<plaintext>`                                                          |
| `switch_relays`          | `[]`                                                                          | `["<relay-url>", "<relay-url>", ...]` 或 `null`                        |
| `logout`                 | `[]`                                                                          | `"ack"`                                                                |

### 请求的权限

`connect` 方法可提供 `optional_requested_perms` 以方便用户。权限是一个以逗号分隔的 `method[:params]` 列表，例如 `nip44_encrypt,sign_event:4` 表示允许调用 `nip44_encrypt` 以及使用 `kind:4` 调用 `sign_event`。`sign_event` 的可选参数是 kind 编号，其他方法的参数待后续定义。`nostrconnect://` 字符串中 `metadata` 的 `perms` 字段也可使用相同的权限格式。

### 客户端元数据

`connect` 方法可提供 `optional_client_metadata`，以便远程签名器识别和标记连接。它是一个 JSON 字符串化的对象，其字段与 `nostrconnect://` 查询参数对应，均为可选：

- `name` —— 客户端应用的名称
- `url` —— 客户端应用的规范 URL
- `image` —— 代表客户端应用的小图片

这对于由远程签名器发起的连接（`bunker://` 流程）最为有用，因为与 `nostrconnect://` 不同，远程签名器没有其他来源获取此信息。要在不请求权限的情况下发送元数据，必须为 `optional_requested_perms` 传递空字符串，以使元数据占据第四个位置。元数据由客户端提供且未经认证；远程签名器应仅将其视为显示提示，不得用于授权决策。

### 切换中继

远程签名器应始终控制其与客户端之间连接所使用的中继。因此，它应能随着时间的推移不断更新其中继集合，以应对旧中继停运和新中继出现的情况。更重要的是，在客户端发起的连接情况下，客户端可能选择完全不符合远程签名器偏好的中继，因此必须能够立即切换这些中继。

因此，兼容的客户端应在建立连接后立即（始终发送，或以合理的间隔）发送 `switch_relays` 请求。收到此类请求后，远程签名器应回复其更新的中继列表，若无变化则回复 `null`。客户端在收到更新的中继列表后应立即更新其本地状态，并在新的中继上发送后续请求。之后远程签名器可按需断开与之前中继的连接。

### 结束会话

当用户在客户端注销时，客户端可发送 `logout` 请求，告知远程签名器该会话不再需要。收到此类请求后，远程签名器应移除与请求 `client-pubkey` 关联的会话，并拒绝来自该客户端的任何后续请求（需要重新 `connect` 才能建立新会话）。远程签名器应在移除会话之前回复 `"ack"`。

`logout` 请求是一个善意的提示，并非安全边界：远程签名器绝不能依赖收到该请求，客户端在注销时无论是否收到确认，仍必须删除本地存储的 `client-keypair`。

## 响应事件 `kind:24133`

```json
{
    "id": <id>,
    "kind": 24133,
    "pubkey": <remote-signer-pubkey>,
    "content": <nip44(<response>)>,
    "tags": [["p", <client-pubkey>]],
    "created_at": <unix timestamp in seconds>
}
```

`content` 字段是一个类似 JSON-RPC 的消息，经过 [NIP-44](44.md) 加密，结构如下：

```json
{
    "id": <request_id>,
    "result": <results_string>,
    "error": <optional_error_string>
}
```

- `id` 是该响应所对应的请求 ID。
- `result` 是调用结果的字符串（可以是字符串或 JSON 字符串化的对象）。
- `error`，可选项，如果有错误则以字符串形式表示。其存在表示请求出错。

对于未知或不支持的方法的请求，必须回复错误。

## 签名事件的示例流程

- `remote-signer-pubkey` 为 `fa984bd7dbb282f07e16e7ae87b26a2a7b9b90b7246a44771f0cf5ae58018f52`
- `user-pubkey` 也是 `fa984bd7dbb282f07e16e7ae87b26a2a7b9b90b7246a44771f0cf5ae58018f52`
- `client-pubkey` 为 `eff37350d839ce3707332348af4549a96051bd695d3223af4aabce4993531d86`

### 签名请求

```yaml
{
    "kind": 24133,
    "pubkey": "eff37350d839ce3707332348af4549a96051bd695d3223af4aabce4993531d86",
    "content": nip44({
        "id": <random_string>,
        "method": "sign_event",
        "params": [json_stringified(<{
            content: "Hello, I'm signing remotely",
            kind: 1,
            tags: [],
            created_at: 1714078911
        }>)]
    }),
    "tags": [["p", "fa984bd7dbb282f07e16e7ae87b26a2a7b9b90b7246a44771f0cf5ae58018f52"]], // p-tags the remote-signer-pubkey
}
```

### 响应事件

```yaml
{
    "kind": 24133,
    "pubkey": "fa984bd7dbb282f07e16e7ae87b26a2a7b9b90b7246a44771f0cf5ae58018f52",
    "content": nip44({
        "id": <random_string>,
        "result": json_stringified(<signed-event>)
    }),
    "tags": [["p", "eff37350d839ce3707332348af4549a96051bd695d3223af4aabce4993531d86"]], // p-tags the client-pubkey
}
```

### 示意图

![signing-example](https://i.nostr.build/P3gW.png)


## 认证挑战

认证挑战是远程签名器在需要用户通过其他方式认证时可以发送回的响应。响应 `content` 对象将采用以下形式：

```json
{
    "id": <request_id>,
    "result": "auth_url",
    "error": <URL_to_display_to_end_user>
}
```

客户端应（在弹窗或新标签页中）显示 `error` 字段中的 URL，然后订阅/监听来自远程签名器的另一个响应（使用相同的请求 ID）。该事件将在用户在另一个窗口中认证后发送（如果用户未认证则永远不会到达）。

### 带认证挑战的事件签名请求示例

![signing-example-with-auth-challenge](https://i.nostr.build/W3aj.png)

## 附录

### 发布远程签名器元数据

远程签名器可以通过 [NIP-05](05.md) 和 [NIP-89](89.md) 发布其元数据。使用 NIP-05，对 `<remote-signer>/.well-known/nostr.json?name=_` 的请求可返回：

```yaml
{
    "names":{
        "_": <remote-signer-app-pubkey>,
    },
    "nip46": {
        "relays": ["wss://relay1","wss://relay2"...],
        "nostrconnect_url": "https://remote-signer-domain.example/<nostrconnect>"
    }
}
```

`<remote-signer-app-pubkey>` 可用于验证远程签名器 NIP-89 事件中的域名（见下文）。`relays` 应用于为特定远程签名器构造更精确的 `nostrconnect://` 字符串。`nostrconnect_url` 模板可用于通过将 `<nostrconnect>` 占位符替换为实际的 `nostrconnect://` 字符串，将用户重定向到远程签名器的连接流程。

### 通过 NIP-89 发现远程签名器

远程签名器可以发布带有 `k` 标签为 `24133` 的 NIP-89 `kind: 31990` 事件，该事件还可以包含一个或多个 `relay` 标签，并可包含 `nostrconnect_url` 标签。`relay` 和 `nostrconnect_url` 标签的语义与上一节相同。

客户端可以通过使用远程签名器的 `kind: 31990` 事件来改善 UX，从而发现远程签名器。客户端可以预生成远程签名器的 `nostrconnect://` 字符串，并且在这种情况下应验证 `kind: 31990` 事件的作者是否在签名器的 `nostr.json?name=_` 文件中被提及为 `<remote-signer-app-pubkey>`。