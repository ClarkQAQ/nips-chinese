NIP-57
======

闪电打赏
--------------

`draft` `optional`

本 NIP 定义了两种新的事件类型，用于记录用户之间的闪电支付。`9734` 是 `打赏请求`，代表付款方向收款方闪电钱包请求发票。`9735` 是 `打赏收据`，代表收款方闪电钱包确认响应 `打赏请求` 而发出的发票已被支付。

在 nostr 上拥有闪电收据，允许客户端显示网络上实体的闪电支付。这些可以用于娱乐或作为垃圾邮件威慑。

## 协议流程

1. 客户端从被打赏的事件上的 `zap` 标签计算收款方的 lnurl 支付请求 URL（参见附录 G），或者根据其个人资料上的 lud16 字段按照 [lnurl 规范](https://github.com/lnurl/luds) 进行解码。客户端必须向此 URL 发送 GET 请求并解析响应。如果存在 `allowsNostr` 且为 `true`，并且存在 `nostrPubkey` 且是有效的十六进制 BIP 340 公钥，客户端应将此信息与用户的 `callback`、`minSendable` 和 `maxSendable` 值一起关联。
2. 客户端可以选择在每个帖子或用户个人资料上显示闪电打赏按钮。如果用户的 lnurl 支付请求端点支持 nostr，客户端应使用本 NIP 请求 `打赏收据`，而不是普通的 lnurl 发票。
3. 当用户（"发送者"）表示想要打赏另一个用户（"接收者"）时，客户端应按照本 NIP 附录 A 的描述创建 `打赏请求` 事件并签名。
4. 不发布 `打赏请求`，而是将 `9734` 事件通过 GET 请求发送到从接收者的 lnurl 支付端点获得的 `callback` URL。详见附录 B 及示例。
5. 接收者的 lnurl 服务器将收到此 `打赏请求` 并进行验证。关于如何正确配置 lnurl 服务器以支持打赏的详细说明见附录 C，关于如何验证 `nostr` 查询参数的详细说明见附录 D。
6. 如果 `打赏请求` 有效，服务器应获取一个描述哈希发票，其中描述就是此 `打赏请求` 笔记，且仅此笔记。不包含额外的 lnurl 元数据。这将根据 [LUD06](https://github.com/lnurl/luds/blob/luds/06.md) 在响应中返回。
7. 收到发票后，客户端可以支付它，或将其传递给能够支付发票的应用。
8. 发票支付后，接收者的 lnurl 服务器必须按照附录 E 的描述生成 `打赏收据`，并将其发布到 `打赏请求` 中指定的 `relays`。
9. 客户端可以获取帖子和个人资料上的 `打赏收据`，但必须按照附录 F 的描述验证其有效性。如果 `打赏请求` 笔记包含非空的 `content`，它可以显示打赏评论。通常客户端应向用户展示 `打赏请求` 笔记，并使用 `打赏收据` 来显示"打赏由 ... 授权"，但这是可选的。

## 参考和示例

### 附录 A：打赏请求事件

`打赏请求` 是一个 kind `9734` 的事件，它*不*发布到中继，而是发送到接收者的 lnurl 支付 `callback` URL。此事件的 `content` 可以是随付款一起发送的可选消息。事件必须包含以下标签：

- `relays` 是中继列表，接收者的钱包应将其 `打赏收据` 发布到这些中继。请注意，中继不应嵌套在额外的列表中，而应按下面示例所示包含。
- `amount` 是发送者打算支付的金额，单位为*毫聪*，格式为字符串。这是推荐的，但为可选。
- `lnurl` 是接收者的 lnurl 支付 URL，使用 bech32 编码，前缀为 `lnurl`。这是推荐的，但为可选。
- `p` 是接收者的十六进制编码公钥。

此外，事件可以包含以下标签：

- `e` 是可选的十六进制编码事件 ID。如果打赏的是事件而非个人，客户端必须包含此标签。
- `a` 是可选的坐标，允许打赏可寻址事件，如 NIP-23 长文笔记。
- `k` 是目标事件的字符串化 kind。

示例：

```json
{
  "kind": 9734,
  "content": "Zap!",
  "tags": [
    ["relays", "wss://nostr-pub.wellorder.com", "wss://anotherrelay.example.com"],
    ["amount", "21000"],
    ["lnurl", "lnurl1dp68gurn8ghj7um5v93kketj9ehx2amn9uh8wetvdskkkmn0wahz7mrww4excup0dajx2mrv92x9xp"],
    ["p", "04c915daefee38317fa734444acee390a8269fe5810b2241e5e6dd343dfbecc9"],
    ["e", "9ae37aa68f48645127299e9453eb5d908a0cbb6058ff340d528ed4d37c8994fb"],
    ["k", "1"]
  ],
  "pubkey": "97c70a44366a6535c145b333f973ea86dfdc2d7a99da618c40c64705ad98e322",
  "created_at": 1679673265,
  "id": "30efed56a035b2549fcaeec0bf2c1595f9a9b3bb4b1a38abaf8ee9041c4b7d93",
  "sig": "f2cb581a84ed10e4dc84937bd98e27acac71ab057255f6aa8dfa561808c981fe8870f4a03c1e3666784d82a9c802d3704e174371aa13d63e2aeaf24ff5374d9d"
}
```

### 附录 B：打赏请求 HTTP 请求

已签名的 `打赏请求` 事件不发布，而是通过 HTTP GET 请求发送到接收者的 `callback` URL，该 URL 由接收者的 lnurl 支付端点提供。此请求应包含以下查询参数：

- `amount` 是发送者打算支付的金额，单位为*毫聪*
- `nostr` 是 `9734` `打赏请求` 事件，先 JSON 编码，再 URI 编码
- `lnurl` 是接收者的 lnurl 支付 URL，使用 bech32 编码，前缀为 `lnurl`

此请求应返回一个包含 `pr` 键的 JSON 响应，`pr` 是发送者必须支付以完成打赏的发票。以下是一个 JavaScript 示例流程：

```javascript
const senderPubkey // 发送者的公钥
const recipientPubkey = // 接收者的公钥
const callback = // 从接收者 lnurl 支付端点收到的回调 URL
const lnurl = // 接收者的闪电地址，编码为 lnurl
const sats = 21

const amount = sats * 1000
const relays = ['wss://nostr-pub.wellorder.net']
const event = encodeURI(JSON.stringify(await signEvent({
  kind: 9734,
  content: "",
  pubkey: senderPubkey,
  created_at: Math.round(Date.now() / 1000),
  tags: [
    ["relays", ...relays],
    ["amount", amount.toString()],
    ["lnurl", lnurl],
    ["p", recipientPubkey],
  ],
})))

const {pr: invoice} = await fetchJson(`${callback}?amount=${amount}&nostr=${event}&lnurl=${lnurl}`)
```

### 附录 C：LNURL 服务器配置

lnurl 服务器需要一些额外信息，以便客户端知道支持打赏发票：

1. 在 lnurl-pay 静态端点 `/.well-known/lnurlp/<user>` 中添加 `nostrPubkey`，其中 `nostrPubkey` 是您的服务器将用于签署 `打赏收据` 事件的 nostr 公钥。客户端将使用此值验证 `打赏收据`。
2. 添加 `allowsNostr` 字段并将其设置为 true。

### 附录 D：LNURL 服务器打赏请求验证

当客户端将 `打赏请求` 事件发送到服务器的 lnurl-pay 回调 URL 时，会有一个 `nostr` 查询参数，其值是 URI 编码和 JSON 编码的事件。如果存在，`打赏请求` 事件必须按以下方式验证：

1. 必须具有有效的 nostr 签名
2. 必须包含 tags
3. 必须仅有一个 `p` 标签
4. 必须有 0 或 1 个 `e` 标签
5. 应有一个 `relays` 标签，包含要发送 `打赏收据` 的中继
6. 如果有 `amount` 标签，它必须等于 `amount` 查询参数
7. 如果有 `a` 标签，它必须是有效的坐标
8. 必须有 0 或 1 个 `P` 标签。如果有，它必须等于 `打赏收据` 的 `pubkey`

事件随后必须存储起来，以便在发票支付时使用。

### 附录 E：打赏收据事件

`打赏收据` 由闪电节点在 `打赏请求` 生成的发票被支付时创建。`打赏收据` 仅在发票描述（提交到描述哈希）包含 `打赏请求` 笔记时才会创建。

收到付款时，执行以下步骤：

1. 获取发票的描述。这需要在生成描述哈希发票时将其保存到某处。如果您使用 CLN（此处使用的参考实现），系统会自动为您保存。
2. 将 bolt11 描述解析为 JSON nostr 事件。这应根据附录 D 的要求进行验证，可以在接收时验证，也可以在发票支付前验证。
3. 创建一个 kind `9735` 的 nostr 事件，如下所述，并将其发布到 `打赏请求` 中声明的 `relays`。

关于 `打赏收据` 事件的以下要求：

- `content` 应为空。
- `created_at` 日期应设置为发票的 `paid_at` 日期，以保证幂等性。
- `tags` 必须包含 `p` 标签（打赏接收者）以及可选的来自 `打赏请求` 的 `e` 标签、可选的来自 `打赏请求` 的 `a` 标签以及可选的 `P` 标签（来自打赏请求的公钥，即打赏发送者）。
- `打赏收据` 必须有一个 `bolt11` 标签，包含描述哈希 bolt11 发票。
- `打赏收据` 必须包含一个 `description` 标签，其值是 JSON 编码的打赏请求。
- `SHA256(description)` 应与 bolt11 发票中的描述哈希匹配。
- `打赏收据` 可以包含一个 `preimage` 标签，用于与 bolt11 发票的支付哈希匹配。这实际上不是支付证明，没有真正的方法证明发票是真实的或已被支付。您需要信任 `打赏收据` 的作者关于付款的合法性。

`打赏收据` 不是支付证明，它仅证明某些 nostr 用户获取了一张发票。`打赏收据` 的存在暗示发票已被支付，但在恶意实现下可能是虚假的。

支持打赏的 lnurl 服务器的参考实现可以参见 [此处](https://github.com/jb55/cln-nostr-zapper)。

`打赏收据` 示例：

```json
{
    "id": "67b48a14fb66c60c8f9070bdeb37afdfcc3d08ad01989460448e4081eddda446",
    "pubkey": "9630f464cca6a5147aa8a35f0bcdd3ce485324e732fd39e09233b1d848238f31",
    "created_at": 1674164545,
    "kind": 9735,
    "tags": [
      ["p", "32e1827635450ebb3c5a7d12c1f8e7b2b514439ac10a67eef3d9fd9c5c68e245"],
      ["P", "97c70a44366a6535c145b333f973ea86dfdc2d7a99da618c40c64705ad98e322"],
      ["e", "3624762a1274dd9636e0c552b53086d70bc88c165bc4dc0f9e836a1eaf86c3b8"],
      ["k", "1"],
      ["bolt11", ""],
      ["description", "{\"pubkey\":\"97c70a44366a6535c145b333f973ea86dfdc2d7a99da618c40c64705ad98e322\",\"content\":\"\",\"id\":\"d9cc14d50fcb8c27539aacf776882942c1a11ea4472f8cdec1dea82fab66279d\",\"created_at\":1674164539,\"sig\":\"77127f636577e9029276be060332ea565deaf89ff215a494ccff16ae3f757065e2bc59b2e8c113dd407917a010b3abd36c8d7ad84c0e3ab7dab3a0b0caa9835d\",\"kind\":9734,\"tags\":[[\"e\",\"3624762a1274dd9636e0c552b53086d70bc88c165bc4dc0f9e836a1eaf86c3b8\"],[\"p\",\"32e1827635450ebb3c5a7d12c1f8e7b2b514439ac10a67eef3d9fd9c5c68e245\"],[\"relays\",\"wss://relay.damus.io\",\"wss://nostr-relay.wlvs.space\",\"wss://nostr.fmt.wiz.biz\",\"wss://relay.nostr.bg\",\"wss://nostr.oxtr.dev\",\"wss://nostr.v0l.io\",\"wss://brb.io\",\"wss://nostr.bitcoiner.social\",\"ws://monad.jb55.com:8080\",\"wss://relay.snort.social\"]]}"],
      ["preimage", "5d006d2cf1e73c7148e7519a4c68adc81642ce0e25a432b2434c99f97344c15f"]
    ],
    "content": "",
  }
```

### 附录 F：验证打赏收据

客户端可以使用 NIP-01 过滤器获取事件和公钥上的 `打赏收据`，例如 `{"kinds": [9735], "#e": [...]}`。打赏必须使用以下步骤进行验证：

- `打赏收据` 事件的 `pubkey` 必须与接收者的 lnurl 提供者的 `nostrPubkey`（在协议流程第 1 步中获取）相同。
- `打赏收据` 的 `bolt11` 标签中包含的 `invoiceAmount` 必须等于 `打赏请求` 的 `amount` 标签（如果存在）。
- `打赏请求` 的 `lnurl` 标签（如果存在）应等于接收者的 `lnurl`。

### 附录 G：其他事件上的 `zap` 标签

当事件包含一个或多个 `zap` 标签时，希望打赏的客户端应根据标签值而不是事件作者的资料字段计算 lnurl 支付请求。标签的第二个参数是接收者公钥的 `hex` 字符串，第三个参数是用于下载接收者元数据（Kind-0）的中继。可选的第四个参数指定分配给相应接收者的权重（百分比的泛化）。客户端应解析所有权重，计算总和，然后计算每个接收者的百分比。如果未提供权重，客户端应将打赏金额平均分配给所有接收者。如果仅部分提供了权重，没有权重的接收者不应被打赏（`weight = 0`）。

```yaml
{
    "tags": [
        [ "zap", "82341f882b6eabcd2ba7f1ef90aad961cf074af15b9ef44a09f9d2a8fbfbe6a2", "wss://nostr.oxtr.dev", "1" ],  // 25%
        [ "zap", "fa984bd7dbb282f07e16e7ae87b26a2a7b9b90b7246a44771f0cf5ae58018f52", "wss://nostr.wine/",    "1" ],  // 25%
        [ "zap", "460c25e682fda7832b52d1f22d3d22b3176d972f60dcdc3212ed8c92ef85065c", "wss://nos.lol/",       "2" ]   // 50%
    ]
}
```

客户端可以在笔记中显示打赏拆分配置。

## 未来工作

打赏可以通过将 `打赏请求` 笔记加密给目标用户来变得更私密，但为简单起见，这已被排除在初始草案之外。
