NIP-23
======

长文内容
--------

`draft` `optional`

本 NIP 定义了 `kind:30023`（一种_可寻址事件_），用于长文文本内容，通常称为"文章"或"博客帖子"。

已弃用：`kind:30024` 曾用于长文草稿（自加密 nip04，格式与 `kind:30023` 相同）。推荐使用 [NIP-37](37.md) 来编辑长文章草稿。

以 `kind:1` 笔记为主的"社交"客户端不应被要求实现本 NIP。

### 格式

这些事件的 `.content` 应为 Markdown 语法的字符串文本。为了在不同客户端和设备之间最大化兼容性和可读性，任何创建长文笔记的客户端：

- 不得对段落文本进行硬换行，例如在 80 列边界处任意换行。

- 不得支持在 Markdown 中添加 HTML。

### 元数据

最后更新日期应使用 `.created_at` 字段，"标签"/"话题标签"（即事件可能相关的主题）应使用 `t` 标签。

其他元数据字段可以根据需要作为标签添加到事件中。这里我们标准化了 4 个可能有用的字段，但它们仍然是严格可选的：

- `"title"`，文章标题
- `"image"`，指向与标题一起显示的图片的 URL
- `"summary"`，文章摘要
- `"published_at"`，文章首次发布时的 Unix 秒级时间戳（字符串形式）

### 可编辑性

这些文章应该是可编辑的，因此它们应包含一个带有文章标识符的 `d` 标签。客户端应注意只在实现了该功能的 relay 上发布和读取这些事件。如果 relay 不支持，客户端还应隐藏可能收到的同一文章的旧版本。

### 链接

文章可以使用 [NIP-19](19.md) 的 `naddr` 编码以及 `a` 标签进行链接。

### 引用

对其他 Nostr 笔记、文章或个人资料的引用必须按照 [NIP-27](27.md) 进行，即使用 [NIP-21](21.md) 的 `nostr:...` 链接，并可选择为这些链接添加标签（参见下面的示例）。

## 示例事件

```json
{
  "kind": 30023,
  "created_at": 1675642635,
  "content": "Lorem [ipsum][nostr:nevent1qqst8cujky046negxgwwm5ynqwn53t8aqjr6afd8g59nfqwxpdhylpcpzamhxue69uhhyetvv9ujuetcv9khqmr99e3k7mg8arnc9] dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.\n\nRead more at nostr:naddr1qqzkjurnw4ksz9thwden5te0wfjkccte9ehx7um5wghx7un8qgs2d90kkcq3nk2jry62dyf50k0h36rhpdtd594my40w9pkal876jxgrqsqqqa28pccpzu.",
  "tags": [
    ["d", "lorem-ipsum"],
    ["title", "Lorem Ipsum"],
    ["published_at", "1296962229"],
    ["t", "placeholder"],
    ["e", "b3e392b11f5d4f28321cedd09303a748acfd0487aea5a7450b3481c60b6e4f87", "wss://relay.example.com"],
    ["a", "30023:a695f6b60119d9521934a691347d9f78e8770b56da16bb255ee286ddf9fda919:ipsum", "wss://relay.nostr.org"]
  ],
  "pubkey": "...",
  "id": "..."
}
```

### 回复与评论

对 `kind 30023` 的回复必须使用 [NIP-22](./22.md) 的 `kind 1111` 评论。
