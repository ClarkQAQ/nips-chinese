NIP-56
======

举报
----

`可选`

举报是一个 `kind 1984` 事件，用于向用户和中继发出信号，表明某些被引用的内容是不良的。不良的定义
显然是主观的，网络上的所有主体（用户、应用、中继等）
可以根据自己的判断消费并对其采取行动。

`content` 可能包含举报内容的实体提交的额外信息。

标签
----

举报事件必须包含一个引用被举报用户公钥的 `p` 标签。

如果举报的是笔记，则还必须包含一个引用笔记 ID 的 `e` 标签。

一个 `报告类型` 字符串必须作为被举报的 `e`、`p` 或 `x` 标签的第三个条目包含在内，
包括以下举报类型：

- `nudity` - 裸体、色情等描绘
- `malware` - 病毒、木马、蠕虫、机器人、间谍软件、广告软件、后门、勒索软件、rootkit、绑架软件等
- `profanity` - 粗口、仇恨言论等
- `illegal` - 在某些司法管辖区可能非法的内容
- `spam` - 垃圾信息
- `impersonation` - 冒充他人
- `other` - 不适合上述类别的举报

某些举报标签仅对个人资料举报有意义，例如 `impersonation`。

- `x` 标签应为意图举报的 blob 的信息哈希。当使用了 `x` 标签时，客户端必须包含一个 `e` 标签，该标签是包含所提及 blob 的事件 ID。此外，这些事件还可以包含一个 `server` 标签，用于指向可能包含所提及媒体的媒体服务器。

`l` 和 `L` 标签也可以按照 [NIP-32](32.md) 中的定义使用，以支持
进一步的限定和查询。

示例事件
--------

```yaml
{
  "kind": 1984,
  "tags": [
    ["p", "<pubkey>", "nudity"],
    ["L", "social.nos.ontology"],
    ["l", "NS-nud", "social.nos.ontology"]
  ],
  "content": "",
  // 其他字段...
}
```

```yaml
{
  "kind": 1984,
  "tags": [
    ["e", "<eventId>", "illegal"],
    ["p", "<pubkey>"]
  ],
  "content": "他在侮辱国王！",
  // 其他字段...
}
```

```yaml
{
  "kind": 1984,
  "tags": [
    ["p", "<impersonator pubkey>", "impersonation"]
  ],
  "content": "个人资料正在冒充 nostr:<victim bech32 pubkey>",
  // 其他字段...
}
```

```yaml
{
  "kind": 1984,
  "tags": [
    ["x", "<blob hash>", "malware"],
    ["e", "<event id which contains the blob on x tag>", "malware"],
    ["server", "https://you-may-find-the-blob-here.com/path-to-url.ext"]
  ],
  "content": "此文件包含恶意软件。",
  // 其他字段...
}
```

客户端行为
--------

客户端可以使用来自好友的举报来做出审核决定（如果它们
选择这样做）。例如，如果你的 3 个或更多好友举报某个个人资料为 `nudity`，
客户端可以选择自动模糊该账户的照片。

中继行为
--------

不建议中继使用举报执行自动审核，
因为举报容易被操纵。管理员可以使用来自可信审核员的举报来
删除非法或露骨内容（如果中继不允许此类内容）。
