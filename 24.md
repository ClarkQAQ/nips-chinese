NIP-24
======

额外的元数据字段和标签
------------------------------

`draft` `optional`

本 NIP 记录了可添加到事件中的额外可选字段，这些字段虽未在其他地方定义，但已成为事实上的标准，以及其他一些不值得单独编写 NIP 且在其他 NIP 中也没有归宿的次要实现可能性。

### kind 0

以下是 NIP-01 未指定但可能出现在元数据事件 JSON 字符串化结果中的额外字段：

  - `display_name`：一个可选的、字符更丰富的更大名称，优于 `name`。无论元数据中是否存在 `display_name`，都应始终设置 `name`。
  - `website`：与事件作者相关的任何形式的 Web URL。
  - `banner`：一张宽幅（约 1024x768）图片的 URL，可选择在个人资料屏幕的背景中显示。
  - `bot`：一个布尔值，用于说明 content 全部或部分由自动化产生，例如聊天机器人或新闻推送。
  - `birthday`：代表作者出生日期的对象。格式为 `{ "year": number, "month": number, "day": number }`。每个字段均可省略。

#### 已弃用的字段

以下是当在外部发现时应忽略或移除的字段：

  - `displayName`：请改用 `display_name`。
  - `username`：请改用 `name`。

### kind 3

以下是 NIP-02 未指定但可能出现在关注事件 JSON 字符串化结果中的额外字段：

#### 已弃用的字段

  - `{<relay-url>: {"read": <true|false>, "write": <true|false>}, ...}`：用户用于读取/写入的中继对象。应改用 [NIP-65](65.md)。

### 标签

以下标签可能出现在多种事件 kind 中。如果没有更具体的 NIP 指定不同的含义，则它们具有以下含义：

  - `r`：事件以某种方式引用的 Web URL。
  - `i`：事件以某种方式引用的外部 ID——参见 [NIP-73](73.md)。
  - `title`：[NIP-51](51.md) 集合、[NIP-52](52.md) 日历事件、[NIP-53](53.md) 直播事件或 [NIP-99](99.md) 列表的名称。
  - `t`：一个话题标签。值必须为小写字符串。
