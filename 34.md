NIP-34
======

`git` 相关
-----------

`draft` `optional`

本 NIP 定义了所有使用或邻近 [`git`](https://git-scm.com/) 进行代码协作的方式，这些方式可以通过 Nostr 实现。

## 仓库公告

Git 仓库托管在支持 Git 的服务器上，但它们的存在可以通过 Nostr 事件进行公告。通过这样做，作者声明自己是主要项目的维护者，除非包含 `u` 标签。

```yaml
{
  "kind": 30617,
  "content": "",
  "tags": [
    ["d", "<repo-id>"], // 通常为 kebab-case 短名称
    ["name", "<人类可读的项目名称>"],
    ["description", "<简短的人类可读项目描述>"],
    ["web", "<浏览用网址>", ...], // 如果使用的 git 服务器提供此功能，则为网页 URL
    ["clone", "<用于 git 克隆的网址>", ...], // 提供给 `git clone` 的 URL，以便任何人都可以克隆它
    ["relays", "<中继 URL>", ...], // 此仓库将监听补丁和议题的中继
    ["r", "<最早的唯一提交 ID>", "euc"],
    ["maintainers", "<其他认可的维护者>", ...],
    ["u","30617:<pubkey>:<identifier>|<git-url-https-prefered>","<relay-hint>","<author-pubkey>"], // 可选，指示仓库是子分支
    ["t", "<任意字符串>"], // 标记仓库的标签
  ]
}
```

`web`、`clone`、`relays`、`maintainers` 标签可以有多个值。

带有 `"euc"` 标记的 `r` 标签应为该仓库最早唯一提交的提交 ID，用于在分支中识别它，并可将其与其他地方托管的可能代表同一项目的仓库归为一组。大多数情况下，它将是仓库的根提交。如果是两个项目之间的永久性分支，则应使用分支之后的第一个提交。

除 `d` 外，所有标签都是可选的。

### Nostr 克隆 URL 格式

`nostr://` URL 可用于引用仓库公告，当安装了 [git-remote-nostr](https://git-scm.com/docs/gitremote-helpers) 助手时，与 `git clone` 兼容：

```
nostr://<naddr>
nostr://<npub|nip05>/<identifier>
nostr://<npub|nip05>/<relay-hint>/<identifier>
```

`<relay-hint>` 和 `<identifier>` 必须按照 [RFC 3986 第 2.1 节](https://datatracker.ietf.org/doc/html/rfc3986#section-2.1) 进行百分号编码。`<relay-hint>` 是一个中继 URL，其 `wss://` 方案可为简洁而省略。`<identifier>` 是 kind `30617` 事件的 `d` 标签值。

示例：

```
nostr://npub15qydau2hjma6ngxkl2cyar74wzyjshvl65za5k5rl69264ar2exs5cyejr/relay.ngit.dev/ngit
nostr://npub15qydau2hjma6ngxkl2cyar74wzyjshvl65za5k5rl69264ar2exs5cyejr/my%20%F0%9F%9A%80%20repo
nostr://danconwaydev.com/ws%3A%2F%2Flocalhost%3A7334/my-local-only-repo
nostr://danconwaydev.com/relay.ngit.dev/ngit
```

## 仓库状态公告

可选的分支和标签状态信息来源。

```yaml
{
  "kind": 30618,
  "content": "",
  "tags": [
    ["d", "<repo-id>"], // 匹配对应仓库公告中的标识符
    ["refs/<heads|tags>/<branch-or-tag-name>","<commit-id>"]
    ["HEAD", "ref: refs/heads/<branch-name>"]
  ]
}
```

`refs` 标签可以出现多次，也可以不出现。

## 补丁和拉取请求

补丁和 PR 可以由任何人发送到任何仓库。发送到特定仓库的补丁和 PR 应发送到该仓库公告事件的 `"relays"` 标签中指定的中继。补丁和 PR 事件应包含一个指向该仓库公告地址的 `a` 标签。

如果每个事件小于 60kb，应使用补丁；否则应使用 PR。

### 补丁

补丁集中的补丁应包含指向上一补丁的 [NIP-10](10.md) `e` `reply` 标签。

补丁修订中的第一个补丁修订应包含指向原始根补丁的 [NIP-10](10.md) `e` `reply` 标签。

```yaml
{
  "kind": 1617,
  "content": "<patch>", // <git format-patch> 的内容
  "tags": [
    ["a", "30617:<base-repo-owner-pubkey>:<base-repo-id>"],
    ["r", "<repo-earliest-unique-commit-id>"], // 以便客户端可以订阅发送到本地 git 仓库的所有补丁
    ["p", "<repository-owner>"],
    ["p", "<other-user>"], // 可选，将补丁发送给另一个用户以引起其注意

    ["t", "root"], // 系列中的后续补丁省略此标签
    // 修订中的第一个补丁
    ["t", "root-revision"],

    // 当希望合并后的补丁具有稳定的提交 ID 时的可选标签
    // 这些字段对于确保应用补丁后的提交具有与提议者机器上相同的 ID 是必要的
    // 如果维护者不在意这些，所有这些标签都可以省略
    ["commit", "<current-commit-id>"],
    ["r", "<current-commit-id>"], // 以便客户端可以找到特定提交的现有补丁
    ["parent-commit", "<parent-commit-id>"],
    ["commit-pgp-sig", "-----BEGIN PGP SIGNATURE-----..."], // 未签名的提交为空字符串
    ["committer", "<name>", "<email>", "<timestamp>", "<timezone offset in minutes>"],
  ]
}
```

系列中的第一个补丁可以是 `git format-patch` 生成的封面信。

### 拉取请求

拉取请求是一个指向 git 仓库中提议变更的事件。

```yaml
{
  "kind": 1618,
  "content": "<markdown 文本>",
  "tags": [
    ["a", "30617:<base-repo-owner-pubkey>:<base-repo-id>"],
    ["r", "<repo-earliest-unique-commit-id>"], // 以便客户端可以订阅发送到本地 git 仓库的所有 PR
    ["p", "<repository-owner>"],
    ["p", "<other-user>"], // 可选，将 PR 发送给另一个用户以引起其注意

    ["subject", "<PR 主题>"],
    ["t", "<PR 标签>"], // 可选
    ["t", "<另一个 PR 标签>"], // 可选

    ["c", "<current-commit-id>"], // PR 分支的顶端
    ["clone", "<clone-url>", ...], // 至少一个可以下载提交的 git clone URL
    ["branch-name", "<branch-name>"], // 可选推荐的 branch 名称

    ["e", "<root-patch-event-id>"], // 可选，表示 PR 是现有补丁的修订版，应关闭原补丁
    ["merge-base", "<commit-id>"], // 可选：与目标分支最近的共同祖先
  ]
}
```

### 拉取请求更新

PR 更新更改引用的 PR 事件的顶端。

```yaml
{
  "kind": 1619,
  "content": "",
  "tags": [
    ["a", "30617:<base-repo-owner-pubkey>:<base-repo-id>"],
    ["r", "<repo-earliest-unique-commit-id>"], // 以便客户端可以订阅发送到本地 git 仓库的所有 PR
    ["p", "<repository-owner>"],
    ["p", "<other-user>"], // 可选，将 PR 发送给另一个用户以引起其注意

    // NIP-22 标签
    ["E", "<pull-request-event-id>"],
    ["P", "<pull-request-author>"],

    ["c", "<current-commit-id>"], // 更新后的 PR 顶端
    ["clone", "<clone-url>", ...], // 至少一个可以下载提交的 git clone URL
    ["merge-base", "<commit-id>"], // 可选：与目标分支最近的共同祖先
  ]
}
```

## 议题

议题是与仓库相关的、人类可读的讨论线程的 Markdown 文本：错误报告、功能请求、问题或任何类型的评论。与补丁一样，这些应发送到该仓库公告事件的 `"relays"` 标签中指定的中继。

议题可以有一个 `subject` 标签，客户端可以利用它来显示标题。此外，可以包含一个或多个 `t` 标签来为议题提供标签。

```json
{
  "kind": 1621,
  "content": "<markdown 文本>",
  "tags": [
    ["a", "30617:<base-repo-owner-pubkey>:<base-repo-id>"],
    ["p", "<repository-owner>"]
    ["subject", "<议题主题>"]
    ["t", "<议题标签>"]
    ["t", "<另一个议题标签>"]
  ]
}
```

## 回复

对 `kind:1621`（_议题_）、`kind:1617`（_补丁_）或 `kind:1618`（_拉取请求_）事件的回复应遵循 [NIP-22 评论](22.md)。

## 状态

根补丁、PR 和议题有一个状态，默认为"开放"（Open），可以通过发布状态事件来设置。

```yaml
{
  "kind": 1630, // 开放（Open）
  "kind": 1631, // 已应用/已合并（Applied/Merged）（对于补丁）；已解决（Resolved）（对于议题）
  "kind": 1632, // 已关闭（Closed）
  "kind": 1633, // 草稿（Draft）
  "content": "<markdown 文本>",
  "tags": [
    ["e", "<issue-or-PR-or-original-root-patch-id-hex>", "", "root"],
    ["e", "<accepted-revision-root-id-hex>", "", "reply"], // 当修订被应用时
    ["p", "<repository-owner>"],
    ["p", "<root-event-author>"],
    ["p", "<revision-author>"],

    // 可选，用于提高订阅筛选效率
    ["a", "30617:<base-repo-owner-pubkey>:<base-repo-id>", "<relay-url>"],
    ["r", "<repo-earliest-unique-commit-id>"]

    // `1631` 状态的可选字段
    ["q", "<applied-or-merged-patch-event-id>", "<relay-url>", "<pubkey>"], // 各一个
    // 当合并时
    ["merge-commit", "<merge-commit-id>"]
    ["r", "<merge-commit-id>"]
    // 当应用时
    ["applied-as-commits", "<commit-id-in-master-branch>", ...]
    ["r", "<applied-commit-id>"] // 各一个
  ]
}
```

最近（按 `created_at` 日期）来自议题/补丁作者或维护者的状态事件被视为有效。

补丁修订的状态要么是根补丁的状态，如果根补丁的状态是 `1631`（_已应用/已合并_）且补丁修订未被标记在 `1631`（_已应用/已合并_）事件中，则为 `1632`（_已关闭_）。

## 用户 grasp 列表

用户通常希望用于 NIP-34 相关活动的 [grasp 服务器](https://njump.me/naddr1qvzqqqrhnypzpgqgmmc409hm4xsdd74sf68a2uyf9pwel4g9mfdg8l5244t6x4jdqy28wumn8ghj7un9d3shjtnwva5hgtnyv4mqqpt8wfshxuqlnvh8x) 列表。其功能类似于 NIP-65 中继列表和 NIP-B7 blossom 列表。

该事件应包含一个 `g` 标签列表，包含按优先顺序排列的 grasp 服务 WebSocket URL。

```yaml
{
  "kind": 10317,
  "content": "",
  "tags": [
    ["g", "<grasp-service-websocket-url>"], // 零个或多个 grasp 服务器 URL
  ],
}
```

## 可能稍后添加的内容

- 内联文件评论 kind（我们可能需要对补丁使用一种，对合并后的文件使用另一种）
