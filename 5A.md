NIP-5A
======

静态网站（nsites）
------------------------

`draft` `optional`

本 NIP 描述了一种从 Blossom 资产托管静态网站的方法。

### 站点清单

站点清单事件 `MUST` 是 [NIP-01](01.md) 中定义的常规、可替换或可寻址事件。

有两种类型的 nsite 清单事件种类：

- **根站点**：使用 kind `15128`，且 `MUST NOT` 包含 `d` 标签。这是每个公钥的单个可替换事件，作为该公钥的根站点。
- **命名站点**：使用 kind `35128`。这些可以作为公钥下的较小子网站，可以理解为子域名。

### 命名站点

命名站点使用 kind `35128`，且 `MUST` 包含一个包含站点标识符的 `d` 标签。

`d` 标签标识命名站点，并作为该站点在作者命名空间内的标识符。

对于规范的命名站点 URL，`d` 标签值 `MUST` 匹配 `^[a-z0-9-]{1,13}$`，且 `MUST NOT` 以 `-` 结尾。

由于 DNS 标签限制为 63 个字符，且 `pubkeyB36` 占用其中 50 个，`d` 标签值必须为 1-13 个字符。

#### Nsite 事件标签


| 标签          | 角色                                                                                                | 必需                          |
| ------------- | --------------------------------------------------------------------------------------------------- | ------------------------------ |
| `path`        | 将绝对路径映射到该路径下提供服务的文件的 SHA-256 哈希值。                                               | 是，一个或多个                  |
| `x`           | 提供完整清单的聚合哈希，使站点版本可索引和可发现。                                                   | 可选，推荐                     |
| `d`           | 命名站点的短标识符。此标签仅用于 kind `35128` 事件。                                                 | 仅命名站点必需                  |
| `a`           | 引用此复制站点所来自的直接父 nsite。                                                                 | 仅复制站点必需                  |
| `A`           | 引用复制站点谱系的源 nsite。                                                                         | 仅复制站点必需                  |
| `server`      | 提示哪些 Blossom 服务器可用于检索 blob。                                                              | 否                             |
| `title`       | 提供人类可读的站点标题。                                                                             | 否                             |
| `description` | 提供站点的人类可读简短描述。                                                                        | 否                             |
| `source`      | 指向站点源代码仓库或源码归档。                                                                      | 否                             |

事件 `MUST` 包含一个或多个 `path` 标签，将绝对路径映射到 SHA-256 哈希值。每个 `path` 标签 `MUST` 具有格式 `["path", "/absolute/path", "sha256hash"]`，其中：

- 第一个元素是字面字符串 `"path"`
- 第二个元素是以文件名和扩展名结尾的绝对路径
- 第三个元素是在此路径下提供服务的文件的 SHA-256 哈希值

事件 `SHOULD` 包含一个 `x` 标签，包含如下定义的站点聚合哈希。`x` 标签 `MUST` 具有格式 `["x", "<sha256-hex>", "aggregate"]`，其中 `<sha256-hex>` 是小写十六进制聚合哈希。

事件 `MAY` 包含 `server` 标签，提示哪些 Blossom 服务器可用于查找与哈希关联的 blob。

事件 `MAY` 包含 `title` 和 `description` 标签，提供简单的站点信息。

事件 `MAY` 包含一个 `source` 标签，链接到站点的源代码仓库或源码归档。`source` 标签 `MUST` 具有格式 `["source", "<url>"]`。对于源代码仓库，`<url>` `MAY` 是由 [NIP-34](34.md) 定义的 `nostr://` git URL，或绝对的 `https://` git URL。对于源码归档，`<url>` `MUST` 是绝对的 `https://` URL。

### 清单快照

清单快照使用 kind `5128`，是一个常规事件。其目的是在特定时间点捕获根站点或命名站点的状态。

在发布根站点或命名站点时，作者 `MAY` 同时发布清单快照事件作为附加事件，以保留版本历史并使历史版本可通过快照事件 id 寻址。对于给定站点，快照事件的 `created_at` 时间戳是该快照的版本时间戳。

要对现有根站点或命名站点进行快照，清单快照事件 `MUST` 复制源清单的 `path` 标签，`MUST` 包含恰好一个其值与源清单聚合 `x` 标签完全匹配的 `x` 标签，并且 `MUST` 包含恰好一个引用源根站点或命名站点的 `a` 标签。如果源清单包含 `A` 标签，快照事件 `MUST` 不变地复制该 `A` 标签。如果源清单不包含 `A` 标签，快照事件 `SHOULD` 省略它。被引用的站点标识被快照的 nsite，而快照事件自身的 `created_at` 标识版本。其他有效的 nsite 标签，如 `title`、`description`、`source`、`app`、`server` 或中继提示，如果作者打算保留它们，`MAY` 也被复制，但它们是可选的。

### 聚合哈希

站点聚合哈希是站点 `path` 标签的确定性哈希。它标识特定的站点版本，并可用于标识不同公钥发布的等效站点清单。

聚合哈希 `MUST` 仅使用 `path` 标签计算。它 `MUST NOT` 依赖于清单中标签的顺序，并且 `MUST` 忽略所有其他标签和事件字段，包括 `app` 标签。

当且仅当两个站点清单事件产生相同的聚合哈希时，它们是等效的。

站点清单 `SHOULD` 在 `x` 标签中包含此值，格式为 `["x", "<sha256-hex>", "aggregate"]`，以便聚合哈希可索引以实现可发现性和查找。kind `5128` 的清单快照事件 `MUST` 包含恰好一个此类标签，且 `MUST` 与父 nsite 的聚合 `x` 标签完全匹配。

计算聚合哈希的方法：

1. 收集每个 `path` 标签。
2. 对于每个标签，按以下精确格式生成一行：`<sha256hash> <absolute-path>\n`。
3. 按升序词典顺序对所有行进行排序。
4. 将排序后的行连接为 UTF-8 字节。
5. 计算连接字节的 SHA-256 哈希。

生成的摘要 `MUST` 编码为小写十六进制。

输入示例：

```text
186ea5fd14e88fd1ac49351759e7ab906fa94892002b60bf7f5a428f28ca1c99 /index.html
fedcba0987654321fedcba0987654321fedcba0987654321fedcba0987654321 /favicon.ico
```

计算示例：

```bash
nak req -k 15128 -a <pubkey> --limit 1 wss://relay.example.com | jq -r '.tags[] | select(.[0] == "path") | "\(.[2]) \(.[1])"' | sort | sha256sum
```

```javascript
const aggregateHash = sha256(event.tags.filter(t => t[0] === "path").map(([, path, hash]) => `${hash} ${path}\n`).sort().join(""))
```

### 上游应用描述符

nsite `MAY` 包含一个或多个 `app` 标签，引用清单所属的上游应用描述符事件。`app` 标签 `MUST` 具有格式 `["app", "<kind>:<pubkey>:<d-tag>", "<relay>"]`，其中第二个元素是对应用描述符的可寻址事件引用，第三个元素是用于定位该事件的中继提示。

此关联是可选的。其目的是为应用级别的发现和打包流程留出空间，而无需应用创建者自行扩展 nsite 清单或用非清单问题使清单过载。

引用的应用描述符种类有意保持通用，因此 nsite 可以引用 [NIP-89](89.md) 应用程序以及其他现有或未来形式的应用描述符。可发现性、应用商店元数据、运行时兼容性以及类似的应用级别问题属于那些应用描述符事件，而 nsite 清单则保持专注于描述站点的文件。

### 复制 nsites

nsite 可以通过复制另一个根站点或命名站点来创建，以便将该站点以特定状态固定在不同的作者命名空间下。

复制的目的是保留和重新发布现有的 nsite，同时允许新作者独立维护它。因此，创建复制 nsite 的作者 `SHOULD` 在仍然适用的情况下复制源 nsite 的所有标签，包括 `path`、`x`、`app`、`server`、`title`、`description` 和 `source`。

复制 nsite 不需要保留原始站点的种类或标识符。复制的根站点 `MAY` 成为命名站点，复制的命名站点 `MAY` 成为根站点，复制的命名站点 `MAY` 使用与其父站点不同的 `d` 标签。

当站点从另一个 nsite 复制时，新清单 `MUST` 包含恰好一个小写 `a` 标签，引用其复制来源的直接父 nsite。

复制站点还 `MUST` 包含恰好一个大写 `A` 标签，引用复制谱系的源 nsite。如果站点直接从原始 nsite 复制，`a` 和 `A` 标签 `MAY` 引用同一个 nsite。如果站点从另一个复制站点复制，`A` 标签 `MUST` 从父站点不变地复制。

由于复制站点可能更改种类或标识符，`a` 和 `A` 标签是稳定的、可索引的引用，保留了复制站点的谱系。

未从其他 nsite 复制的站点 `SHOULD NOT` 包含 `a` 或 `A` 标签。

### 主机服务器实现

主机服务器是一个 HTTP 服务器，负责提供公钥静态网站。

#### 地址格式

为实现互操作性，主机服务器 `SHOULD` 使用以下规范 URL 格式：

- 根站点：`<npub>.nsite-host.com`
- 快照：`v<snapshotIdB36>.nsite-host.com`
- 命名站点：`<pubkeyB36><dTag>.nsite-host.com`

`pubkeyB36` 是作者的原始 32 字节公钥，使用 base36 编码（小写，数字 `0-9` 后跟字母 `a-z`，无填充），始终恰好为 50 个字符。

`snapshotIdB36` 是原始 32 字节快照事件 id，使用 base36 编码（小写，数字 `0-9` 后跟字母 `a-z`，无填充），始终恰好为 50 个字符。

`dTag` 是站点标识符（`d` 标签值），作为纯文本。它直接附加在 `pubkeyB36` 之后，无分隔符。

这种单标签格式避免了多级子域的泛域名证书限制。

如果主机服务器使用子域路由，它 `MAY` 在其自己的根域名 `nsite-host.com` 下提供任何内容（例如着陆页）。

子域示例：

- 根站点：`npub10phxfsms72rhafrklqdyhempujs9h67nye0p67qe424dyvcx0dkqgvap0e.nsite-host.com`
- 快照：`v<50-char-snapshotIdB36>.nsite-host.com`
- 命名站点：`<50-char-pubkeyB36><dTag>.nsite-host.com`

对于规范子域格式，主机服务器 `MUST` 按如下方式解析最左侧的 DNS 标签：

1. 如果标签是有效的 `npub`，解码它并解析根站点清单。
2. 否则，如果标签匹配 `^v[0-9a-z]{50}$`，将其视为快照标签，其中：
   - `snapshotIdB36` 是前导 `v` 之后的最后 50 个字符
   - 将 `snapshotIdB36` 解码为 32 字节事件 id
   - 解析该事件 id 的清单快照
3. 否则，如果标签匹配 `^[0-9a-z]{50}[a-z0-9-]{1,13}$` 且不以 `-` 结尾，将其视为命名站点标签，其中：
   - `pubkeyB36` 是前 50 个字符
   - `dTag` 是剩余的 1-13 个字符
   - 将 `pubkeyB36` 解码为 32 字节公钥
   - 使用 `dTag` 作为标识符（`d` 标签值）
   - 解析该公钥和标识符的命名站点清单

如果解析失败，主机服务器 `MUST` 将站点视为未找到。

#### 解析路径

一旦找到站点清单事件，主机服务器 `MUST` 从清单中的 `path` 标签提取路径到哈希的映射。主机服务器 `SHOULD` 查找第二个元素与请求路径匹配的 `path` 标签。

主机服务器 `SHOULD` 验证所提供任何 blob 的 SHA-256 哈希是否与相应 `path` 标签中的哈希匹配。如果 blob 哈希不匹配，主机服务器 `SHOULD` 将站点视为未找到。

##### 索引页面

如果请求路径不以文件名结尾，主机服务器 `MUST` 回退到使用 `index.html` 文件名。

例如：`/` -> `/index.html` 或 `/blog/` -> `/blog/index.html`

一旦主机服务器找到站点清单事件并定位到请求路径的匹配 `path` 标签，它应使用 `path` 标签第三个元素中定义的 SHA-256 哈希来检索文件。

主机服务器 `SHOULD` 优先使用站点清单事件中的 `server` 标签作为查询哪些 Blossom 服务器的提示。如果清单包含 `server` 标签，主机服务器 `SHOULD` 首先尝试从这些服务器检索文件。

如果公钥有 `10063` [BUD-03 用户服务器](https://github.com/hzrd149/blossom/blob/master/buds/03.md) 事件，服务器 `MUST` 尝试使用 [BUD-01](https://github.com/hzrd149/blossom/blob/master/buds/01.md#get-sha256---get-blob) 中定义的路径从列出的服务器检索文件。

如果公钥没有 `10063` 事件且清单中未找到 `server` 标签，主机服务器 `MUST` 以状态码 404 响应。

主机服务器 `MUST` 转发来自 Blossom 服务器的 `Content-Type` 和 `Content-Length` 标头。如果两者都未定义，主机服务器 `MAY` 根据请求路径中的文件扩展名设置 `Content-Type`。

##### 未找到

如果主机服务器无法找到站点清单事件或请求路径的匹配 `path` 标签，它 `MUST` 使用 `/404.html` 作为回退路径。

### 示例

#### 根站点清单

```yaml
{
  "content": "",
  "created_at": 1727373475,
  "id": "5324d695ed7abf7cdd2a48deb881c93b7f4e43de702989bbfb55a1b97b35a3de",
  "kind": 15128,
  "pubkey": "266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5",
  "sig": "f4e4a9e785f70e9fcaa855d769438fea10781e84cd889e3fcb823774f83d094cf2c05d5a3ac4aebc1227a4ebc3d56867286c15a6df92d55045658bb428fd5fb5",
  "tags": [
    ["path", "/index.html", "186ea5fd14e88fd1ac49351759e7ab906fa94892002b60bf7f5a428f28ca1c99"],
    ["path", "/about.html", "a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456"],
    ["path", "/favicon.ico", "fedcba0987654321fedcba0987654321fedcba0987654321fedcba0987654321"],
    ["x", "<site-aggregate-sha256>", "aggregate"],
    ["app", "31990:266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5:my-app", "wss://relay.example.com"],
    ["app", "32267:266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5:com.example.webapp", "wss://relay.example.com"],
    ["server", "https://blossom.example.com"],
    ["title", "My Nostr Site"],
    ["description", "A static website hosted on Nostr"],
    ["source", "https://github.com/example/my-nostr-site"]
  ]
}
```

#### 命名站点清单

```yaml
{
  "content": "",
  "created_at": 1727373475,
  "id": "a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456",
  "kind": 35128,
  "pubkey": "266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5",
  "sig": "f4e4a9e785f70e9fcaa855d769438fea10781e84cd889e3fcb823774f83d094cf2c05d5a3ac4aebc1227a4ebc3d56867286c15a6df92d55045658bb428fd5fb5",
  "tags": [
    ["d", "blog"],
    ["path", "/index.html", "186ea5fd14e88fd1ac49351759e7ab906fa94892002b60bf7f5a428f28ca1c99"],
    ["path", "/post.html", "a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456"],
    ["x", "<site-aggregate-sha256>", "aggregate"],
    ["app", "31990:266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5:my-blog-app", "wss://relay.example.com"],
    ["app", "32267:266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5:com.example.blog", "wss://relay.example.com"],
    ["server", "https://blossom.example.com"],
    ["title", "My Blog"],
    ["description", "A blog hosted on Nostr"],
    ["source", "https://github.com/example/my-nostr-blog"]
  ]
}
```

#### 复制的命名站点清单

```jsonc
{
  "content": "",
  "created_at": 1727373475,
  "id": "b1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456",
  "kind": 35128,
  "pubkey": "9f6815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd4000ff",
  "sig": "f4e4a9e785f70e9fcaa855d769438fea10781e84cd889e3fcb823774f83d094cf2c05d5a3ac4aebc1227a4ebc3d56867286c15a6df92d55045658bb428fd5fb5",
  "tags": [
    ["d", "blog"],
    // copied directly from the original named site
    ["a", "35128:266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5:blog"],
    // origin of this copy lineage; same as `a` for a direct copy
    ["A", "35128:266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5:blog"],
    ["path", "/index.html", "186ea5fd14e88fd1ac49351759e7ab906fa94892002b60bf7f5a428f28ca1c99"],
    ["path", "/post.html", "b1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456"],
    ["x", "<site-aggregate-sha256>", "aggregate"],
    ["title", "My Copied Blog"],
    ["description", "A copy of another named nsite"]
  ]
}
```

#### 从另一个复制站点派生出的复制站点

```jsonc
{
  "content": "",
  "created_at": 1727374475,
  "id": "c1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456",
  "kind": 35128,
  "pubkey": "7a6815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd4bbbb",
  "sig": "f4e4a9e785f70e9fcaa855d769438fea10781e84cd889e3fcb823774f83d094cf2c05d5a3ac4aebc1227a4ebc3d56867286c15a6df92d55045658bb428fd5fb5",
  "tags": [
    ["d", "blog"],
    // copied from another copied site, not directly from the origin
    ["a", "35128:9f6815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd4000ff:blog"],
    // still points at the original site where this lineage began
    ["A", "35128:266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5:blog"],
    ["path", "/index.html", "186ea5fd14e88fd1ac49351759e7ab906fa94892002b60bf7f5a428f28ca1c99"],
    ["path", "/post.html", "c1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456"],
    ["x", "<site-aggregate-sha256>", "aggregate"],
    ["title", "My Derived Blog"],
    ["description", "A copy of a copied nsite that preserves the original lineage"]
  ]
}
```

#### 清单快照

```jsonc
{
  "content": "",
  "created_at": 1727373475,
  "id": "9f8e7d6c5b4a39281716151413121110ffeeddccbbaa99887766554433221100",
  "kind": 5128,
  "pubkey": "266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5",
  "sig": "f4e4a9e785f70e9fcaa855d769438fea10781e84cd889e3fcb823774f83d094cf2c05d5a3ac4aebc1227a4ebc3d56867286c15a6df92d55045658bb428fd5fb5",
  "tags": [
    ["a", "35128:266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5:blog"],
    ["path", "/index.html", "186ea5fd14e88fd1ac49351759e7ab906fa94892002b60bf7f5a428f28ca1c99"],
    ["path", "/post.html", "a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456"],
    ["x", "<site-aggregate-sha256>", "aggregate"],
    ["server", "https://blossom.example.com"],
    ["title", "My Blog v1"],
    ["description", "An immutable snapshot of a blog hosted on Nostr"],
    ["source", "https://github.com/example/my-nostr-blog"]
  ]
}
```

#### 复制站点的清单快照

```jsonc
{
  "content": "",
  "created_at": 1727375475,
  "id": "8f8e7d6c5b4a39281716151413121110ffeeddccbbaa99887766554433221100",
  "kind": 5128,
  "pubkey": "9f6815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd4000ff",
  "sig": "f4e4a9e785f70e9fcaa855d769438fea10781e84cd889e3fcb823774f83d094cf2c05d5a3ac4aebc1227a4ebc3d56867286c15a6df92d55045658bb428fd5fb5",
  "tags": [
    // the site manifest this snapshot captures
    ["a", "35128:9f6815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd4000ff:blog"],
    // copied unchanged from the snapshotted site's origin lineage
    ["A", "35128:266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5:blog"],
    ["path", "/index.html", "186ea5fd14e88fd1ac49351759e7ab906fa94892002b60bf7f5a428f28ca1c99"],
    ["path", "/post.html", "b1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456"],
    ["x", "<site-aggregate-sha256>", "aggregate"],
    ["app", "31990:266815e0c9210dfa324c6cba3573b14bee49da4209a9456f9484e5106cd408a5:my-blog-app", "wss://relay.example.com"],
    ["title", "My Copied Blog v1"],
    ["description", "An immutable snapshot of a copied nsite"]
  ]
}
```
