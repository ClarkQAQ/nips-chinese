NIP-42
======

客户端到中继的认证
-------------------

`draft` `optional` `relay`

本 NIP 定义了一种客户端通过签名一个瞬态事件来向中继进行认证的方式。

## 动机

中继可能希望要求客户端进行认证以访问受限资源。例如：

  - 中继可能要求付费或其他形式的白名单才能发布事件——这可以简单通过将发布限制为由白名单公钥签名的事件来实现，但使用本 NIP，它们可以选择接受任何事件，只要它们是由已认证用户发布的；
  - 中继可能将 `kind: 4` 私信的访问限制为仅涉及聊天交流的参与方，为此，它可能要求客户端在查询该 kind 之前进行认证。
  - 中继可能将任何类型的订阅限制为付费用户或通过任何其他方式列入白名单的用户，并要求认证。

## 定义

### 新的客户端-中继协议消息

本 NIP 定义了一条新消息 `AUTH`，中继可以在支持认证时发送它，客户端也可以在想要进行认证时向中继发送它。当中继发送时，消息格式如下：

```
["AUTH", <质询-字符串>]
```

而当客户端发送时，格式如下：

```
["AUTH", <已签名的事件-JSON>]
```

客户端可以在连续的 `AUTH` 消息序列中提供来自多个公钥的已签名事件。中继必须相应地将所有公钥视为已认证。

客户端发送的 `AUTH` 消息必须像任何 `EVENT` 消息一样，用 `OK` 消息进行应答。

### 规范认证事件

已签名的事件是一个瞬态事件，不打算发布或查询，其 `kind` 必须为 `22242`，并且应该至少有两个标签：一个用于中继 URL，一个用于从中继接收到的质询字符串。中继必须排除将 `kind: 22242` 事件广播给任何客户端。`created_at` 应为当前时间。示例：

```yaml
{
  "kind": 22242,
  "tags": [
    ["relay", "wss://relay.example.com/"],
    ["challenge", "challengestringhere"]
  ],
  // 其他字段...
}
```

### `OK` 和 `CLOSED` 机器可读前缀

本 NIP 定义了两个可在 `OK`（响应客户端的事件写入）和 `CLOSED`（响应客户端被拒绝的订阅）中使用的新前缀：

- `"auth-required: "` - 当客户端尚未执行 `AUTH`，而中继要求这样做才能完成查询或写入事件时使用。
- `"restricted: "` - 当客户端已执行 `AUTH`，但用于执行认证的密钥仍未被中继允许或超出其授权时使用。

## 协议流程

在任何时候，中继都可以向客户端发送一条包含质询的 `AUTH` 消息。该质询在连接持续期间有效，直到中继发送另一个质询为止。客户端可以在任何时刻决定发送其 `AUTH` 事件，此后已认证的会话在连接持续期间有效。

### 响应 `REQ` 消息的 `auth-required`

鉴于中继可能只要求客户端为某些任务执行认证，例如响应 `REQ` 或接受 `EVENT` 写入，以下是一些常见的预期流程：

```
relay: ["AUTH", "<质询>"]
client: ["REQ", "sub_1", {"kinds": [4]}]
relay: ["CLOSED", "sub_1", "auth-required: we can't serve DMs to unauthenticated users"]
client: ["AUTH", {"id": "abcdef...", ...}]
client: ["AUTH", {"id": "abcde2...", ...}]
relay: ["OK", "abcdef...", true, ""]
relay: ["OK", "abcde2...", true, ""]
client: ["REQ", "sub_1", {"kinds": [4]}]
relay: ["EVENT", "sub_1", {...}]
relay: ["EVENT", "sub_1", {...}]
relay: ["EVENT", "sub_1", {...}]
relay: ["EVENT", "sub_1", {...}]
...
```

在这种情况下，来自中继的 `AUTH` 消息可以在客户端连接时立即发送，也可以在 `CLOSED` 发送之前立即发送。唯一的要求是_客户必须存储与该中继关联的质询_，以便能够响应 `auth-required` 的 `CLOSED` 消息。

### 响应 `EVENT` 消息的 `auth-required`

当客户端想要向中继写入 `EVENT` 时，同样的流程也适用，不同之处在于中继现在返回的是 `OK` 消息而不是 `CLOSED` 消息：

```
relay: ["AUTH", "<质询>"]
client: ["EVENT", {"id": "012345...", ...}]
relay: ["OK", "012345...", false, "auth-required: we only accept events from registered users"]
client: ["AUTH", {"id": "abcdef...", ...}]
relay: ["OK", "abcdef...", true, ""]
client: ["EVENT", {"id": "012345...", ...}]
relay: ["OK", "012345...", true, ""]
```

## 已签名事件验证

为了验证 `AUTH` 消息，中继必须确保：

  - `kind` 为 `22242`；
  - 事件的 `created_at` 接近当前时间（例如在约 10 分钟内）；
  - `"challenge"` 标签与之前发送的质询匹配；
  - `"relay"` 标签与中继 URL 匹配：
    - 可以使用 URL 规范化技术。大多数情况下，仅检查域名是否正确就足够了。
