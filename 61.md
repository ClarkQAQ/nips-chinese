NIP-61
======

Nutzaps
-------

`draft` `optional`

Nutzap 是一种 P2PK Cashu 代币，其中支付本身就是收据。

## 高层流程
Alice 因为她喜欢的事件 `event-id-1`，想要 nutzap 给 Bob 1 聪。

### Alice nutzap Bob
1. Alice 从 Bob 那里获取 `kind:10019` 事件，以查看 Bob 信任的铸币厂。
2. 她在该铸币厂铸造一个代币（或兑换一些她已在该铸币厂持有的代币），并以 Bob 在其 `kind:10019` 中列出的公钥进行 P2PK 锁定。
3. 她将 `kind:9321` 事件发布到 Bob 指示的中继，其中包含她铸造的证明。

### Bob 接收 nutzap
1. 在某个时刻，Bob 的客户端从中继获取标签为 p 的 `kind:9321` 事件。
2. Bob 的客户端将代币兑换到他的钱包中。

## Nutzap 信息事件
```yaml
{
    "kind": 10019,
    "tags": [
        [ "relay", "wss://relay1" ],
        [ "relay", "wss://relay2" ],
        [ "mint", "https://mint1", "usd", "sat" ],
        [ "mint", "https://mint2", "sat" ],
        [ "pubkey", "<p2pk-公钥>" ]
    ]
}
```

* `kind:10019` 是一个事件，用于帮助他人知道如何向用户汇款。
* `relay`：用户将从中读取代币事件的中继。如果某人想向用户汇款，应写入这些中继。
* `mint`：用户明确同意用于接收资金的铸币厂。客户端不应在此列表之外的铸币厂上发送资金，否则有资金损失的风险。可以使用额外的标记来列出铸币厂支持的基础单位。
* `pubkey`：必须用于 P2PK 锁定接收 nutzaps 的公钥——实现不得使用目标用户的主 Nostr 公钥。此公钥对应用户 [nip-60](60.md) _钱包事件_ 中加密的 `privkey` 字段。

### Nutzap 事件
事件 `kind:9321` 是由发送者发布的 nutzap 事件，标签 p 指向接收者。输出以接收者在其 `kind:10019` 事件中指示的公钥进行 P2PK 锁定。

客户端必须为 P2PK 锁定的公钥添加前缀 `"02"`（为了 nostr<>cashu 兼容性）。

```yaml
{
    "kind": 9321,
    "content": "感谢这个好主意。",
    "pubkey": "<发送者-公钥>",
    "tags": [
        [ "proof", "{\"amount\":1,\"C\":\"02277c66191736eb72fce9d975d08e3191f8f96afb73ab1eec37e4465683066d3f\",\"id\":\"000a93d6f8a1d2c4\",\"secret\":\"[\\\"P2PK\\\",{\\\"nonce\\\":\\\"b00bdd0467b0090a25bdf2d2f0d45ac4e355c482c1418350f273a04fedaaee83\\\",\\\"data\\\":\\\"02eaee8939e3565e48cc62967e2fde9d8e2a4b3ec0081f29eceff5c64ef10ac1ed\\\"}]\"}" ],
        [ "unit", "sat" ],
        [ "u", "https://stablenut.umint.cash" ],
        [ "e", "<被 nutzap 的事件-id>", "<中继提示>" ],
        [ "k", "<被 nutzap 的 kind>"],
        [ "p", "e9fbced3a42dcf551486650cc752ab354347dd413b307484e4fd1818ab53f991" ], // nutzap 接收者
    ]
}
```

* `.content` 是 nutzap 的可选评论
* `.tags`：
  * `proof` 是一个或多个以接收者在其 `kind:10019` 事件中指定的公钥进行 P2PK 锁定的证明，并包含 DLEQ 证明。
  * `unit` 证明的基础单位（例如：`sat`、`usd`、`eur`）。如果省略，默认为 `sat`。
  * `u` 是铸币厂 URL，与接收者的 `kind:10019` 中指定的完全相同。
  * `p` 是 nutzap 接收者的 Nostr 身份公钥。
  * `e` 是被 nutzap 的事件（如果有）。

## 发送 nutzap

* 发送者获取接收者的 `kind:10019`。
* 发送者在接收者列出的某个铸币厂铸造/兑换 ecash。
* 发送者以接收者在其 `kind:10019` 中指定的公钥进行 P2PK 锁定。

## 接收 nutzaps

客户端应 REQ 获取 nutzaps：
* 使用 `#u` 过滤他们期望从中接收 ecash 的铸币厂。
  * 这是为了防止与用户未明确声明的铸币厂进行交互。
* 使用 `since` 过滤，其值为同一用户最近创建的 `kind:7376` 事件的时间。
  * 这可以用作用户已兑换的 nutzaps 的标记——客户端可能选择使用其他类型的标记，包括内部状态——这只是一个可能方法的指南。

`{ "kinds": [9321], "#p": ["我的-公钥"], "#u": ["<铸币厂-1>", "<铸币厂-2>"], "since": <最近-kind-7376-的-created_at> }`.

收到新的 nutzap 时，客户端应将代币兑换到用户控制的钱包中，可以是 [NIP-60](60.md) 钱包、他们自己的 LN 钱包或任何其他方式。

### 更新 nutzap 兑换历史
在领取代币时，客户端应创建一个 `kind:7376` 事件，并使用 `e` 标签指向原始的 nutzap 事件。这是为了记录该代币已被领取（不应再次尝试），并作为向接收者发出的信号，表明 ecash 已被兑换。

多个 `kind:9321` 事件可以在同一个 `kind:7376` 事件中被标记。

```yaml
{
    "kind": 7376,
    "content": nip44_encrypt([
        [ "direction", "in" ], // in = 收到，out = 发出
        [ "amount", "1" ],
        [ "unit", "sat" ],
        [ "e", "<7375-事件-id>", "<中继提示>", "created" ] // 已创建的新代币事件
    ]),
    "tags": [
        [ "e", "<9321-事件-id>", "<中继提示>", "redeemed" ], // 已被兑换的 nutzap 事件
        [ "p", "<发送者-公钥>" ] // 9321 事件作者（nutzap 发送者）的公钥
    ]
}
```

兑换 nutzap 的事件应发布到发送者的 [NIP-65](65.md) "读取"中继。

### 验证 Cashu 打赏
在列出或统计任何给定事件接收到的打赏时，观察者客户端应：

* 检查接收用户是否发布了 `kind:10019` 事件，其中标记了铸造 cashu 的铸币厂。
* 检查代币是否锁定到用户在其 `kind:10019` 中列出的公钥。
* 查看 `u` 标签，并检查代币是否由 `kind:10019` 中列出的某个铸币厂发行。
* 本地验证所发送代币的 DLEQ 证明。

所有这些检查都可以离线完成（只要观察者拥有接收者铸币厂的密钥集和其 `kind:10019` 事件），因此过程应该相当快。

### 最终考虑
1. 客户端应引导其用户在其 `kind:10019` 事件中使用兼容 NUT-11（P2PK）和 NUT-12（DLEQ 证明）的铸币厂，以避免收到任何人可花费的 nutzaps。
2. 客户端应按照 NIP-65 的描述规范化并去重铸币厂 URL。
3. nutzap 事件必须包含接收者在其 `kind:10019` 中列出的某个铸币厂的证明，并发布到接收者的 NIP-65 中继，否则接收者可能永远不会看到该事件，导致发送者将代币捐赠给了铸币厂。
