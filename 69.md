NIP-69
======

点对点订单事件
-------------------------

`draft` `optional`

## 摘要

点对点（P2P）平台近年来经历了增长，虽然有越来越多的选择是积极的，但在 P2P 的特定情况下，拥有多种选择会导致流动性分散，意味着有时没有足够的资产可供交易。如果我们将所有这些独立的解决方案合并到一个大的订单池中，与单一权威机构控制流动性的中心化系统相比，它们将更具竞争力。

本 NIP 定义了点对点订单事件的简单标准，从而为所有参与的 P2P 平台创建一个大流动性池。

## 事件

事件是 [可寻址事件](01.md#kinds)，使用 `38383` 作为事件 kind，一个 P2P 事件如下所示：

```json
{
  "id": "84fad0d29cb3529d789faeff2033e88fe157a48e071c6a5d1619928289420e31",
  "pubkey": "dbe0b1be7aafd3cfba92d7463edbd4e33b2969f61bd554d37ac56f032e13355a",
  "created_at": 1702548701,
  "kind": 38383,
  "tags": [
    ["d", "ede61c96-4c13-4519-bf3a-dcf7f1e9d842"],
    ["k", "sell"],
    ["f", "VES"],
    ["s", "pending"],
    ["amt", "0"],
    ["fa", "100"],
    ["pm", "当面交易", "银行转账"],
    ["premium", "1"],
    [
      "rating",
      "{\"total_reviews\":1,\"total_rating\":3.0,\"last_rating\":3,\"max_rate\":5,\"min_rate\":1}"
    ],
    ["source", "https://t.me/p2plightning/xxxxxxx"],
    ["network", "mainnet"],
    ["layer", "lightning"],
    ["name", "Nakamoto"],
    ["g", "<地理哈希>"],
    ["bond", "0"],
    ["expires_at", "1719391096"],
    ["expiration", "1719995896"],
    ["y", "lnp2pbot"],
    ["z", "order"]
  ],
  "content": "",
  "sig": "7e8fe1eb644f33ff51d8805c02a0e1a6d034e6234eac50ef7a7e0dac68a0414f7910366204fa8217086f90eddaa37ded71e61f736d1838e37c0b73f6a16c4af2"
}
```

## 标签

- `d` < 订单 ID >: 订单的唯一标识符。
- `k` < 订单类型 >: `sell`（卖出）或 `buy`（买入）。
- `f` < 货币 >: 交易的资产，使用 [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) 标准。
- `s` < 状态 >: `pending`（待处理）、`canceled`（已取消）、`in-progress`（进行中）、`success`（成功）、`expired`（已过期）。
- `amt` < 金额 >: 要交易的比特币数量，以聪为单位，如果为 `0` 表示在接收者接受订单后将通过公共 API 获取聪的数量。
- `fa` < 法币金额 >: 交易的法币金额，对于范围订单，需要两个值，最小金额和最大金额。
- `pm` < 支付方式 >: 交易使用的支付方式，如果订单有多个支付方式，它们应使用逗号分隔。
- `premium` < 溢价 >: 挂单方愿意支付的溢价百分比。
- `source` [来源]: 订单的来源，可以是重定向到订单的 URL。
- `rating` [评分]: 挂单方的评分，本文档不定义评分如何计算，由各平台自行定义。
- `network` < 网络 >: 交易使用的网络，可以是 `mainnet`、`testnet`、`signet` 等。
- `layer` < 层 >: 交易使用的层，可以是 `onchain`、`lightning`、`liquid` 等。
- `name` [名称]: 挂单方的名称。
- `g` [地理哈希]: 交易的地理哈希，在当面交易中可能有用。
- `bond` [保证金]: 保证金金额，保证金是双方必须支付的安全押金。
- `expires_at` < 过期时间 >: 事件以 `pending` 状态发布的过期时间，在此时间之后事件状态应更改为 `expired`。
- `expiration` < 到期时间 >: 事件的到期时间，在此时间之后中继应删除它（[NIP-40](40.md)）。
- `y` < 平台 >: 创建订单的平台。
- `z` < 文档 >: `order`。

必需标签用 `<tag>` 括起来，可选标签用 `[tag]` 括起来。

## 实现

目前已在以下平台上实现：

- [Mostro](https://github.com/MostroP2P/mostro)
- [@lnp2pBot](https://github.com/lnp2pBot/bot)
- [Robosats](https://github.com/RoboSats/robosats/pull/1362)
- [Peach Bitcoin](https://github.com/Peach2Peach/peach-nostr-announcer-bot)

## 参考

- [Mostro 协议规范](https://mostro.network/protocol/)
- [点对点 NIP 提案的消息规范](https://github.com/nostr-protocol/nips/blob/8250274a22f4882f621510df0054fd6167c10c9e/31001.md)
- [n3xB](https://github.com/nobu-maeda/n3xb)
