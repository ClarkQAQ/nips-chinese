NIP-01
======

基本协议流程描述
-------------------------------

`draft` `mandatory` `relay`

本 NIP 定义了所有人都应实现的基本协议。新的 NIP 可以为此处描述的结构和流程添加新的可选（或强制）字段、消息和功能。

## 事件和签名

每个用户都有一个密钥对。签名、公钥和编码均按照 [Schnorr 签名标准（`secp256k1` 曲线）](https://bips.xyz/340) 执行。

唯一存在的对象类型是 `event`（事件），其在网络上的格式如下：

```yaml
{
  "id": <32-bytes lowercase hex-encoded sha256 of the serialized event data>,
  "pubkey": <32-bytes lowercase hex-encoded public key of the event creator>,
  "created_at": <unix timestamp in seconds>,
  "kind": <integer between 0 and 65535>,
  "tags": [
    [<arbitrary string>...],
    // ...
  ],
  "content": <arbitrary string>,
  "sig": <64-bytes lowercase hex of the signature of the sha256 hash of the serialized event data, which is the same as the "id" field>
}
```

要获得 `event.id`，我们对序列化后的事件进行 `sha256`。序列化是对以下结构的 UTF-8 JSON 序列化字符串（下文将描述）进行的：

```
[
  0,
  <pubkey, as a lowercase hex string>,
  <created_at, as a number>,
  <kind, as a number>,
  <tags, as an array of arrays of non-null strings>,
  <content, as a string>
]
```

为防止因实现差异导致同一事件产生不同的事件 ID，序列化时必须遵循以下规则：
- 应使用 UTF-8 进行编码。
- 输出 JSON 中不应包含空格、换行或其他不必要的格式。
- content 字段中的以下字符必须按所示方式转义，所有其他字符必须原样包含：
  - 换行符（`0x0A`），使用 `\n`
  - 双引号（`0x22`），使用 `\"`
  - 反斜杠（`0x5C`），使用 `\\`
  - 回车符（`0x0D`），使用 `\r`
  - 制表符（`0x09`），使用 `\t`
  - 退格符（`0x08`），使用 `\b`
  - 换页符（`0x0C`），使用 `\f`

### 标签

每个标签是由一个或多个字符串组成的数组，并有一些约定。请看下面的示例：

```yaml
{
  "tags": [
    ["e", "5c83da77af1dec6d7289834998ad7aafbd9e2191396d75ec3cc27f5a77226f36", "wss://nostr.example.com"],
    ["p", "f7234bd4c1394dda46d09f35bd384dd30cc552ad5541990f98844fb06676e9ca"],
    ["a", "30023:f7234bd4c1394dda46d09f35bd384dd30cc552ad5541990f98844fb06676e9ca:abcd", "wss://nostr.example.com"],
    ["alt", "reply"],
    // ...
  ],
  // ...
}
```

标签数组的第一个元素称为标签的 _名称_ 或 _键_，第二个元素称为标签的 _值_。因此我们可以说，上面的事件有一个 `e` 标签，其值为 `"5c83da77af1dec6d7289834998ad7aafbd9e2191396d75ec3cc27f5a77226f36"`；有一个 `alt` 标签，其值为 `"reply"`，依此类推。第二个元素之后的所有元素没有常规名称。

本 NIP 定义了 3 个标准标签，可在所有事件 kind 中使用，且含义相同。它们如下所示：

- `e` 标签，用于引用一个事件：`["e", <32-bytes lowercase hex of the id of another event>, <recommended relay URL, optional>, <32-bytes lowercase hex of the author's pubkey, optional>]`
- `p` 标签，用于引用另一个用户：`["p", <32-bytes lowercase hex of a pubkey>, <recommended relay URL, optional>]`
- `a` 标签，用于引用一个可寻址或可替换的事件
    - 对于可寻址事件：`["a", "<kind integer>:<32-bytes lowercase hex of a pubkey>:<d tag value>", <recommended relay URL, optional>]`
    - 对于普通可替换事件：`["a", "<kind integer>:<32-bytes lowercase hex of a pubkey>:", <recommended relay URL, optional>]`（注意：包含末尾的冒号）

按照惯例，所有单字母（仅英文字母：a-z, A-Z）键标签都应被中继索引，以便可以（例如）使用 `{"#e": ["5c83da77af1dec6d7289834998ad7aafbd9e2191396d75ec3cc27f5a77226f36"]}` 过滤器查询或订阅引用事件 `"5c83da77af1dec6d7289834998ad7aafbd9e2191396d75ec3cc27f5a77226f36"` 的事件。任何给定标签中只有第一个值被索引。

### Kinds

Kind 指定了客户端应如何解释每个事件的含义以及每个事件的其他字段（例如，`"r"` 标签在 kind 1 的事件中可能有一种含义，而在 kind 10002 的事件中则可能完全不同）。每个 NIP 都可以定义一组在其他地方未定义的 kind 的含义。例如，[NIP-10](10.md) 为社交媒体应用定义了 `kind:1` 文本笔记。

本 NIP 定义了一个基本 kind：

- `0`：**用户元数据**：`content` 被设置为一个字符串化的 JSON 对象 `{name: <昵称或全名>, about: <简短简介>, picture: <图片URL>}`，描述创建事件的用户。可以设置[额外的元数据字段](24.md#kind-0)。一旦收到同一公钥的新事件，中继可以删除旧事件。

同时还定义了 kind 范围的约定，以便于更轻松地进行实验和中继实现的灵活性：

- 对于满足 `1000 <= n < 10000 || 4 <= n < 45 || n == 1 || n == 2` 的 kind `n`，事件是 **常规的**（regular），这意味着它们都应被中继存储。
- 对于满足 `10000 <= n < 20000 || n == 0 || n == 3` 的 kind `n`，事件是 **可替换的**（replaceable），这意味着对于每个 `pubkey` 和 `kind` 的组合，中继必须只存储最新的一个事件，旧版本可以丢弃。
- 对于满足 `20000 <= n < 30000` 的 kind `n`，事件是 **瞬态的**（ephemeral），这意味着它们不应被中继存储。
- 对于满足 `30000 <= n < 40000` 的 kind `n`，事件通过其 `kind`、`pubkey` 和 `d` 标签值 **可寻址**（addressable）——这意味着对于每个 `kind`、`pubkey` 和 `d` 标签值的组合，中继必须只存储最新的一个事件，旧版本可以丢弃。

对于时间戳相同的可替换事件，应保留 id 最小（按字典序排列在前）的事件，并丢弃另一个。

在响应针对可替换事件的 `REQ` 消息时（例如 `{"kinds":[0],"authors":[<hex-key>]}`），即使中继存储了多个版本，也应只返回最新的一个。

这些只是约定，中继的实现可能有所不同。

## 客户端与中继之间的通信

中继暴露一个 WebSocket 端点，客户端可以连接。客户端应为每个中继打开一个 WebSocket 连接，并将其用于所有订阅。中继可以限制来自特定 IP/客户端等的连接数量。

中继必须只接受连接到单个端点，额外的路径段不应影响其行为。

### 从客户端到中继：发送事件和创建订阅

客户端可以发送 3 种类型的消息，这些消息必须是 JSON 数组，按照以下模式：

  * `["EVENT", <event JSON as defined above>]`，用于发布事件。
  * `["REQ", <subscription_id>, <filters1>, <filters2>, ...]`，用于请求事件并订阅新更新。
  * `["CLOSE", <subscription_id>]`，用于停止先前的订阅。

`<subscription_id>` 是一个任意非空字符串，最大长度 64 个字符。它表示每个连接上的一个订阅。中继必须独立管理每个 WebSocket 连接的 `<subscription_id>`。`<subscription_id>` 不保证全局唯一。

`<filtersX>` 是一个 JSON 对象，决定该订阅中哪些事件将被发送，它可以具有以下属性：

```yaml
{
  "ids": <a list of event ids>,
  "authors": <a list of lowercase pubkeys, the pubkey of an event must be one of these>,
  "kinds": <a list of a kind numbers>,
  "#<single-letter (a-zA-Z)>": <a list of tag values, for #e — a list of event ids, for #p — a list of pubkeys, etc.>,
  "since": <an integer unix timestamp in seconds. Events must have a created_at >= to this to pass>,
  "until": <an integer unix timestamp in seconds. Events must have a created_at <= to this to pass>,
  "limit": <maximum number of events relays SHOULD return in the initial query>
}
```

收到 `REQ` 消息后，中继应返回匹配过滤器的事件。在连接关闭、收到具有相同 `<subscription_id>` 的 `CLOSE` 事件，或使用相同的 `<subscription_id>` 发送新的 `REQ`（此时会创建新订阅并替换旧订阅）之前，任何新收到的事件都应发送到同一个 WebSocket。

包含列表的过滤器属性（`ids`、`authors`、`kinds` 和类似 `#e` 的标签过滤器）是包含一个或多个值的 JSON 数组。数组中至少有一个值必须与事件的相应字段匹配，条件才被视为匹配。对于标量事件属性（如 `authors` 和 `kind`），事件的该属性必须包含在过滤器列表中。对于标签属性（如 `#e`），一个事件可能有多个值，事件和过滤器条件的值必须至少有一个共同项。

`ids`、`authors`、`#e` 和 `#p` 过滤器列表必须包含精确的 64 字符小写十六进制值。

`since` 和 `until` 属性可用于指定订阅中返回事件的时间范围。如果过滤器包含 `since` 属性，则 `created_at` 大于或等于 `since` 的事件被视为匹配过滤器。`until` 属性类似，但要求 `created_at` 小于或等于 `until`。简而言之，如果满足 `since <= created_at <= until`，则事件匹配过滤器。

一个过滤器中指定的所有条件都必须满足，事件才能通过过滤器，即多个条件被解释为 `&&` 条件。

`REQ` 消息可以包含多个过滤器。在这种情况下，匹配任何一个过滤器的事件都将被返回，即多个过滤器被解释为 `||` 条件。

过滤器的 `limit` 属性仅对初始查询有效，之后必须被忽略。当存在 `limit: n` 时，假设初始查询返回的事件将是由 `created_at` 排序的最后 `n` 个事件。较新的事件应排在前面，如果时间相同，则 id 最小（按字典序排列在前）的事件应排在前面。中继应使用 `limit` 值来指导初始响应中返回的事件数量。返回较少的事件是可以接受的，但应避免返回（远远）更多的事件，以防止压垮客户端。

### 从中继到客户端：发送事件和通知

中继可以发送 5 种类型的消息，这些消息也必须是 JSON 数组，按照以下模式：

  * `["EVENT", <subscription_id>, <event JSON as defined above>]`，用于发送客户端请求的事件。
  * `["OK", <event_id>, <true|false>, <message>]`，用于指示接受或拒绝 `EVENT` 消息。
  * `["EOSE", <subscription_id>]`，用于指示已存储事件的 _结束_ 以及实时接收新事件的开始。
  * `["CLOSED", <subscription_id>, <message>]`，用于指示订阅已在服务器端结束。
  * `["NOTICE", <message>]`，用于向客户端发送人类可读的错误消息或其他信息。

本 NIP 没有定义 `NOTICE` 消息的发送或处理规则。

- `EVENT` 消息只能使用与客户端先前发起的订阅（使用上述 `REQ` 消息）相关的订阅 ID 发送。
- `OK` 消息必须作为对从客户端收到的 `EVENT` 消息的响应发送，当事件已被中继接受时，第三个参数必须设置为 `true`，否则为 `false`。第四个参数必须始终存在，但当第三个参数为 `true` 时，它可以是空字符串；否则，它必须是由一个机器可读的单字前缀后跟 `:` 再跟一个人类可读消息组成的字符串。一些示例：
  * `["OK", "b1a649ebe8...", true, ""]`
  * `["OK", "b1a649ebe8...", true, "pow: difficulty 25>=24"]`
  * `["OK", "b1a649ebe8...", true, "duplicate: already have this event"]`
  * `["OK", "b1a649ebe8...", false, "blocked: you are banned from posting here"]`
  * `["OK", "b1a649ebe8...", false, "blocked: please register your pubkey at https://my-expensive-relay.example.com"]`
  * `["OK", "b1a649ebe8...", false, "rate-limited: slow down there chief"]`
  * `["OK", "b1a649ebe8...", false, "invalid: event creation date is too far off from the current time"]`
  * `["OK", "b1a649ebe8...", false, "pow: difficulty 26 is less than 30"]`
  * `["OK", "b1a649ebe8...", false, "restricted: not allowed to write."]`
  * `["OK", "b1a649ebe8...", false, "error: could not connect to the database"]`
  * `["OK", "b1a649ebe8...", false, "mute: no one was listening to your ephemeral event and it wasn't handled in any way, it was ignored"]`
- `CLOSED` 消息必须作为对中继拒绝执行的 `REQ` 的响应发送。当中继决定在客户端断开连接或发送 `CLOSE` 之前在其一侧终止订阅时，也可以发送此消息。此消息使用与 `OK` 消息相同的模式，包含机器可读前缀和人类可读消息。一些示例：
  * `["CLOSED", "sub1", "unsupported: filter contains unknown elements"]`
  * `["CLOSED", "sub1", "error: could not connect to the database"]`
  * `["CLOSED", "sub1", "error: shutting down idle subscription"]`
- `OK` 和 `CLOSED` 的标准机器可读前缀为：`duplicate`、`pow`、`blocked`、`rate-limited`、`invalid`、`restricted`、`mute`，以及当以上都不适用时的 `error`。
