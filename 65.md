NIP-65
======

中继列表元数据
--------------

`草稿` `可选`

定义了一个使用 `kind:10002` 的可替换事件，用于公布用户通常**写入**的中继和用户通常从中**读取**提及的中继。

该事件必须包含一个 `r` 标签列表，列表值为中继 URL，并带有一个可选的 `read` 或 `write` 标记。如果省略标记，则该中继同时用于**读取**和**写入**。

```yaml
{
  "kind": 10002,
  "tags": [
    ["r", "wss://alicerelay.example.com"],
    ["r", "wss://brando-relay.com"],
    ["r", "wss://expensive-relay.example2.com", "write"],
    ["r", "wss://nostr-relay.example.com", "read"]
  ],
  "content": "",
  // 其他字段...
}
```

当**从**某个用户下载事件时，客户端应使用该用户的**写入**中继。

当**关于**某个用户（即该用户被标签提及）下载事件时，客户端应使用该用户的**读取**中继。

当发布事件时，客户端应：

- 将事件发送到作者的**写入**中继
- 将事件发送到每个被标签用户的**读取**中继
- 将作者的 `kind:10002` 事件发送到事件发布到的所有中继

### 大小

客户端应引导用户保持 `kind:10002` 列表精简（每个类别 2-4 个中继）。

### 可发现性

客户端应尽可能多地将作者的 `kind:10002` 事件传播到可行的中继，特别关注那些在任何时候自然充当这些中继列表的知名公共索引器的中继（大多数其他客户端和用户连接这些中继来发布和获取这些列表）。
