> __警告__  `不推荐`: 过于复杂，试试 [99](99.md)

NIP-15
======

Nostr 市场
-----------------

`draft` `unrecommended` `optional`

基于 [Diagon-Alley](https://github.com/lnbits/Diagon-Alley)。

已在 [NostrMarket](https://github.com/lnbits/nostrmarket) 和 [Plebeian Market](https://github.com/PlebeianTech/plebeian-market) 中实现。

## 术语

- `商家` - 拥有 NOSTR 密钥对的商品卖家
- `顾客` - 拥有 NOSTR 密钥对的商品买家
- `商品` - 由 `商家` 出售的物品
- `摊位` - 由 `商家` 管理的商品列表（一个 `商家` 可以有多个摊位）
- `市场` - 用于搜索 `摊位` 和购买 `商品` 的客户端软件

## Nostr 市场客户端

### 商家管理后台

`商家` 在此创建、更新和删除 `摊位` 和 `商品`，以及管理销售、支付和与 `顾客` 的沟通。

`商家` 管理软件可以完全在客户端运行，但为了 `方便` 和正常运行时间，实现方案可能包含一个监听 NOSTR 事件的服务器客户端。

### 市场

`市场` 软件应完全在客户端运行，可以是独立应用，也可以是纯前端网页。`顾客` 订阅不同商家的 NOSTR 公钥，这些 `商家` 的 `摊位` 和 `商品` 就会被列出并可搜索。市场客户端就像任何其他电子商务网站一样，具有购物车和结账功能。`市场` 可能还需要包含一个 `顾客` 支持区域，用于与 `商家` 进行直接消息通信。

## `商家` 发布/更新商品（事件）

商家可以发布以下事件：
| Kind      |                    | 描述                                        |
| --------- | ------------------ | --------------------------------------------------------------------------------------------------------------- |
| `0`       | `set_meta`         | 商家描述（与任何 `nostr` 公钥类似）。                                                |
| `30017`   | `set_stall`        | 创建或更新一个摊位。                                                                                       |
| `30018`   | `set_product`      | 创建或更新一个商品。                                                                                     |
| `4`       | `direct_message`   | 与顾客沟通。消息可以是纯文本或 JSON。                                          |
| `5`       | `delete`           | 删除一个商品或一个摊位。                                                                                    |

### 事件 `30017`: 创建或更新一个摊位。

**事件内容**

```json
{
  "id": <string, 由商家生成的ID。不推荐使用顺序ID（`0`、`1`、`2`...）>,
  "name": <string, 摊位名称>,
  "description": <string (可选), 摊位描述>,
  "currency": <string, 使用的货币>,
  "shipping": [
    {
      "id": <string, 配送区域ID，由商家生成>,
      "name": <string (可选), 区域名称>,
      "cost": <float, 配送基础费用。货币在摊位级别定义>,
      "regions": [<string, 此区域包含的地区>]
    }
  ]
}
```

无需解释的字段：
 - `shipping`:
   - 此摊位可能的配送区域数组。
   - 顾客必须从这些配送区域中选择恰好一个。
   - 配送到不同区域可能有不同的费用。对于某些商品（例如数字商品），费用可以为零。
   - `id` 是商家使用的内部值。此值必须在顾客选择时返回。
   - 每个配送区域包含发往该区域的订单的基础费用，但如果某商品的配送费用高于基础费用指定的金额，也可以为该商品指定具体的配送费用。

**事件标签**

```yaml
{
  "tags": [["d", <string, 摊位ID]],
  // 其他字段...
}
```
 - `d` 标签是必需的，其值必须与摊位 `id` 相同。

### 事件 `30018`: 创建或更新一个商品

**事件内容**

```json
{
  "id": <string, 由商家生成的ID（不推荐使用顺序ID）>,
  "stall_id": <string, 此商品所属摊位的ID>,
  "name": <string, 商品名称>,
  "description": <string (可选), 商品描述>,
  "images": <[string], 图片URL数组，可选>,
  "currency": <string, 使用的货币>,
  "price": <float, 商品价格>,
  "quantity": <int 或 null, 可用数量>,
  "specs": [
    [<string, 规格键>, <string, 规格值>]
  ],
  "shipping": [
    {
      "id": <string, 配送区域ID（必须与摊位定义的区域之一匹配）>,
      "cost": <float, 配送附加费。货币在摊位级别定义>
    }
  ]
}
```

无需解释的字段：
 - `quantity` 对于无限供应的商品（如数字商品或服务）可以为 null
 - `specs`:
   - 可选的键值对数组。允许客户 UI 以结构化的方式展示商品规格。也支持商品之间的比较
   - 例如：`[["operating_system", "Android 12.0"], ["screen_size", "6.4 inches"], ["connector_type", "USB Type C"]]`

    _开放问题_：是否将 `spec` 移到事件的 `tags` 部分？

- `shipping`:
   - 可选的附加费用数组，用于每个配送区域，仅适用于需要特殊配送费用的商品（需在摊位定义的基础配送费用之上添加）
   - `id` 应与摊位 `shipping` 字段中定义的配送区域 ID 匹配
   - 要计算订单的总配送费用，用户将在结账时选择一个配送选项，然后客户端必须考虑以下费用：
     - 所选配送选项的 `摊位基础费用`
     - 商品数量乘以 `商品中指定的配送费用`（如果有的话）。

**事件标签**

```yaml
  "tags": [
    ["d", <string, 商品ID],
    ["t", <string (可选), 商品类别],
    ["t", <string (可选), 商品类别],
    // 其他字段...
  ],
  ...
```

 - `d` 标签是必需的，其值必须与商品 `id` 相同。
 - `t` 标签是可搜索标签，表示商品可以属于的不同类别（`食品`、`水果`）。可以有多个 `t` 标签。

## 结账事件

所有结账事件以 JSON 字符串形式通过 [NIP-04](04.md) 发送。

`商家` 和 `顾客` 可以交换代表不同动作的 JSON 消息。每个 `JSON` 消息 `必须` 有一个 `type` 字段指示该 JSON 代表的含义。可能的类型：

| 消息类型 | 发送方   | 描述             |
|--------------|----------|---------------------|
| 0            | 顾客 | 新订单           |
| 1            | 商家 | 付款请求     |
| 2            | 商家 | 订单状态更新 |

### 第1步：`顾客` 下单（事件）
以下 JSON 放在 [NIP-04](04.md) 的 content 中。

```json
{
  "id": <string, 由顾客生成的ID>,
  "type": 0,
  "name": <string (可选), ???>,
  "address": <string (可选), 对于实物商品应提供地址>,
  "message": <string (可选), 给商家的留言>,
  "contact": {
    "nostr": <32字节十六进制公钥>,
    "phone": <string (可选), 如果顾客希望被电话联系>,
    "email": <string (可选), 如果顾客希望被邮件联系>
  },
  "items": [
    {
      "product_id": <string, 商品ID>,
      "quantity": <int, 顾客订购的商品数量>
    }
  ],
  "shipping_id": <string, 配送区域ID>
}

```

_开放问题_：`contact.nostr` 是否是必需的？

### 第2步：`商家` 请求付款（事件）

由商家发送回执以请求付款。商家可验证的任何付款方式均有效。

以下 JSON 放在 [NIP-04](04.md) 的 `content` 中。

`payment_options`/`type` 包括：

- `url` 付款页面的 URL，如 stripe、paypal、btcpayserver 等
- `btc` 链上比特币地址
- `ln` 比特币闪电发票
- `lnurl` 比特币 lnurl-pay

```json
{
  "id": <string, 订单ID>,
  "type": 1,
  "message": <string, 给顾客的消息，可选>,
  "payment_options": [
    {
      "type": <string, 选项类型>,
      "link": <string, url、btc地址、ln发票等>
    },
    {
      "type": <string, 选项类型>,
      "link": <string, url、btc地址、ln发票等>
    },
    {
      "type": <string, 选项类型>,
      "link": <string, url、btc地址、ln发票等>
    }
  ]
}
```

### 第3步：`商家` 验证付款/已发货（事件）

在收到并处理付款后发送。

以下 JSON 放在 [NIP-04](04.md) 的 `content` 中。

```json
{
  "id": <string, 订单ID>,
  "type": 2,
  "message": <string, 给顾客的消息>,
  "paid": <bool: 是否已收到付款>,
  "shipped": <bool: 是否已发货>,
}
```

## 自定义市场

使用 [NIP-19](19.md#shareable-identifiers-with-extra-metadata) 中的 `naddr` 创建自定义用户体验。使用 `naddr` 可以轻松共享市场事件，同时包含丰富的元数据集。这些元数据可以包括中继、商家资料等。随后，它允许将商家分组到一个市场中，使市场创建者能够配置市场的用户界面和用户体验，并共享该市场。这种自定义可以包含市场名称、描述、标志、横幅、主题甚至配色方案等元素，提供量身定制且独特的市场体验。

### 事件 `30019`: 创建或更新市场 UI/UX

**事件内容**

```yaml
{
  "name": <string (可选), 市场名称>,
  "about": <string (可选), 市场描述>,
  "ui": {
    "picture": <string (可选), 市场标志图片 URL>,
    "banner": <string (可选), 市场标志横幅 URL>,
    "theme": <string (可选), 市场主题>,
    "darkMode": <bool, 是否暗色模式>
  },
  "merchants": [公钥数组 (可选)],
  // 其他字段...
}
```

此事件利用 naddr 实现市场配置的全面自定义和共享，营造独特且引人入胜的市场环境。

## 拍卖

### 事件 `30020`: 创建或更新作为拍卖出售的商品

**事件内容**：
```json
{
    "id": <String, 由商家生成的 UUID。不推荐使用顺序ID（`0`、`1`、`2`...）>,
    "stall_id": <String, 此商品所属摊位的 UUID>,
    "name": <String, 商品名称>,
    "description": <String (可选), 商品描述>,
    "images": <[String], 图片 URL 数组，可选>,
    "starting_bid": <int>,
    "start_date": <int (可选) UNIX 时间戳，拍卖开始/将开始的日期>,
    "duration": <int, 拍卖将持续的秒数，不包括可能发生的时间延长>,
    "specs": [
        [<String, 规格键>, <String, 规格值>]
    ],
    "shipping": [
        {
            "id": <String, 配送区域的 UUID。必须与摊位定义的区域之一匹配>,
            "cost": <float, 配送附加费。货币在摊位级别定义>
        }
    ]
}
```

> [!NOTE]
> 作为拍卖出售的商品在结构上与固定价格商品非常相似，但有一些值得注意的重要区别。

* `start_date` 可以设置为未来的日期（如果拍卖计划在该日期开始），如果开始日期未知/隐藏，则可以省略。如果未指定开始日期，则稍后需要编辑拍卖以设置实际日期。

* 拍卖在 `start_date` 之后运行由 `duration` 指定的初始秒数。

### 事件 `1021`: 出价

```yaml
{
    "content": <int, 聪的数量>,
    "tags": [["e", <要竞价的拍卖的事件 ID>]],
    // 其他字段...
}
```

出价是 kind `1021` 的事件，`content` 字段以拍卖货币指定金额。出价必须引用一个拍卖。

> [!NOTE]
> 拍卖可以被作者编辑任意多次（它们是"可寻址事件"）——即使是在开始日期之后，但在收到第一次出价后就不能再编辑！这是通过出价引用拍卖的事件 ID（而非商品 UUID）来强制执行的，而事件 ID 会随着拍卖商品的每个新版本而改变。因此，出价始终附加到某一个"版本"。在出价后编辑拍卖将导致新商品失去该出价！

### 事件 `1022`: 出价确认

**事件内容**：

```json
{
    "status": <String, "accepted" | "rejected" | "pending" | "winner">,
    "message": <String (可选)>,
    "duration_extended": <int (可选), 秒数>
}
```

**事件标签**：
```json
  "tags": [["e" <被确认的出价的事件 ID>], ["e", <拍卖的事件 ID>]],
```

出价在商家确认之前不应被视为其他客户端的有效出价。因此，客户端应订阅他们所关注的每个拍卖的*出价确认*事件（kind `1022`）以及实际出价，并应检查出价确认的公钥是否与商家的公钥匹配（除了检查签名之外）。

`content` 字段是一个 JSON，至少包含一个 `status`。`winner` 是拍卖结束后，商家选定获胜出价后对该出价的回复方式。

出价被标记为 `rejected` 或 `pending` 的原因取决于商家的实现和配置——它们可以是基本的验证错误（金额太低），也可以是竞标者被列入黑名单或竞标者缺乏足够的*信任度*，这可能导致出价被标记为 `pending`，直到完成足够的验证。两者的区别在于 `pending` 出价*可能*在竞标者采取额外步骤后被批准，而 `rejected` 出价则不能事后批准。

在 `content` JSON 中可以出现一个额外的 `message` 字段，以提供关于出价被 `rejected` 或 `pending` 的进一步上下文。

另一件可能发生的情况是——如果出价非常接近拍卖的结束日期——商家可能决定将拍卖持续时间延长几分钟。这通过在出价确认中传递一个 `duration_extended` 字段来实现，该字段包含一个秒数，初始持续时间将延长相应的秒数。因此，拍卖的实际结束日期始终是 `start_date + duration + (SUM(c.duration_extended) FOR c in all confirmations)`。

## 顾客支持事件

顾客支持通过指定的任何通信方式处理。如果通过 nostr 通信，则使用 [NIP-04](04.md)。

## 附加信息

标准数据模型可参见 [此处](https://raw.githubusercontent.com/lnbits/nostrmarket/main/models.py)
