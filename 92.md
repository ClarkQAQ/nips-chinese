NIP-92
======

媒体附件元数据（`imeta`）
------------------------------------

可以通过在事件内容中包含 URL 以及匹配的 `imeta` 标签来向事件添加媒体附件（图片、视频和其他文件）。

`imeta`（"内联元数据"）标签可以添加关于事件内容中媒体 URL 的信息。每个 `imeta` 标签应与事件内容中的一个 URL 匹配。客户端可以用富媒体预览替换 imeta URL。

`imeta` 标签是可变参数的，每个条目是一个空格分隔的键/值对。
每个 `imeta` 标签必须有一个 `url` 和至少一个其他字段。`imeta` 可以包含
[NIP 94](./94.md) 中指定的任何字段。每个 URL 应只有一个 `imeta` 标签。

## 示例

```json
{
  "content": "More image metadata tests don't mind me https://nostr.build/i/my-image.jpg",
  "kind": 1,
  "tags": [
    [
      "imeta",
      "url https://nostr.build/i/my-image.jpg",
      "m image/jpeg",
      "blurhash eVF$^OI:${M{o#*0-nNFxakD-?xVM}WEWB%iNKxvR-oetmo#R-aen$",
      "dim 3024x4032",
      "alt A scenic photo overlooking the coast of Costa Rica",
      "x <sha256 hash as specified in NIP 94>",
      "fallback https://nostrcheck.me/alt1.jpg",
      "fallback https://void.cat/alt1.jpg"
    ]
  ]
}
```

## 推荐客户端行为

在新发帖期间上传文件时，客户端可以在文件上传并包含到帖子后包含此元数据。

在撰写帖子时粘贴 URL 时，客户端可以下载文件并在帖子发送前添加此元数据。

客户端可以忽略与事件内容中 URL 不匹配的 `imeta` 标签。
