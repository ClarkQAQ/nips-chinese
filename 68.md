NIP-68
======

图片优先信息流
-----------

`draft` `optional`

本 NIP 定义了专用于图片优先客户端的事件 kind `20`。图片必须自包含。它们托管在外部，并使用 `imeta` 标签引用。

其理念是此类事件服务于类似 Instagram、Flickr、Snapchat 或 9GAG 等平台的 Nostr 客户端，在这些平台中图片本身在用户体验中占据中心地位。

## 图片事件

图片事件包含 `title` 标签和 `.content` 中的描述。

它们可以包含多张图片作为单个帖子展示。

```yaml
{
  "id": <32-bytes lowercase hex-encoded SHA-256 of the the serialized event data>,
  "pubkey": <32-bytes lowercase hex-encoded public key of the event creator>,
  "created_at": <Unix timestamp in seconds>,
  "kind": 20,
  "content": "<帖子描述>",
  "tags": [
    ["title", "<帖子短标题>"],

    // 图片数据
    [
      "imeta",
      "url https://nostr.build/i/my-image.jpg",
      "m image/jpeg",
      "thumbhash zOcFFIIKmXdCinaXaHcmcHUFSA==",
      "blurhash eVF$^OI:${M{o#*0-nNFxakD-?xVM}WEWB%iNKxvR-oetmo#R-aen$",
      "dim 3024x4032",
      "alt A scenic photo overlooking the coast of Costa Rica",
      "x <sha256 hash as specified in NIP 94>",
      "fallback https://nostrcheck.me/alt1.jpg",
      "fallback https://void.cat/alt1.jpg"
    ],
    [
      "imeta",
      "url https://nostr.build/i/my-image2.jpg",
      "m image/jpeg",
      "thumbhash zOcFFIIKmXgzmWaXWIcmcFQEGA",
      "dim 3024x4032",
      "alt Another scenic photo overlooking the coast of Costa Rica",
      "x <sha256 hash as specified in NIP 94>",
      "fallback https://nostrcheck.me/alt2.jpg",
      "fallback https://void.cat/alt2.jpg",

      "annotate-user <32-bytes hex of a pubkey>:<posX>:<posY>" // 标记图片中特定位置的用户
    ],

    ["content-warning", "<reason>"], // 如果为 NSFW

    // 被标记的用户
    ["p", "<32-bytes hex of a pubkey>", "<optional recommended relay URL>"],
    ["p", "<32-bytes hex of a pubkey>", "<optional recommended relay URL>"],

    // 指定媒体类型，以便客户端通过过滤器筛选支持的类型
    ["m", "image/jpeg"],

    // 每张图片的哈希值，使其可查询
    ["x", "<sha256>"]

    // 话题标签
    ["t", "<tag>"],
    ["t", "<tag>"],

    // 位置
    ["location", "<location>"], // 城市名、州、国家
    ["g", "<geohash>"],

    // 当图片中包含文字时，添加标签表示语言
    ["L", "ISO-639-1"],
    ["l", "en", "ISO-639-1"]
  ]
}
```

`imeta` 标签的 `annotate-user` 在图片的特定位置放置一个用户链接。

仅接受以下媒体类型：
- `image/apng`：动画可移植网络图形（APNG）
- `image/avif`：AV1 图像文件格式（AVIF）
- `image/gif`：图形交换格式（GIF）
- `image/jpeg`：联合图像专家组图片（JPEG）
- `image/png`：可移植网络图形（PNG）
- `image/webp`：Web 图片格式（WEBP）

图片事件可以与 [NIP-71](71.md) 的 kind `22` 一起使用，在同一信息流中显示短视频。
