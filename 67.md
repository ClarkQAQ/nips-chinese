NIP-67
======

EOSE 完整性提示
---------------

`草稿` `可选`

本 NIP 扩展了 [NIP-01](01.md) 中定义的 `EOSE` 消息，增加了一个可选的第三个元素，用于向客户端发出信号，表明中继已发送了与订阅过滤器匹配的所有已存储事件。

## 动机

NIP-01 中定义的 `EOSE` 标志着已存储事件与实时事件之间的边界。它并没有告知客户端是否还有更多中继选择不发送的已存储事件。中继通常会强制执行一个独立于客户端 `limit` 的内部上限（例如，每个过滤器 500 个事件）。当今的客户端通过比较收到的事件数量与请求的 `limit` 来猜测完整性：

- 如果到达的事件少于 `limit`，则假定完成。
- 否则，发出另一个 `REQ`，并将 `until` 设置为收到的最旧事件的 `created_at`，然后重复。

这种启发式方法有两个众所周知的问题：

1. 当中继的内部上限低于客户端的 `limit` 时（例如，上限为 300，客户端请求 500），客户端收到 300 个事件并错误地认为结果是完整的，从而静默丢失数据。
2. 当匹配事件的总数恰好等于中继的上限时，客户端无法判断，必须发出额外的 `REQ` 只是为了观察到零结果。一个上限为 300 个事件的中继，对于任何耗尽该上限的订阅，总是至少被查询两次。

`EOSE` 上的一个额外字段就消除了这种歧义。

## 规范

实现本 NIP 的中继可以在 `EOSE` 消息中追加第三个元素：一个提示字符串数组。

```
["EOSE", <subscription_id>, [<hint>, ...]]
```

目前定义了两个提示值：

- `"finish"`：中继已发送了与订阅过滤器匹配的所有已存储事件。客户端不应进一步对此订阅进行分页。
- `"more"`：中继持有比已发送的更多匹配的已存储事件。客户端应进行分页以检索它们。中继可以发送此提示，但非必须，因为在每个存储后端上检测是否存在更多事件并不廉价。

该数组可以同时携带多个提示，也可以为空。客户端必须忽略未知的提示值而不报错。

它们的存在是决定性的；它们的缺失则不是。当中继省略了第三个元素，或既未发送 `"finish"` 也未发送 `"more"` 时，客户端应像往常一样使用 `until = 收到的最旧事件的 created_at` 进行分页。

不实现本 NIP 的客户端将忽略额外的元素，因为带有尾随元素的 JSON 数组是符合标准的解析器所接受的，并且现有的实现按位置索引 `EOSE`。不实现本 NIP 的中继继续发送两个元素的 `EOSE`，客户端则回退到上述传统启发式方法。

该提示仅指已存储事件。它对新事件的实时投递没有影响，实时投递将继续按照常规的 NIP-01 规则进行，直到客户端发送 `CLOSE` 或中继发送 `CLOSED`。

本 NIP 的未来版本可能定义额外的提示值。实现应接受未知的提示值而不报错，并将其视为不存在。

## 关于 `created_at` 并列的语义

当中继未发送 `"finish"` 时，多个事件可能共享响应中最旧的 `created_at` 值。使用 `until = oldest_created_at` 进行分页的客户端可能会错过共享该时间戳的事件。中继应尽可能提前内部游标，使得具有边界 `created_at` 的所有事件都包含在一个响应中，并且仅当没有更旧的事件剩余时才发出 `"finish"`。

## 中继通告

实现本 NIP 的中继应在其 [NIP-11](11.md) 文档的 `supported_nips` 字段中包含 `67`。

## 示例

中继拥有的匹配事件超过其内部上限（传统形式的响应，客户端分页）：

```
["REQ", "sub1", {"authors": ["abcd..."], "kinds": [1]}]
["EVENT", "sub1", {...}]
... (共 300 个事件)
["EOSE", "sub1"]
```

中继已发送所有匹配的已存储事件：

```
["REQ", "sub2", {"ids": ["abcd..."]}]
["EVENT", "sub2", {...}]
["EOSE", "sub2", ["finish"]]
```

中继还有更多匹配事件并明确说明：

```
["REQ", "sub2b", {"authors": ["abcd..."], "kinds": [1]}]
["EVENT", "sub2b", {...}]
... (共 300 个事件)
["EOSE", "sub2b", ["more"]]
```

中继未实现本 NIP（传统行为——客户端回退到启发式方法）：

```
["REQ", "sub3", {"kinds": [1], "limit": 500}]
["EVENT", "sub3", {...}]
... (N 个事件)
["EOSE", "sub3"]
```
