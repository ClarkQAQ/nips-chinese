NIP-78
======

任意的自定义应用数据
-------------------------

`draft` `optional`

本 NIP 的目标是为不关心互操作性的自定义应用提供类似 [remoteStorage](https://remotestorage.io/) 的能力。

尽管互操作性很好，但某些应用不希望或不需要互操作性，这对它们来说也没有意义。但 Nostr 仍然可以以"自带数据库"的方式为这些应用提供通用数据存储服务，例如：用户打开应用并以某种方式输入他们偏好的用于存储的中继，从而使这些应用能够将应用特定的数据存储在其中。

## Nostr 事件

本 NIP 指定使用事件 kind `30078`（一个_可寻址_事件），其 `d` 标签包含对应用名称和上下文的引用——或任何其他任意字符串。`content` 和其他 `tags` 可以是任何内容或任何格式。

它还定义了 kind `78`（一个_普通_事件），用于应用需要存储和查询同一类型的多个事件的情况。应用应使用一些唯一标签（甚至可以是 `d` 标签）来将这些事件分组在一起。

## 一些用例

 - Nostr 客户端（及其他与 Nostr 无关的应用）上的用户个人设置
 - 客户端开发者向用户传播动态参数而无需用户更新的方式
 - 由与 Nostr 无关的应用生成的个人私有数据，但允许用户使用 Nostr 中继作为其个人数据库
