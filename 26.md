> __警告__ `不推荐`: 增加不必要的负担且收益甚微

NIP-26
=======

委托事件签名
-----------------------

`草案` `不推荐` `可选` `中继`

本 NIP 定义了如何委托事件，以便它们可以由其他密钥对签名。

此提案的另一个应用是抽象掉与客户端交互时对"根"密钥对的使用。例如，用户可以为每个要使用的客户端生成新的密钥对，并授权这些密钥对代表其根公钥生成事件，而根密钥对则存储在冷存储中。

#### 引入"委托"标签

本 NIP 引入了一个新标签：`delegation`，其格式如下：

```json
[
  "delegation",
  <委托方的公钥>,
  <条件查询字符串>,
  <委托令牌：委托字符串的 sha256 哈希的 64 字节 Schnorr 签名>
]
```

##### 委托令牌

**委托令牌**应为以下字符串的 sha256 哈希的 64 字节 Schnorr 签名：

```
nostr:delegation:<发布者（受托方）的公钥>:<条件查询字符串>
```

##### 条件查询字符串

以下字段和运算符在上述查询字符串中得到支持：

**字段**：
1. `kind`
   - **运算符**：
     - `=${KIND_NUMBER}` - 受托方只能签署此类型的事件
2. `created_at`
   - **运算符**：
     - `<${TIMESTAMP}` - 受托方只能签署***在***指定时间戳之前创建的事件
     - `>${TIMESTAMP}` - 受托方只能签署***在***指定时间戳之后创建的事件

要创建单个条件，必须使用支持的字段和运算符。单个查询字符串中可以使用多个条件，包括在同一字段上。条件必须用 `&` 组合。

例如，以下条件字符串是有效的：

- `kind=1&created_at<1675721813`
- `kind=0&kind=1&created_at>1675721813`
- `kind=1&created_at>1674777689&created_at<1675721813`

对于绝大多数用例，建议：
1. 查询字符串应包含反映当前时间的 `created_at` ***之后***条件，以防止受托方代表委托方发布历史笔记。
2. 查询字符串应包含一个不为空且不是某个极其遥远的未来时间的 `created_at` ***之前***条件。如果委托没有时间范围限制，其暴露的安全风险与直接使用根密钥进行认证相似。

#### 示例

```
# 委托方：
私钥: ee35e8bb71131c02c1d7e73231daa48e9953d329a4b701f7133c8f46dd21139c
公钥: 8e0d3d3eb2881ec137a11debe736a9086715a8c8beeeda615780064d68bc25dd

# 受托方：
私钥: 777e4f60b4aa87937e13acc84f7abcc3c93cc035cb4c1e9f7a9086dd78fffce1
公钥: 477318cfb5427b9cfc66a9fa376150c1ddbc62115ae27cef72417eb959691396
```

授权受托方（477318cf）从现在起未来 30 天内发布笔记的委托字符串，假设当前时间戳为 `1674834236`。
```json
nostr:delegation:477318cfb5427b9cfc66a9fa376150c1ddbc62115ae27cef72417eb959691396:kind=1&created_at>1674834236&created_at<1677426236
```

委托方（8e0d3d3e）然后对以上委托字符串的 SHA256 哈希进行签名，其结果即为委托令牌：
```
6f44d7fe4f1c09f3954640fb58bd12bae8bb8ff4120853c4693106c82e920e2b898f1f9ba9bd65449a987c39c0423426ab7b53910c0c6abfb41b30bc16e5f524
```

受托方（477318cf）现在可以代表委托方（8e0d3d3e）构建事件。受托方用自己的私钥对事件进行签名并发布。
```json
{
  "id": "e93c6095c3db1c31d15ac771f8fc5fb672f6e52cd25505099f62cd055523224f",
  "pubkey": "477318cfb5427b9cfc66a9fa376150c1ddbc62115ae27cef72417eb959691396",
  "created_at": 1677426298,
  "kind": 1,
  "tags": [
    [
      "delegation",
      "8e0d3d3eb2881ec137a11debe736a9086715a8c8beeeda615780064d68bc25dd",
      "kind=1&created_at>1674834236&created_at<1677426236",
      "6f44d7fe4f1c09f3954640fb58bd12bae8bb8ff4120853c4693106c82e920e2b898f1f9ba9bd65449a987c39c0423426ab7b53910c0c6abfb41b30bc16e5f524"
    ]
  ],
  "content": "Hello, world!",
  "sig": "633db60e2e7082c13a47a6b19d663d45b2a2ebdeaf0b4c35ef83be2738030c54fc7fd56d139652937cdca875ee61b51904a1d0d0588a6acd6168d7be2909d693"
}
```

如果条件满足（在本例中为 `kind=1`、`created_at>1674834236` 和 `created_at<1677426236`），并且经验证委托令牌与原始委托字符串中的条件一致，则该事件应被视为有效的委托。

客户端应显示委托笔记，就好像它是由委托方（8e0d3d3e）直接发布的一样。

#### 中继与客户端支持

中继应通过同时查询 `pubkey` 和委托标签 `[1]` 值来响应诸如 `["REQ", "", {"authors": ["A"]}]` 之类的请求。

中继应允许委托方（8e0d3d3e）删除由受托方（477318cf）发布的事件。
